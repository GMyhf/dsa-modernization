{
  "id": "string_class",
  "title": "字符串类 String",
  "listings": [
    {
      "id": "代码4.1",
      "anchor": "class String {",
      "test": "默认构造是空串"
    },
    {
      "id": "算法4.3",
      "anchor": "String& concatenate(const char* s)",
      "test": "concatenate 接在串尾"
    },
    {
      "id": "算法4.4",
      "anchor": "int compare(const String& other) const noexcept",
      "test": "小于时 compare 为负"
    },
    {
      "id": "算法4.5",
      "anchor": "String substr(size_type pos, size_type len) const",
      "test": "从中间抽取"
    }
  ],
  "standard": "c++17",
  "owner": "claude",
  "notes": "原书缺陷：算法4.4 的 assert(str != '\\0') 是指针与整数比较，本身编译不过；String(char* s) 使书中自己的例子 String s1 = \"Hello\" 在 C++11 起为非法转换（-Werror 下即错误）；算法4.5 越界时 return NULL，随后 strlen(nullptr) 当场 SEGV。详见 legacy.md。"
}
