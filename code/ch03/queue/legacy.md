# 队列

原书代码3.13–3.15 用出参加 bool 和容器内 `cout` 表达空队列；现代 `dequeue()` 返回 `std::optional<T>`（D-001 §3c）。代码3.14 的循环数组采用**牺牲一个槽位**：容量为 n 的可用队列申请 n+1 个元素，`front == rear` 为空，`(rear+1)%slots == front` 为满，代价是少一个可用位置。链式队列补齐深拷贝、移动和析构，避免默认浅拷贝二次释放。
