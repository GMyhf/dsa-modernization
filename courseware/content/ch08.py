# -*- coding: utf-8 -*-
"""第8章 内排序 —— 课件内容。素材出自 book/ch08-sorting.md，代码摘自 code/ch08/sorting。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第8章　内排序',
    'subtitle': '插入 · 选择 · 交换 · 归并 · 分配 · 以及一条谁也绕不过的下界',
    'footer': '数据结构与算法 · 第8章 内排序',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写；code/ 全部单元双档编译运行通过'],
}

SLIDES = [

    ('bullets', '读每一种排序方法时，同时问四件事', [
        '**① 它靠元素之间比较，还是靠关键码的数字结构做分配？**',
        '**② 相等的键排完之后相对次序变不变？**（稳定性）',
        '**③ 除了原序列还要多少额外空间？**',
        '**④ 最好、平均、最坏各是什么时间？**',
        '「稳定」不是装饰：按成绩排序时，两名同分学生若仍按原来的姓名顺序出现，'
        '**后续按姓名再排才有意义**',
        '本章所有实现都是**手写的** —— 没有用 `std::sort` 顶替任何一个，那样等于把这一章删掉',
    ]),

    ('key', '本章的一句话',
     '**同阶不等于同速。** 渐近记号告诉你 n 变大时谁会输，'
     '不告诉你在你关心的那个 n 上谁更快。本章有实测数据为证。'),

    ('section', '第 1 节', '8.1 排序问题的基本概念', '术语、稳定性、怎么衡量代价'),

    ('bullets', '8.1 术语先约定清楚', [
        '**内排序**：记录都能放进内存（本章）；**外排序**：要访问外存（第 9 章）',
        '讨论排序时用**记录 / 元素**代替前 7 章的「结点」，用**序列**代替「线性表」',
        '每个记录内有一个**排序码**域作为排序的依据',
        '⚠️ **排序码不一定是关键码。** 关键码唯一确定一个记录；',
        '- 排序码不是关键码时，**可能有多个记录排序码相同，排序结果因此可能不唯一**',
        '**正序**：已经符合要求；**逆序**：反过来正好符合要求',
        '**稳定**：相同排序码的记录，排序后相对次序不变',
    ]),

    ('bullets', '怎么衡量代价', [
        '时间代价一般用**记录的总比较次数**和**总移动次数**来衡量',
        '- 一次移动就是一次记录赋值，**一次 `swap` 交换算三次移动**',
        '- 这条换算在 8.7.2 解释「同样是 Θ(n²)，冒泡为什么比插入慢几十倍」时要用',
        '影响相对运行时间的因素：记录数量、排序码与记录的大小、**输入的原始有序程度**',
        '要分别考虑最佳、最差、平均三种情况',
        '- **长度 n 的序列共有 n! 种排列，假定等概率，平均运行时间就是平均时间代价**',
    ]),

    ('table', '本章的分类（沿用原书）', [
        ['类别', '算法', '节'],
        ['**插入排序**', '直接插入排序、Shell 排序', '8.2'],
        ['**选择排序**', '直接选择排序、堆排序', '8.3'],
        ['**交换排序**', '冒泡排序、快速排序', '8.4'],
        ['**归并排序**', '二路归并', '8.5'],
        ['**分配排序**', '桶式排序、基数排序、索引排序', '8.6'],
    ], '另一种常见分类：把插入、直接选择、冒泡归为「简单排序」，快排和归并归为「分治排序」'),

    ('section', '第 2 节', '8.2 插入排序', '直接插入与 Shell'),

    ('bullets', '8.2.1 直接插入排序', [
        '把第 i 个记录插入到前面已排好序的 i 个记录中的**恰当位置**',
        '**最好 Θ(n)** —— 输入已有序时，内层循环一次都不进',
        '**最坏 Θ(n²)** —— 输入逆序时',
        '**稳定**：只与相邻元素比较，遇到相等就停，**绝不跳过相等元素**',
        '代价的来源说得清楚：**时间主要由记录移动到目标位置所需的步长之和决定**，'
        '而步长由序列中的**逆置数**决定',
        '- 长度 n 的序列平均有 `n(n-1)/4` 对逆置',
        '- ⇒ **任何一种只对相邻记录进行比较的排序算法，平均时间代价都是 Θ(n²)**',
    ]),

    ('code', '8.2.2 Shell 排序：跨越相邻', '''// 算法8.2：增量每次减半的 Shell 排序。
inline void shell_sort(std::vector<int>& values) {
    for (std::size_t gap = values.size() / 2; gap != 0; gap /= 2) {
        for (std::size_t index = gap; index < values.size(); ++index) {
            const int value = values[index];
            std::size_t hole = index;
            while (hole >= gap && value < values[hole - gap]) {
                values[hole] = values[hole - gap];    // 一步跨 gap 格
                hole -= gap;
            }
            values[hole] = value;
        }
    }
}''',
     '它利用了插入排序的两条性质：**正序时 Θ(n)**，以及**短序列上很有效**'),

    ('ascii', 'Shell 排序：一步跨四格', r"""
初始       45  34  78  12 34'  32  29  64        （两个 34 用 34 和 34' 区分）

gap=4     34'  32  29  12  45  34  78  64
          子序列 {45,34'} {34,32} {78,29} {12,64} 各自排好
          29 从下标 6 一步搬到下标 2；34' 从下标 4 一步搬到下标 0

gap=2      29  12 34'  32  45  34  78  64

gap=1      12  29  32 34'  34  45  64  78        最后一趟：序列已基本有序
""", '注意结果里 34′ 排在 34 前面，与输入次序相反 —— **Shell 排序不稳定**'),

    ('bullets', '为什么 Shell 不稳定，以及增量怎么选', [
        '**不稳定的原因正是「跨越式移动」**：34 与 34′ 落在不同的子序列里，'
        '34′ 在第一趟就越过了 34',
        '- 而稳定性恰恰依赖「只与相邻元素比较、绝不跳过相等元素」',
        '**增量序列的选择直接决定复杂度**，这是一个至今没有完全解决的问题：',
        '- 减半序列（本书所用）最坏 **Θ(n²)**',
        '- Hibbard 序列 `1, 3, 7, …, 2ᵏ-1` 最坏 **Θ(n^1.5)**',
        '- 实测常用的 Sedgewick 序列更好',
        '本书取减半，是因为它最容易讲清楚「增量」这件事本身',
    ]),

    ('section', '第 3 节', '8.3 / 8.4 选择排序与交换排序', '选择、堆排、冒泡、快排'),

    ('bullets', '8.3 选择排序', [
        '**直接选择排序**：每轮从剩余部分选出最小的，与当前位置交换',
        '- **最好情况也是 Θ(n²)** —— 无论输入什么样，都要把剩余部分完整扫一遍',
        '- 换来的是别人没有的性质：**交换次数 ≤ n−1**',
        '- **不稳定**：交换会把相等元素跨过去',
        '**堆排序**（8.3.2）：用第 5 章的**最大堆**',
        '- 建堆 **O(n)**，然后 n 次取最大 —— 每次 `O(log n)`',
        '- **总代价 O(n log n)，最坏情况也有保证**；原地排序，不要额外空间',
        '- 那个 `log n` **全部来自后面 n 次取最大，建堆是白送的**（5.5.1a）',
    ]),

    ('bullets', '8.4.2 快速排序：分、治、不用合', [
        '1962 年 Tony Hoare 发明。**被评选为 20 世纪十大算法之一**，平均 Θ(n log n)',
        '① 从序列中选一个记录作**轴值**（pivot）；② 把其余记录**分割**成 L 和 R；',
        '③ L 中都 ≤ k，R 中都 ≥ k，**于是 k 正好位于它最终的位置**；④ 对 L、R 递归',
        '**不需要明显的「合」过程** —— 轴值已经到位，L 的记录再也不会跑到 k 的右边',
        '- 这是它与归并排序（8.5）**最大的区别**',
        '分割是快排的全部工作量：它是 **O(n) 的一次线性扫描**，'
        '而**分得均不均匀决定了递归有多深**',
        '- 每次平分是 `O(n log n)`；**每次只切下一个元素就退化成 `O(n²)`**',
    ]),

    ('bullets', '轴值怎么选，影响很大', [
        '**最糟糕的情况莫过于轴值恰好是第一个或者最后一个记录** —— '
        '分出的两个子序列中会有一个为空',
        '- 选第一个或最后一个记录当轴值时，**输入恰好正序或逆序就每次都最坏**',
        '选**中间点** `(start + end) / 2`：正序或逆序时正好平分序列，实验效果非常好',
        '实务中的快排一定要配**三数取中**或**随机枢轴**',
        '**等于轴值的记录怎么办？** 快排不稳定，所以有多种处理方法',
        '- 但**全相等的输入必须也能结束** —— 处理不当会死循环，这是最常见的一处 bug',
    ]),

    ('section', '第 4 节', '8.5 / 8.6 归并排序与分配排序', '稳定的 n log n；以及不比较的排序'),

    ('bullets', '8.5 归并排序', [
        '也是分治：**分**成两半各自排好，**合**成一个有序序列',
        '与快排相反 —— 快排把功夫花在「分」上，**归并把功夫花在「合」上**',
        '**稳定**（合并时相等元素优先取左半边的），**最坏也是 O(n log n)**',
        '代价是**需要 O(n) 的辅助空间** —— 这是它与堆排序的分水岭',
        '- 堆排序原地但不稳定；归并稳定但费空间；快排平均最快但最坏退化',
        '- **三者各占一角，没有一个全占** —— 这就是为什么标准库要把它们拼起来用',
    ]),

    ('bullets', '8.6.1 桶式排序：不再比较', [
        '前面每一种排序都靠**元素之间的比较**决定次序，而比较排序最坏至少 `Ω(n log n)`',
        '**要突破这个下界，只能不再比较** —— 转而利用关键码本身的数字结构，'
        '直接算出每个元素该去哪儿。这类方法叫**分配排序**',
        '桶式排序：已知取值都在 `[0, m)` 之间时，',
        '- ① 准备 m 个计数器，扫一遍数出每个取值出现几次',
        '- ② 把计数**累加**成「小于等于 i 的元素共有几个」',
        '- ③ 这就直接给出了每个取值在结果里的位置区间',
        '**「先数一遍再累加」的用处**：预先给每个桶留出恰好够用的一段，'
        '于是只要一个长度 n 的辅助数组，而不是 m 条变长的链',
    ]),

    ('bullets', '8.6.2 基数排序', [
        '桶式排序的问题是 **m 太大时空间吃不消**（值域有多大就要多少计数器）',
        '基数排序把关键码**按位拆开**，每一位的取值只有 r 种（十进制 r=10）',
        '**LSD（低位优先）**：从最低位开始，每一位做一次分配 + 收集，共 d 趟',
        '⚠️ **第一趟之后的每一趟都必须用稳定排序** —— '
        '否则前面几趟的成果会被后面的趟数打乱',
        '- 这是本章「稳定性不是装饰」最硬的一个例子',
        '代价 `Θ(d(n + r))`。**它没有违反 Ω(n log n) 下界，是站在下界管辖范围之外**',
        '- 代价是对关键码的形态有要求：**值域要够密，或者位数要够少**',
    ]),

    ('section', '第 5 节', '8.7 排序算法的时间代价', '实测数字，以及一条下界'),

    ('table', '8.7.1 三种简单排序：差别不在数量级，在对输入形态的反应', [
        ['算法', '最好', '平均', '最坏', '稳定', '交换/搬移'],
        ['直接插入', '**Θ(n)**（已有序）', 'Θ(n²)', 'Θ(n²)（逆序）', '稳定', '搬移 Θ(n²)'],
        ['冒泡（带无交换退出）', '**Θ(n)**（已有序）', 'Θ(n²)', 'Θ(n²)', '稳定', '交换 Θ(n²)'],
        ['直接选择', '**Θ(n²)**', 'Θ(n²)', 'Θ(n²)', '**不稳定**', '**交换 ≤ n−1**'],
    ], '选择排序那一行是关键：**它的最好情况也是 Θ(n²)** —— 它换来的是「交换次数最少」'),

    ('ascii', '8.7.2 实测（g++ 13.3，-O2，随机数据，毫秒）', r"""
      n     插入     选择     冒泡     堆排     快排     归并
   1000      0.1      0.2      2.8      0.0      0.0      0.0
  10000      7.9     18.9    162.4      0.7      0.4      0.5
  50000    213.5    482.1   5821.9      3.0      2.2      3.1


再看输入形态的影响（2 万个数）：

                    插入      冒泡    优化快排
  已经排好序          0.0       0.0     105.9
  随机               58.5     675.1       0.7
""", '这两张表是本章最值得盯着看的两页 —— 下一页逐条读'),

    ('bullets', '从这两张表读出来的三件事', [
        '**① 同为 Θ(n²)，冒泡比插入慢 27 倍**（5822 对 214）',
        '- 常数差异来自：冒泡每遇到一个逆序对做一次**三步交换**，插入只做**一次搬移**',
        '- **Θ 告诉你 n 变大时谁会输，不告诉你在你关心的那个 n 上谁更快**',
        '**② n 从 1 万涨到 5 万（5 倍）**：Θ(n²) 的三个涨了约 **25 倍**，'
        'Θ(n log n) 的三个只涨了 5～6 倍 —— **这就是量出来的数量级**',
        '**③ 优化快排在已排好序的输入上比插入排序慢 100 倍以上**',
        '- 本书实现取末元素作枢轴，有序输入每次划分都最坏，`Θ(n²)`',
        '- **那两处优化管的是栈深和常数，救不了最坏时间**',
    ]),

    ('bullets', '原书量出的几条敏感性结论', [
        '**选择排序、归并排序、基数排序对数据分布不敏感**',
        '插入、冒泡、Shell 都是**正序比逆序快**；正序或基本有序时，**简单算法反而最好**',
        '**快速排序的正序和逆序速度一样**，都比随机分布快 —— '
        '因为原书取**中间点**作轴值，正序逆序时正好平分',
        '- 本书实现取末元素，所以在这两种输入上反而最慢',
        '- **同一个算法，选轴策略不同，对输入形态的反应可以完全相反**',
        '**堆排序逆序比正序稍快** —— 用的是最大堆，逆序时已经自然成堆',
        '顺序基数排序比链式快；但记录很大时应改用链式以减少移动',
    ]),

    ('bullets', '标准库里的排序是怎么做的', [
        '`std::sort` 是一种基于快速排序的**混合算法**（今天通称 introsort）：',
        '- 先用快速排序，轴值**三点选中**；',
        '- 长度小于阈值（原书记为 n < 20）时**停止递归**，最后对整个序列做一次插入排序；',
        '- 引入**递归计数**：深度超过阈值（原书记为 `2 log n`）时**转入堆排序**',
        '`stable_sort` 用归并排序，`partial_sort` 用堆排序',
        '**这段话正好解释了本书为什么不用 `std::sort` 顶替这一章**：',
        '- 它是把本章三种算法拼起来的产物，**读懂它的前提恰恰是先把这三种都手写一遍**',
    ]),

    ('bullets', '8.7.3 排序问题的下限', [
        '**下限**是解决问题所能达到的最佳可能效率（哪怕算法还没设计出来）；'
        '**上限**是已知最快算法的效率',
        '**一条谁也绕不过去的下限**：任何算法的时间代价都不可能小于它的 I/O 时间',
        '- 至少要花 n 步读入、n 步输出 → **排序的时间只可能在 `Ω(n)` 到 `O(n log n)` 之间**',
        '已知比较排序的上限是 `O(n log n)`。下限能不能降到 `Ω(n)`？',
        '**答案是不能：任何基于比较的排序，最坏情况下至少需要 `Ω(n log n)` 次比较。**',
    ]),

    ('ascii', '判定树：Ω(n log n) 的证明', r"""
把一次排序的执行过程画成一棵二叉树：

    每个内部结点 = 一次比较「a_i < a_j ?」
    两条边       = 两种结果
    每片叶子     = 一种最终的输出排列

于是「算法在某个输入上做了多少次比较」= 从根走到对应叶子的路径长度。

  n 个互不相同的元素有 n! 种排列，每一种都必须能被这棵树区分出来
     （否则算法会对两个不同的输入给出同一个答案）

           ==>  叶子至少有 n! 片
  高度为 h 的二叉树最多有 2^h 片叶子

           ==>  2^h >= n!  ==>  h >= log2(n!) = Theta(n log n)

  而树的高度，正是最坏情况下的比较次数。
""", 'Stirling：log₂(n!) ≥ log₂(n/2)^(n/2) = (n/2)·log₂(n/2) = Θ(n log n)'),

    ('key', '这个下界只约束比较排序',
     '桶式排序和基数排序**不比较元素**，它们直接用关键码的值算位置 —— '
     '**它们没有违反下界，是站在下界管辖范围之外。**'),

    ('bullets', '本章小结', [
        '**排序、检索和索引历来是数据结构讨论的重点问题**，三者互相支撑',
        '**稳定性**决定了能不能做「多关键码排序」—— 基数排序 LSD 完全依赖它',
        '三种简单排序都是 Θ(n²)，**根本原因是「只对相邻记录比较和移动」**',
        '- 想从数量级上提高，就必须摆脱这种一步步操作的思想（Shell 是第一步）',
        '**堆排序原地但不稳定；归并稳定但费空间；快排平均最快但最坏退化** —— 各占一角',
        '**比较排序的最坏下界是 Ω(n log n)**，用判定树证明；分配排序不受它约束',
        '**同阶不等于同速** —— 冒泡与插入同为 Θ(n²)，实测差 27 倍',
    ]),

    ('bullets', '习题选讲', [
        '**手算**　对同一组数据分别走一遍插入、Shell、选择、冒泡、快排、归并，写出每一趟',
        '**稳定性**　逐一判断本章各算法是否稳定，**并对不稳定的给出最小反例**',
        '**下界**　用判定树证明比较排序的 `Ω(n log n)`；说明基数排序为什么不受约束',
        '**快排**　举例说明取末元素作枢轴时哪种输入最坏；三数取中能不能根治',
        '**基数**　为什么 LSD 的每一趟都必须稳定？举一个用不稳定排序会出错的例子',
        '**原书 11**　大量重复记录时把快排改成**三分数组**（小于 / 等于 / 大于轴值）',
        '**原书 12**　用栈代替递归；最差情况栈有多深？**怎样组织递归顺序能把栈深减到最小**',
        '**上机 1**　复现 8.7.2 的两张实测表，并解释你机器上的数字与书上的差异',
        '**这一页和讲义里都是选讲。** 原书全部 **345 道**习题与上机题的参考答案，在 `book/习题与参考答案.md`',
    ]),
]
