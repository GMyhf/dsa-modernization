# -*- coding: utf-8 -*-
"""第11章 索引技术 —— 课件内容。素材出自 book/ch11-index.md，代码摘自 code/ch11/。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第11章　索引技术',
    'subtitle': '线性索引 · 多分树 · 倒排索引 · B 树与 B+ 树 · 位索引',
    'footer': '数据结构与算法 · 第11章 索引技术',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('key', '本章的关键指标不是 CPU 比较次数',
     '**是磁盘页访问次数。** 第 9 章换过一次成本模型，这一章全程按它算账。'),

    ('bullets', '本章要回答四个问题', [
        '**主文件太大，怎么先查一张小表？** 11.1 线性索引：稠密 vs 稀疏',
        '**索引本身也放不进内存怎么办？** 11.1 多级索引 → 11.2 多分树',
        '**索引还要支持插入删除怎么办？** 11.4 B 树与 B+ 树',
        '**能不能反过来，从属性值找记录？** 11.3 倒排索引；11.5 位图索引',
        '⚠️ 第 11 章在原书里**没有任何【算法】或【代码】清单** —— '
        '它讲的是结构与取舍，不是某一段代码',
    ]),

    ('section', '第 1 节', '11.1 线性索引', '稠密与稀疏、多级索引'),

    ('bullets', '11.1 线性索引', [
        '外存上逐条扫描主文件太贵：**应该先查一张较小的表，再按位置读记录**',
        '线性索引就是这张表：项是 `(key, location)`，按 key 排序，**可以在内存里二分**',
        '**稠密索引** —— 为**每条记录**建一项；**主文件可以无序**，查到索引项直接得到位置',
        '**稀疏索引** —— 只为**每个有序数据块**建一项，通常记该块的最小 key',
        '- 查到块之后还要**在块内再找一次**；更省空间，但**要求主文件按 key 分块有序**',
        '**变长记录尤其适合「索引 + 位置」**：',
        '- 记录长度不同，就不能用「基地址 + 下标 × 固定长度」直接算位置',
    ]),

    ('image', '图 11.1　线性索引与不等长记录', FIG + 'fig-11-1.png',
     '索引项保存关键码和记录地址，把**逻辑顺序与物理布局分开**；主文件移动后要更新地址'),

    ('bullets', '多级索引', [
        '第一层索引太大、内存放不下时，**再为它建一层** —— 成为**多级索引**',
        '一次查询的路径：**内存里的顶层 → 读一个索引页 → 读数据页**',
        '本章的可运行实验（`code/ch11/linear_index`）把这件事量出来了：',
        '- 1000 条记录、每数据页 4 条、**每索引页 4 项** → 层数多，一次查询读的页也多',
        '- 同样的数据、**每索引页 64 项** → **层数立刻降下来，访外次数随之下降**',
        '**索引页装得多，层数就少** —— 这就是 11.2 要做多分树的全部理由',
    ]),

    ('section', '第 2 节', '11.2 静态索引：多分树', '扇出、树高、批量装入'),

    ('bullets', '11.2 为什么要多分', [
        '磁盘按页读写，**一个索引结点通常设计成恰好装满一页**',
        '二叉树每个结点只有两个孩子 —— 同样多的 key 会把树撑得很高，查询就要读很多页',
        '**多分树降低的是高度。** 若每个结点最多 m 个孩子、树高 h，可导航约 `m^h` 个叶页',
        '- **同样覆盖一百万个叶页：二叉树约需 20 层，100 路树只需 3 层**',
        '- 页内要比较更多分界 key，**但整页已经在内存里** —— 代价远小于多读一次外存页',
        '**静态**多分树在批量装入时按页填满，之后结构不再变',
        '- 插入删除会破坏「一页正好满」的约定：多的进**溢出区**，少的页变稀，最终要**重组**',
        '- **所以它适合「一次建好、很少改」的文件，不适合频繁更新的目录**',
    ]),

    ('bullets', '扇出不是随意选的', [
        '页大小 4096 字节，若每个「关键码 + 孩子页号」占 16 字节：',
        '- 扣除页头后大约能放 250 项，**扇出约 251**',
        '**关键码变长或页头元数据增加，扇出就会下降**',
        '**数据库索引喜欢短关键码，不只为省总空间，也为让一页容纳更多分支、压低树高**',
        '**批量装入**本身就是「按页填满、自底向上建层」：',
        '- 必须**先按 key 排好记录**，然后连续填叶页，再用每个孩子的最小 key 建上一层',
        '- 这样**叶页天然相邻，范围扫描接近顺序 I/O**',
        '- ⚠️ 一条条随机插入**得不到**同样的布局：分裂会留下空隙，树形和页利用率都不同',
    ]),

    ('section', '第 3 节', '11.3 倒排索引', '从属性值走到记录集合'),

    ('bullets', '11.3 倒排索引', [
        '前面的检索是**由记录确定属性值**；倒排索引反过来 —— **由属性值确定记录的位置**',
        '原书的例子是教师登记表：主关键码是职工号，另有姓名、系、职称、专长',
        '- 「列出职称为讲师的所有职工号」用普通检索要**顺序扫描所有记录**',
        '- 「列出计算机系擅长英语的教员」还要对每个记录先看「系」再看「专长」—— **效率很低**',
        '**倒排表**：对每个感兴趣的属性值建一个线性表，存放具有该属性值的所有关键码',
        '- `计算机系 → [0310, 0330, 0341]`；`英语专长 → [0310, 0421]`',
        '- **「且」是两个有序列表的交集；「或」是并集；「非」是差集**',
    ]),

    ('bullets', '对正文文件的倒排', [
        '**词索引**：把正文看成词的集合，抽取关键词建结构',
        '- 适合易解析成词的文本（英文）；**不适合生物学数据和一些东方语言**',
        '- 而且**查询词被限制在关键词范围内**',
        '**全文索引**：把正文看成一个长字符串，记录**子字符串**的开始位置',
        '- 查询可以针对任何子串，代价是**通常需要更大的空间**',
        '**倒排文件是使用得最广泛的词索引**：排序关键词的列表，每个词指向一个 posting list',
        '- `{(文档号, 出现次数, 出现位置), …}`',
        '- **可以去掉频率、甚至去掉位置只留文档编号**来省空间',
        '- 但**短语查询需要位置信息** —— 要用「位置是否相邻」来过滤',
    ]),

    ('section', '第 4 节', '11.4 动态索引：B 树与 B+ 树', '本章的核心'),

    ('bullets', '为什么外存索引必须用多分树', [
        '**二叉搜索树不能保证平衡，有可能退化为线性表**',
        '- **外存索引的平衡性能非常重要** —— 退化会带来巨大的访外开销',
        '第 12 章的 AVL 树能保持 `Θ(log n)` 平衡，'
        '**但其频繁的插入删除调整对外存树索引也需要很大的 I/O 量**',
        '于是问题变成两条：',
        '- **怎样保持多分树高度上的平衡，不让它退化？**',
        '- **怎样使结点中关键码尽可能多，而插入删除又不需要太多调整？**',
        '**1970 年 Bayer 与 McCreight 提出 B 树**（balanced tree）',
        '- ⚠️ 那个「-」是英文连字符，**不要说成「B 减树」**',
    ]),

    ('bullets', 'm 阶 B 树的定义：五条，缺一不可', [
        '**① 每个结点至多有 m 个子结点**',
        '**② 除根和叶外，每个结点至少有 `⌈m/2⌉` 个子结点**',
        '**③ 根结点至少有两个子结点**（例外：根可以为空，或者独根）',
        '**④ 所有的叶结点在同一层**，可以有 `⌈m/2⌉−1` 到 `m−1` 个关键码',
        '**⑤ 有 k 个子结点的分支结点恰好包含 k−1 个关键码**',
        '最底层叶结点的指针指向空（对应失败检索），'
        '**空指针数目正好等于关键码数目加 1**',
        '**阶为 3 的 B 树就是通常说的「2-3 树」**',
    ]),

    ('bullets', 'B 树的四条性质', [
        '**① 总是树高平衡的**，所有叶结点都在同一层',
        '**② 更新和检索只影响一些磁盘块**，因此性能很好',
        '**③ 把关键码接近的记录放在同一个磁盘块中** —— **利用了访问局部性原理**',
        '**④ 保证至少有一定比例的结点是满的**',
        '- 既保证空间利用率，又使插入删除**不必像静态多分树那样频繁修改结点**',
        '- 从而总体上减少磁盘读取数',
        '一个含 r 个关键码的内部结点有 **r+1 个孩子**：',
        '- 最左孩子的键都小于第一个关键码，相邻两个关键码之间各管一个区间',
        '- **若孩子数与关键码数不满足「多一个」，结点结构已经损坏**',
    ]),

    ('bullets', '插入：可能朝着根的方向生长', [
        '先检索关键码是否已在树中；在则拒绝插入。不在，则最后找到的叶结点就是插入位置',
        '插入后若关键码个数 `< m`，完成；**否则等于 m，产生上溢出**：',
        '- 取这 m 个关键码的**中位数**为分界码',
        '- 叶结点分裂为 `⌈(m−1)/2⌉` 与 `⌊(m−1)/2⌋` 两个结点',
        '- **分界码插入到父结点**；父结点若也溢出，**分裂过程继续往根传递，可能一直到根**',
        '**访外次数可以算出来**：向下找插入结点读 h 次；',
        '- 每分裂一个非根结点写 2 个，分裂根写 3 个 → **最坏 `3h + 1` 次**',
    ]),

    ('bullets', '根结点为什么可以只有 2 个子结点？', [
        '根允许 `2 ~ m` 个子结点，内部结点是 `⌈m/2⌉ ~ m` —— 这不是随意的例外',
        '**因为插入算法分裂到根时会分为 2 个子结点并产生一个新根**',
        '- 若要求根与内部结点同样的出度，就需要对 B 树做比较大的调整',
        '- **这就违背了 B 树操作的局部调整原则**',
        '而且**实际运行时根结点往往保存在内存中**，不需要遵守下限约束',
        '- 大部分内部结点在外存，需要保证**至少半满**，以折衷存储效率与操作效率',
        '**根也不可能只有一个子结点** —— 那样太浪费存储，还会增加检索时间',
    ]),

    ('bullets', 'B 树的树高：一个能算的上界', [
        '设 B 树含 N 个关键码，则失败检索的外部结点有 `N+1` 个',
        '根至少两个子结点 → 第 1 层至少 2 个结点；此后每层至少乘 `⌈m/2⌉`：',
        '- 第 I 层至少 `2·⌈m/2⌉^(I−1)` 个 → `N+1 ≥ 2·⌈m/2⌉^(I−1)`',
        '**`I ≤ 1 + log_⌈m/2⌉((N+1)/2)`**',
        '**代进数字**：`N = 1999998`、`m = 199` 时，**I 至多等于 4**',
        '- 一次检索至多 I 次存取 —— 这个公式保证了 B 树的检索效率相当高',
        '插入时**每插一个关键码平均分裂的结点数 `s ≤ 1/(⌈m/2⌉−1)`**',
        '- **m 越大这个数越小 —— 这正是「结点开大一点」在插入代价上的回报**',
    ]),

    ('ascii', '全章最值得记住的一笔账（B+ 树的索引规模）', r"""
假定：磁盘块 4096 字节，整数关键码 4 字节，指针 8 字节，不计块头

    求满足  4m + 8(m+1) <= 4096  的最大整数 m       ->   m = 340   （阶）

假若平均充满度介于最大与最小之间，即平均 255 个指针，则 3 层 B+ 树：

    根结点             255 个子结点
    第二层             255^2  =     65 025 个叶结点
    叶层               255^3  ~= 16 600 000 个指向记录的指针

    ==> 记录数 <= 约 1660 万的文件，都能被 3 层 B+ 树容纳，
        3 次访外就能拿到关键码在主文件的地址。

实际还可以更少：B+ 树的根通常常驻内存，第二层也完全放得进缓冲区
    ==> 只需要读一次叶结点磁盘块。
""", '这解释了今天数据库索引的常见形态：**三层 B+ 树 + 常驻内存的上两层**'),

    ('bullets', 'B 树与 B+ 树谁更矮？', [
        '**B 树的关键码在内部结点上也带着数据地址** —— 这正是它与 B+ 树的分水岭',
        'B+ 树的内部分支结点**不需要带这些隐含指针**，同样一页能装更多分支',
        '在页块容量一致、指针大小相同、索引同一主文件的前提下：',
        '- **B 树比 B+ 树高，即 B+ 树索引效率更高**',
        'B+ 树最大充盈度 100%、最小 50%，取平均 75%：',
        '- 平均每结点 `0.75m` 个子结点 → **高度 `⌈log_(0.75m) N⌉`**',
        '还有一条 B 树没有的好处：**数据集中在叶层，叶链可顺序读** —— 范围扫描很快',
    ]),

    ('table', '11.4.4 动态索引与静态索引：更新成本由谁承担', [
        ['工作负载', '更合适的选择', '原因'],
        ['数据几乎只读、可批量重建', '**静态索引**', '结构紧凑，读路径短'],
        ['持续插入和删除', '**B / B+ 树**', '局部调整即可保持查询性能'],
        ['范围扫描很多', '**B+ 树**', '数据集中在叶层，叶链可顺序读'],
        ['记录物理地址必须长期稳定', '**静态索引**', '不因结点分裂搬动既有记录'],
    ], '关键不是「哪一种总是更快」，而是：**静态索引把成本推迟到昂贵的批量重组，动态索引把成本摊到每次更新**'),

    ('section', '第 5 节', '11.5 位索引技术', '位图、签名'),

    ('bullets', '11.5 位图索引', [
        '**位图索引适合取值很少的属性** —— 性别、省份、是否及格',
        '每个属性值对应一个**位串**：第 i 位表示第 i 条记录有没有这个值',
        '**AND、OR、NOT 查询变成机器字上的按位运算**，一次可以处理几十上百条记录',
        '- 这与 10.2 节的位向量是同一个思想，只是那里的「元素」换成了「记录」',
        '⚠️ **属性取值一多，位图会变得很宽** —— 需要压缩（游程、字对齐混合等）',
        '所以它和 B+ 树是互补的：',
        '- **低基数属性（几十种取值）用位图；高基数属性（近乎唯一）用 B+ 树**',
    ]),

    ('bullets', '本章小结', [
        '**成本模型是页访问次数**，不是比较次数',
        '**线性索引**把逻辑顺序与物理布局分开；稠密 vs 稀疏是「空间 vs 块内再查一次」的取舍',
        '**索引页装得多 → 层数少 → 访外少**，这是多分树的全部理由；'
        '**扇出由页大小与项大小定死**',
        '**倒排索引**从属性值走到记录集合，「且 / 或 / 非」变成集合的交 / 并 / 差',
        '**B 树**：五条定义、四条性质；插入靠**分裂**，可能一直传到根，最坏访外 `3h+1`',
        '- 树高 `I ≤ 1 + log_⌈m/2⌉((N+1)/2)`；N≈200 万、m=199 时 **I ≤ 4**',
        '**B+ 树**内部结点不带数据地址，因而更矮；叶层成链，**范围扫描接近顺序 I/O**',
        '**位图索引**适合低基数属性，查询变成按位运算',
    ]),

    ('bullets', '习题（完整题面与参考答案见同名讲义）', [
        '**索引**　同一份数据分别建稠密索引与稀疏索引，比较空间与一次查询的页读次数',
        '**扇出**　页 8192 字节、关键码 8 字节、指针 8 字节时，B+ 树的阶是多少？树高变几层',
        '**B 树**　对给定序列依次插入，画出每次分裂后的形状；标出访外次数',
        '**证明**　推导 `I ≤ 1 + log_⌈m/2⌉((N+1)/2)`，并说明为什么根允许只有 2 个子结点',
        '**对比**　B 树与 B+ 树索引同一主文件，谁更矮？写出高度公式并解释差别的来源',
        '**倒排**　给定三条 posting list，求「A 且 B 非 C」；说明为什么短语查询需要位置信息',
        '**上机**　用 `code/ch11/linear_index` 量出「每索引页项数 → 层数 → 页读次数」的关系曲线',
    ]),
]
