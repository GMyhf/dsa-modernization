# -*- coding: utf-8 -*-
"""第7章 图 —— 课件内容。素材出自 book/ch07-graph.md，代码摘自 code/ch07/graph 与 adjacency_list。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第7章　图',
    'subtitle': '存储结构 · DFS/BFS 与拓扑排序 · 最短路径 · 最小生成树',
    'footer': '数据结构与算法 · 第7章 图',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('bullets', '本章要回答的问题', [
        '**图是什么？** 线性结构唯一前驱唯一后继；树唯一前驱多个后继；'
        '**图对两者都不加限制**',
        '- 线性和树都可以看成受限的图',
        '**同一张图，两种存法差在哪？** 7.3 邻接矩阵 vs 邻接表',
        '**从一个点能走到哪些点？** 7.4 DFS 与 BFS —— 第 5 章周游的推广',
        '**先修课怎么排？** 7.4.3 拓扑排序，顺便检测有没有环',
        '**最短的路 / 最省的网怎么找？** 7.5 Dijkstra 与 Floyd；7.6 Prim 与 Kruskal',
    ]),

    ('key', '本章的一句话',
     '**换存储方式不该换答案，只该换代价。** 本章两套实现（邻接矩阵与邻接表）逐项对拍：'
     '同一张图上 DFS/BFS 序列、拓扑序、Dijkstra 距离必须完全相同。'),

    ('section', '第 1 节', '7.1 图的定义和基本术语', '一次把词汇过完'),

    ('bullets', '7.1 基本定义', [
        '图记作 `G = <V, E>`。V 是顶点的**有穷非空**集合，E 是边的集合',
        '- **无向图**：边写成无序对 `(v₁,v₂)`，`(v₁,v₂)` 与 `(v₂,v₁)` 是同一条边',
        '- **有向图**：写成有序对 `<v₁,v₂>`，也叫**弧**；v₁ 是**弧尾**，v₂ 是**弧头**',
        '- `<v₁,v₂>` 与 `<v₂,v₁>` 是**两条不同的弧**',
        '边上附权表示距离、时间或代价 → **带权图**（网络）',
        '记 n 为顶点数、e 为边数：**无向图 e ≤ n(n-1)/2，有向图 e ≤ n(n-1)**',
        '- 边少叫**稀疏图**，边多叫**稠密图**；取到上限的叫**完全图**',
        '本书不考虑**多重边**与**自环**',
    ]),

    ('bullets', '连通性与生成树', [
        '**度** = 与顶点相关联的边数；有向图分**入度**与**出度**。度为 0 的是**孤立点**',
        '**路径**：从 u 沿边走到 v 的顶点序列；边数是**路径长度**。有向图必须顺着弧走',
        '- 起点终点相同且至少含一条边的路径叫**回路**；除端点外无重复顶点的叫**简单路径**',
        '**连通**（无向图）：任意两点之间都有路径。极大的连通子图叫**连通分量**',
        '**强连通**（有向图）：任意 u、v 之间**双向**都有有向路径',
        '- 只把有向边看成无向边后连通的，叫**弱连通**',
        '**生成树**：包含全部顶点、有 **n−1 条边**、因而没有环的连通子图',
        '- 再加一条边必成环；边数少于 n−1 则必不连通',
    ]),

    ('table', '选算法之前，先分清题目', [
        ['题目', '前提', '结果'],
        ['从一个点能走到哪些点', '任意图', 'DFS 或 BFS 的访问序列'],
        ['课程或任务的可行顺序', '**有向无环图**', '拓扑序；有环则无解'],
        ['一个源点到各点的最短路', '**边权非负**', 'Dijkstra 的距离数组'],
        ['任意两点最短路', '顶点数较小', 'Floyd 的距离矩阵'],
        ['连通所有点且总权最小', '**无向连通图**', 'Prim 或 Kruskal 的边集'],
    ], '每一行的「前提」那一列都不是装饰 —— 前提不满足，算法给出的就是错误答案'),

    ('section', '第 2 节', '7.3 图的存储结构', '稠密用矩阵，稀疏用邻接表'),

    ('bullets', '7.3.1 相邻矩阵（邻接矩阵）', [
        '一个 `V × V` 的二维数组：第 `from` 行第 `to` 列保存弧 `from → to` 的权',
        '- 构造时**对角线为 0**（到自己的距离是 0），其余为 `infinity`',
        '- **无向边要同时写入 (u,v) 和 (v,u) 两格**',
        '⚠️ **不能用 0 表示「无边」，因为权为 0 的边是合法的** —— 本书用 `infinity`',
        '**无向图的矩阵一定对称**；顶点 vᵢ 的度就是第 i 行（或第 i 列）之和',
        '- 有向图不一定对称：第 i **行**之和是**出度**，第 i **列**之和是**入度**',
        '空间 `O(V²)`，**无论实际有多少条边**；查询两点是否相邻 `O(1)`',
    ]),

    ('bullets', '7.3.2 邻接表', [
        '为每个顶点存一张出边表，**只存实际存在的边**',
        '**无向图里一条边出现两次**（在 u 的表里，也在 v 的表里）→ 边表结点共 `2e` 个',
        '**有向图的一条弧只出现一次**，vᵢ 的边表长度就是它的**出度** —— 所以也叫出边表',
        '- 想知道入度就得扫遍整张表，于是有了**逆邻接表**（入边表）',
        '- **7.4.3 节拓扑排序要的正是入度** —— 这就是逆邻接表的用处',
        '带权图在边表结点里多存一个权',
    ]),

    ('table', '两种存法的取舍', [
        ['', '邻接矩阵', '邻接表'],
        ['空间', '`O(V²)`，与边数无关', '`O(V + E)`'],
        ['问「两点之间有没有边」', '**`O(1)`**', '要扫一遍那个顶点的边表'],
        ['扫一个顶点的全部邻居', '扫完整行 `O(V)`', '**只走实际存在的边**'],
        ['遍历全图', '`O(V²)`', '**`O(V + E)`**'],
        ['适合', '**稠密图**；Floyd、Prim', '**稀疏图**；DFS、BFS、Dijkstra、拓扑'],
    ], '本书两套实现逐项对拍，外加 30 轮随机图 —— **换存储方式不该换答案，只该换代价**'),

    ('section', '第 3 节', '7.4 图的周游', 'DFS、BFS 与拓扑排序'),

    ('bullets', '7.4.1 深度优先周游（DFS）', [
        'DFS 是**树的先根次序周游的推广**：进入一个顶点就立刻**标记已访问**，'
        '再沿出边递归下去',
        '某顶点的邻接点都访问过了，就**回退**到上一个顶点，换一条还没走过的边继续',
        '两点值得记住：',
        '- **图不连通时一次 DFS 走不完** —— 要在外层对每个未访问顶点再起一次',
        '- **存储结构一旦定下、源点一旦定下，深度优先序列就是唯一的**',
        '- 序列不同**不是算法不同，是边表次序不同**',
        '⚠️ 与树不同，图可能有**环**，所以「访问过没有」的标记是必需的',
    ]),

    ('key', '代价取决于存储结构，而不是取决于算法',
     '用相邻矩阵时共需检查 n² 个矩阵元素，**O(n²)**；'
     '用邻接表时把所有边结点检查一遍，**O(n + e)**。'),

    ('code', '7.4.2 广度优先周游（BFS）：靠队列', '''[[nodiscard]] std::vector<std::size_t> bfs(std::size_t source) const {
    check_vertex(source);
    std::vector<bool> seen(vertices());
    std::queue<std::size_t> queue;
    std::vector<std::size_t> result;
    queue.push(source);
    seen[source] = true;                    // 入队时就标记，不是出队时
    while (!queue.empty()) {
        const std::size_t from = queue.front();
        queue.pop();
        result.push_back(from);
        for (std::size_t to = 0; to < vertices(); ++to) {
            if (adjacency_[from][to] < infinity && !seen[to]) {
                seen[to] = true;
                queue.push(to);             // 每个顶点只入队一次
            }
        }
    }
    return result;
}''',
     'BFS 与树的层次周游是同一套骨架。**入队时立刻标记** —— 出队时才标记会让同一顶点重复入队'),

    ('bullets', '7.4.3 拓扑排序', [
        '**有向无环图**（DAG）常用来描述一个工程：某些子工程完成之后另一些才能开始',
        '**拓扑序列**：若存在 vᵢ 到 vⱼ 的路径，则序列中 vᵢ 必在 vⱼ 之前',
        '方法是两步的反复：',
        '- ① 选出一个**入度为 0** 的顶点并输出；② 删除它和所有以它为起点的弧',
        '**⚠️ 拓扑序不唯一** —— 图 7.17 的课程图至少有两个合法答案',
        '**当图中还有顶点没有输出时，说明含有环** —— '
        '所以**拓扑排序可以顺便检查有向图是否存在环**',
        '实现：给每个顶点存一个入度域；**把入度为 0 的顶点放进一个队列**，'
        '避免每次扫描整个顶点表',
    ]),

    ('section', '第 4 节', '7.5 最短路径', 'Dijkstra 与 Floyd'),

    ('image', '图 7.18　最短路径的示例', FIG + 'fig-7-18.png',
     '从 A 到 B 的最短路径是 A→C→B —— **并不是边数最少的那条**。「短」指的是边权总和'),

    ('bullets', '7.5.1 Dijkstra：按路径长度递增的次序产生结果', [
        '**单源最短路径**问题，前提是**每条边的权都非负**',
        '把顶点集合划分成 **S**（最短距离已确定）和 **V−S**。初始 `S = {s}`',
        '**特殊路径**：从 s 到 v、**中间只经过 S 中顶点**的路径。数组 D 记录当前最短特殊路径长',
        '每一轮：从 V−S 中取出 **D 值最小**的顶点 u 加入 S，然后**松弛**：',
        '- 若 `D[u] + W[u,v] < D[v]`，就令 `D[v] = D[u] + W[u,v]`',
        '重复到 S 包含全部顶点。这是一个**贪心算法**',
        '**要输出路径本身还需要一个 `pre` 域** —— 记录最短路径上 v 前面的那个顶点',
    ]),

    ('bullets', '7.5.2 Floyd：一个典型的动态规划', [
        '**所有顶点对之间的最短路径**。跑 n 次 Dijkstra 是 `O(n³)`，Floyd 也是 `O(n³)`，'
        '但**形式上简单得多**',
        '核心是这句话：**经过第 k 次迭代，`adj⁽ᵏ⁾[i,j]` 是 i 到 j、'
        '中间顶点序号不大于 k 的最短路径长度**',
        '推导只有两种情况：',
        '- **中间不经过 v_k**：`adj⁽ᵏ⁾[i,j] = adj⁽ᵏ⁻¹⁾[i,j]`',
        '- **中间经过 v_k**：分成 `i→k` 和 `k→j` 两段，各自中间序号不大于 k−1',
        '合起来：`adj⁽ᵏ⁾[i,j] = min{ adj⁽ᵏ⁻¹⁾[i,j], adj⁽ᵏ⁻¹⁾[i,k] + adj⁽ᵏ⁻¹⁾[k,j] }`',
        '**第 1 章那个股市传言问题用的就是它** —— 现在你知道为什么最外层必须是中转点 k 了',
    ]),

    ('section', '第 5 节', '7.6 最小生成树', 'MST 性质、Prim 与 Kruskal'),

    ('bullets', '7.6 目标与依据', [
        '**目标不是任意两点最短，而是连通全部顶点且选中边的总权最小**',
        '连通 n 个城市只需 **n−1 条**线路，问题是选哪 n−1 条',
        'Prim 与 Kruskal 都是贪心算法，都靠同一条**MST 性质**站住脚：',
        '- 设 U 是 V 的非空真子集，若 `(u,v)` 是所有**跨界边**（一端在 U、一端在 V−U）'
        '里权最小的一条，则**一定存在一棵包含 `(u,v)` 的最小生成树**',
        '**反证**：设最小生成树 T 不含 `(u,v)`。把它加进 T 必成回路，'
        '回路上一定另有一条跨界边 `(u′,v′)`',
        '- 删掉 `(u′,v′)` 得到 T′；因为 `W(u,v) ≤ W(u′,v′)`，T′ 代价不比 T 大 —— 矛盾',
        '这条性质就是两个算法「每一步都可以放心地取当前最小的跨界边」的依据',
    ]),

    ('two', '两个算法，互补的适用面',
     'Prim', [
         '从一个顶点出发**长大一棵树**',
         'U 从 {u₀} 开始',
         '每轮取权最小的**跨界边**',
         '把另一端并入 U，重复 n−1 次',
         '`O(n²)`，只与**顶点数**有关',
         '→ 适合**稠密图**（配邻接矩阵）',
     ],
     'Kruskal', [
         '从 n 个孤立顶点**合并森林**',
         '把边**按权排序**',
         '依次取最小边；两端已连通就丢弃',
         '**用并查集判断是否会成环**',
         '`O(e log e)`，只与**边数**有关',
         '→ 适合**稀疏图**',
     ]),

    ('key', '连通图上两者的结果',
     '**总权相同、都是 n−1 条边。** 但具体选中哪些边可能不同 —— '
     '权有相同值时，最小生成树本来就可能不唯一。'),

    ('bullets', '第 6 章的并查集在这里兑现', [
        'Kruskal 的关键一步是：**这条边的两端已经连通了吗？**',
        '- 连通 → 加进去会成环，丢弃；不连通 → 加进去，并**合并**两个连通分量',
        '这正是并查集的两个基本操作 `find` 与 `union`',
        '- 没有并查集，判断「会不会成环」就要每次做一次搜索，`O(n)` 一条边',
        '- 有了它（重量权衡 + 路径压缩），均摊接近常数',
        '**排序 `O(e log e)` 因此成了瓶颈** —— 这就是 Kruskal 复杂度的来历',
        '一个数据结构决定了一个算法能不能用，本章第二次遇到（第一次是 Huffman 用堆）',
    ]),

    ('bullets', '本章小结', [
        '图对前驱后继都不加限制；**线性结构和树都可以看成受限的图**',
        '**邻接矩阵** `O(V²)` 空间、`O(1)` 判邻接，适合稠密图与 Floyd / Prim',
        '**邻接表** `O(V+E)` 空间、遍历 `O(V+E)`，适合稀疏图与 DFS / BFS / Dijkstra / 拓扑',
        '**DFS 靠栈（递归），BFS 靠队列**；两者时间复杂度相同，只是访问顺序不同',
        '**拓扑排序**用入度为 0 的队列实现，**顺便检测环**；拓扑序不唯一',
        '**Dijkstra** 是贪心，要求边权非负；**Floyd** 是动态规划，形式简单，两者都 `O(n³)` 量级',
        '**Prim 与 Kruskal** 都靠 MST 性质；前者与顶点数有关，后者与边数有关，**互补**',
    ]),

    ('bullets', '习题（完整题面与参考答案见同名讲义）', [
        '**存储**　同一张图写出邻接矩阵、邻接表与逆邻接表；分别读出各顶点的入度与出度',
        '**周游**　给定邻接表，写出 DFS 与 BFS 序列；说明**为什么序列由边表次序决定**',
        '**拓扑**　对课程先修图给出**两个不同**的拓扑序；再加一条弧使其成环，说明算法怎么发现',
        '**Dijkstra**　逐轮写出 S 集合与 D 数组；举例说明**边权为负时算法为什么失效**',
        '**Floyd**　写出每一轮迭代后的矩阵；解释为什么最外层循环必须是中转点 k',
        '**MST**　对同一张图分别跑 Prim 与 Kruskal，比较选中的边集与总权',
        '**上机**　用并查集实现 Kruskal，并与 Prim 的结果对拍',
    ]),
]
