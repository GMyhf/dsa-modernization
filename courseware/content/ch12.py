# -*- coding: utf-8 -*-
"""第12章 高级数据结构 —— 课件内容。素材出自 book/ch12-advanced.md，代码摘自 code/ch12/。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第12章　高级数据结构',
    'subtitle': '多维数组与稀疏矩阵 · 广义表与存储管理 · Trie · 改进的 BST',
    'footer': '数据结构与算法 · 第12章 高级数据结构',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('bullets', '本章是四条独立的线', [
        '**12.1 多维数组** —— 地址公式、特殊矩阵的压缩、稀疏矩阵的十字链表',
        '**12.2 广义表与存储管理** —— 元素可以是表；可利用空间表、动态分配、垃圾回收',
        '**12.3 Trie 与 Patricia** —— 换一种「分解」：按关键码的位分层，而不是按对象比较',
        '**12.4 改进的二叉搜索树** —— 最佳 BST（动态规划）、AVL 树、伸展树',
        '四条线共同的主题：**当基本结构不够用时，加什么、代价是什么**',
    ]),

    ('section', '第 1 节', '12.1 多维数组', '地址公式与稀疏矩阵'),

    ('bullets', '12.1.1 多维数组的存储', [
        '多维数组是一维数组的扩充：**数组的数组**。元素类型相同、个数相对固定',
        '- 一旦生成通常只改值、不改相对位置和个数，**因此自然采用顺序存储**',
        '把数组按某种周游次序排成线性序列就能顺序存放：',
        '- **C++ 和 Pascal 按行优先**：先排最右的下标，从右往左排',
        '- **FORTRAN 按列优先**：先排最左下标，公式左右对调',
        '每个元素的定位时间相同，**所以数组是随机存储结构**',
        '⚠️ **定位前仍要逐维检查 `0 ≤ jᵢ < dᵢ`** —— 公式本身不会替程序发现越界',
    ]),

    ('table', '`int A[2][3][4]` 里 `A[1][2][3]` 的偏移（行优先）', [
        ['维', '下标', '后续维大小的乘积', '贡献'],
        ['0', '1', '3 × 4 = 12', '12'],
        ['1', '2', '4', '8'],
        ['2', '3', '1', '3'],
        ['**合计**', '', '', '**23 个元素**'],
    ], 'int 占 4 字节则字节偏移 92。霍纳法：`((1*3+2)*4+3)*4 = 92` —— 一边读下标一边累乘，不必预存跨度'),

    ('bullets', '怎么验证「行优先 vs 列优先」写对了', [
        '**列优先不是把答案随意反过来**，而是把最左下标当作变化最快的一维',
        '同一个 `2×3×4` 形状按列优先存，`[1,2,3]` 的偏移是 `1 + 2×2 + 3×(2×3) = 23`',
        '- **这个角落碰巧仍是 23** —— 拿它测试，两种布局都能过',
        '换成 `[1,0,0]` 就能分辨：**行优先是 12，列优先是 1**',
        '**测试布局时应该选这种能区分规则的下标。**',
        '- 这条方法论适用于所有「两种实现在某些输入上碰巧一致」的场合',
    ]),

    ('bullets', '12.1.2 特殊矩阵', [
        '**三角矩阵**：n 阶上三角（或下三角）里，对角线一侧全是 0 或常数 c',
        '- 只需存另一侧加上那个常数，共 `(n²+n)/2` 个单元',
        '- 下三角的 `a[i][j]`（i ≥ j）：前面有 i 行共 `(i²+i)/2` 个非零元，'
        '再加本行的 j → 下标 `(i²+i)/2 + j`',
        '**对称矩阵**：`a[i][j] = a[j][i]`，只存下三角（含对角线）',
        '- `i ≥ j` 时下标 `(i²+i)/2 + j`，否则 `(j²+j)/2 + i`',
        '4 阶下三角按行压缩后的区间是 `[0]`、`[1..2]`、`[3..5]`、`[6..9]`',
        '- `a[3][1]` 落在 `6 + 1 = 7`；上三角的 `a[1][3]` 先换成 `a[3][1]`，仍读下标 7',
        '**先判断三角区域、再套公式，比背两套公式容易核对。**',
    ]),

    ('bullets', '12.1.3 稀疏矩阵', [
        '`m×n` 矩阵里有 t 个非零元，**稀疏因子 `δ = t/(mn)`**；通常 `δ < 0.05` 就按稀疏处理',
        '这时不应再分配整块二维数组，改存**三元组 `(行, 列, 值)`** 的线性表，或用**十字链表**',
        '**十字链表**：每个非零元**同时挂在一条行链和一条列链上**，便于按行、按列遍历',
        '现代数值计算更常用 **COO（三元组）、CSR（压缩稀疏行）、CSC** ——',
        '- 它们连续存储、**缓存局部性更好**，但动态插入通常需要批量重建',
        '- 十字链表适合**需要频繁按行、按列更新**的场合',
    ]),

    ('table', '三种存法的代价', [
        ['', '整块二维数组', '三元组线性表', '十字链表'],
        ['存储量', '`mn`', '`O(t)`', '`O(t)`，每元多一个指针'],
        ['取 `a[i][j]`', '**`O(1)`**', '`O(log t)`', '`O(该行非零元数)`'],
        ['按列扫一遍', '`O(m)`', '`O(t)`，要滤掉别的列', '**`O(该列非零元数)`**'],
        ['插入一个非零元', '`O(1)`', '`O(t)`，要挪后面的项', '**`O(行内+列内定位)`**'],
    ], '第二行是十字链表**吃亏**的地方，第三、第四行才是它存在的理由'),

    ('bullets', '十字链表：一次改动要动两条链', [
        '结点 `(2,1,5)` 在第 2 行链里可能是唯一结点，在第 1 列链里却接在 `(0,1,8)` 后面',
        '**删掉它必须做两次摘链**：第 2 行头改为空，第 1 列中让 8 的下继越过 5',
        '- **只改一条链，按行和按列看到的就不再是同一个矩阵**',
        '插入 `(0,1,9)` 也不能再造一个同坐标结点，**应找到旧结点并覆盖**',
        '所以测试十字链表至少要查四件事：',
        '- 新坐标**同时出现在两条链**；覆盖**不增加**非零元数；',
        '- **赋值为 0 等价于删除**；删除后**两条链都找不到它**',
        '⚠️ 两条链都是裸指针，析构沿行链**迭代**释放 —— '
        '`unique_ptr` 串链时非零元一多，递归析构就会压穿栈',
    ]),

    ('section', '第 2 节', '12.2 广义表和存储管理', '元素可以是表；以及内存自己怎么管'),

    ('bullets', '12.2.1 广义表', [
        '线性表的元素是不可再分的**原子**。广义表放宽这条：**元素可以是原子，也可以是另一个表**',
        '按是否共享、是否有环分三种：',
        '- **纯表** → 树（每个子表只出现一次）；**再入表** → 有向无环图（子表可共享）；'
        '**循环表** → 有环图',
        '**表头**是第一个元素，**表尾**是去掉表头后剩下的那个表。空表既没有头也没有尾',
        '- 任何非空广义表都可以**唯一地拆成「头 + 尾」**，所以递归算法与定义天然对上',
        '⚠️ **表一旦共享，释放时就必须处理别名，不能只按树来递归 `delete`**',
        '现代说法：**嵌套对象、对象图、共享 DAG** —— 编译器 AST、JSON 嵌套值都是它',
    ]),

    ('bullets', '长度、深度、原子数是三个不同的指标', [
        '对 `L = (a, (b,c), d)`：',
        '- `head(L) = a`，`tail(L) = ((b,c), d)`；对子表 `(b,c)`，头是 b、尾是 `(c)`',
        '**长度**只数最外层的三个元素 → **3**',
        '**深度**要进入子表 → **2**',
        '**原子数**遍历所有层 → **4**',
        '**三个指标回答不同问题，不能从括号或逗号数机械推出来。**',
    ]),

    ('bullets', '12.2.2–12.2.3 存储管理', [
        '**可利用空间表**（free list）：结点复用。原书用重载 `new/delete` 加一条全局 `avail` 链',
        '- 问题是**所有对象共享隐式全局状态**；本书改成显式的**索引句柄池**',
        '- `acquire` 从空闲栈弹一个下标，`release` 推回去；耗尽返回 `nullopt`',
        '**不同长度的空闲块**要复杂得多：分配要**按长度检索**并截取；',
        '- **回收时必须考虑能否与相邻块合并**，否则满足不了后来的大请求',
        '- ⚠️ **长期运行会产生碎片** —— 许多小块因太小而长期得不到使用',
        '- 空闲块要**自带「身份证」**：标记位、链表指针、块长度；通常用**双链表**实现',
    ]),

    ('bullets', '12.2.4 无用单元回收：两种办法', [
        '分配失败时可以：**直接拒绝**、**压缩**（把已分配块搬到一起）、或者**垃圾回收**',
        '**引用计数**：每个对象记有多少指针指着它，减到零就释放',
        '- 实现简单，**但对象互相指形成环时，计数永远掉不到零** → 泄漏',
        '**标记–清除**：从一组**根**出发走遍所有还能碰到的对象并打标记，再扫一遍堆回收未标记的',
        '- **能处理环**，但要求能从根集走到每一个活对象',
        '- 而且**要能区分「这是指针」和「这只是一个看起来像地址的整数」**',
        '**判生死的是「可达性」，不是入度或引用数** —— 这正是循环引用能被整组收回的原因',
    ]),

    ('table', '标记–清除走一遍（根集只有 R→A→B，另有 C↔D 互指）', [
        ['阶段', '待处理栈', '已标记', '动作'],
        ['放入根', '`A`', 'A', '**根先标记再入栈**'],
        ['弹出 A', '`B`', 'A、B', '沿 `A → B` 发现 B'],
        ['弹出 B', '空', 'A、B', '标记阶段结束'],
        ['清扫', '空', 'A、B', '保留 A、B；**回收 C、D**'],
    ], 'C、D 各有一个入边，却从任何根都走不到 —— 引用计数收不回，标记–清除照收'),

    ('bullets', '实现上的两条要求', [
        '**① 一定要「先标记、再入栈」。**',
        '- 若等到弹出时才标，**环或菱形共享会把同一结点反复压栈**',
        '- 结果也许仍对，但空间和时间可能失控',
        '**② 标记阶段用显式栈而不是递归。**',
        '- **垃圾回收恰恰是在内存吃紧时跑的，那时最不该再去吃调用栈**',
        '- 测试里有 20 万个对象的深链，递归标记必然压穿栈',
        '本书的实现仍然是**教学模型**：没有栈扫描、没有写屏障、没有分代，'
        '也不区分指针与整数 —— **而那恰恰是真实垃圾回收器最难的部分**',
    ]),

    ('section', '第 3 节', '12.3 Trie 结构和 Patricia 树', '换一种「分解」'),

    ('key', 'Trie 与 BST 分属两种「分解」',
     'BST 基于**对象空间**分解 —— 范围的分界由**存进树里的关键码值**决定，所以树形依赖插入顺序。'
     'Trie 基于**关键码空间**分解 —— 分界**预先定死**，与插入顺序无关。'),

    ('bullets', '12.3 Trie', [
        '**BST 的树结构与输入数据的顺序有很大关系** —— 按升序输入会退化成线性表，`O(n)`',
        '**要消除这种不平衡，最根本的办法是改变关键码的划分方式**：',
        '- 在关键码范围内，**对树结构中每一个结点预定义划分位置**',
        '- 于是**产生的树不依赖于插入顺序，其深度只受关键码精度的影响**',
        '- 关键码是 0–255 的整数时精度是 8 位，两个只在最低位不同的关键码要到第 8 次划分才分开',
        '`trie` 一词来源于 **retrieval** —— 暗示了它的主要应用：信息检索',
        '**与 B+ 树一样，Trie 内部结点仅作占位符引导检索，数据记录只存储在叶结点中**',
    ]),

    ('bullets', 'Trie 的前提，以及它的不平衡', [
        '**Trie 基于两个原则**：① 关键码集合是固定的；② 能够对结点进行分层标记',
        '- 元素可用 0–9 标记时，根就分出 10 个子结点，每个再分 10 个，直到全部区分开',
        '**公共前缀在树里只存一次** —— 特别适合字典、IP 路由这种「按前缀分类」的问题',
        '**查找时间与关键码长度成正比，与表里有多少个词关系不大** —— 这是它与 BST 的根本差别',
        '⚠️ **但这种结构并不能保证树是平衡的**：',
        '- 若根结点选得不合适，导致所有关键码都小于根，**右子树将没有任何元素**',
        '- **这是因为关键码分布得很不均衡** —— Patricia 树就是来压掉这些单路径的',
    ]),

    ('section', '第 4 节', '12.4 改进的二叉搜索树', '最佳 BST、AVL、伸展树'),

    ('bullets', '12.4.1 最佳二叉搜索树：先数一数有多少棵', [
        '同一个关键码集合，**插入次序不同，长出来的 BST 就不同**',
        '`n` 个关键码有 `n!` 种插入次序，但不同次序可能长出同一棵树',
        '**真正不同的形状只有 `C(2n,n)/(n+1)` 棵 —— 又是 Catalan 数**',
        '- 等价问法：中序排列是 `1..n` 时，这棵二叉树有多少种可能的前序排列',
        '- 例如 `{2,3,1}` 就**不是**任何 BST 的前序排列',
        '衡量的办法是**把树扩充成满二叉树**（5.1.2 节），空子树补成外部结点：',
        '- **每个外部结点代表一类检索失败** —— 落在两个相邻关键码之间的所有取值',
        '- 于是**成功的代价落在内部结点上，失败的代价落在外部结点上**，可以算进同一个目标函数',
    ]),

    ('bullets', '用动态规划求最佳 BST', [
        '**若键的查找频率不同，应把常查的键放得更靠近根**',
        '输入是成功查找权 `p[1..n]` 与失败查找权 `q[0..n]`',
        '**动态规划**：对每个区间尝试每一个键作根，取',
        '- `左子树代价 + 右子树代价 + 本区间总权` 最小的方案',
        '`cost[i][j]` 是区间代价，**`root[i][j]` 记录取得最小值的根** —— 因而能重建树形',
        '- 只记 cost 就只知道「最优值是多少」，**不知道最优解长什么样**',
        '朴素实现是 `O(n³)`。教材样例 `p={1,5,4,3}`、`q={5,4,3,2,1}` 的总成本是 **57**，根是第 2 个键',
    ]),

    ('bullets', '12.4.2 AVL 树', [
        'BST 最好 `O(log n)`、**最坏 `O(n)`** —— 键已有序时退化成一条链',
        '**Adelson-Velskii 和 Landis 发明了 AVL 树**（名字取自两位发明者姓氏的首字母）：',
        '- **任何结点的左子树和右子树高度最多相差 1**',
        '三条性质：① 空树是 AVL 树；② n 个结点的 AVL 树高度为 `O(log n)`；'
        '③ 其左右子树也都是 AVL 树',
        '**因为高度是 `O(log n)`，所以搜索、插入、删除都只需要 `O(log n)`**',
        '**关键问题是**：不论做何种操作都不改变 AVL 特性，'
        '**而且为保持结构所做的操作也要在 `O(log n)` 内完成**',
        '做法是给每个结点记一个**平衡因子**（右子树高减左子树高，只允许 −1、0、1），靠**旋转**维持',
    ]),

    ('bullets', '12.4.3 伸展树，以及第 11 章的呼应', [
        '**伸展树**（splaying tree）由 Hopcroft 与 Tarjan 于 1985 年发明',
        '- 与 10.1 节的线性表调整技术同属**自调整数据结构**',
        '- 它不记平衡因子，而是**每次访问后把该结点旋到根** —— 摊还 `O(log n)`',
        '**为什么外存索引不用 AVL？**（第 11.4 节已给出答案）',
        '- AVL 确实保持 `Θ(log n)` 平衡，**但其频繁的插入删除调整对外存索引需要很大的 I/O 量**',
        '- 外存要的是**每个结点尽可能多的关键码** + **局部调整** → 这就是 B 树',
        '**同一个「平衡」目标，内存里用旋转，外存里用分裂合并** —— 因为成本模型不同',
    ]),

    ('bullets', '本章小结', [
        '**多维数组**用地址公式定位，是随机存储结构；行优先 / 列优先要用能区分的下标去测',
        '**特殊矩阵**用公式压缩；**稀疏矩阵**用三元组或十字链表 —— 后者一次改动要动两条链',
        '**广义表**的元素可以是表；共享与环使得「递归 delete」不再成立',
        '**存储管理**：可利用空间表 → 变长块的分配与合并 → 碎片；'
        '回收有引用计数与标记–清除',
        '- **判生死的是可达性**；标记要「先标记再入栈」，而且要用显式栈',
        '**Trie** 按关键码空间分解，**树形与插入顺序无关，深度只受关键码精度影响**',
        '**最佳 BST** 用动态规划求；**AVL** 用旋转维持平衡；**伸展树**自调整',
    ]),

    ('bullets', '习题（完整题面与参考答案见同名讲义）', [
        '**地址公式**　`A[3][4][5]` 行优先下 `A[2][1][3]` 的偏移；改成列优先再算一遍',
        '**特殊矩阵**　推导下三角压缩存储的下标公式；说明对称矩阵怎样复用它',
        '**稀疏矩阵**　对给定非零元建十字链表；删除一个结点，写出两条链各自的变化',
        '**广义表**　对 `L = (a, (b, (c)), d)` 求长度、深度、原子数；写出 `head` / `tail` 的分解',
        '**垃圾回收**　构造一个引用计数回收不了、标记–清除能回收的例子',
        '**最佳 BST**　用动态规划手算 4 个键的最优树；说明 `root` 表的作用',
        '**AVL**　依次插入一组键，画出每次旋转后的形状；说明为什么外存索引不用它',
    ]),
]
