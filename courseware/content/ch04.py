# -*- coding: utf-8 -*-
"""第4章 字符串 —— 课件内容。

素材出自 book/ch04-string.md，代码摘自 code/ch04/string_class 与
code/ch04/pattern_matching。
"""

FIG = '../book/assets/scan/'

META = {
    'title': '第4章　字符串',
    'subtitle': '字符编码与比较 · 串的存储 · 朴素匹配与 KMP',
    'footer': '数据结构与算法 · 第4章 字符串',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('bullets', '本章要回答三个问题', [
        '**字符串和普通线性表差在哪？**',
        '- 数据对象限定为**字符集**；运算的对象是**整段**，不是单个元素',
        '**「一个字符」到底是几个字节？**',
        '- 4.1.1 从 ASCII 到 UTF-8，以及一条 2008 年的书还讲不到的结论',
        '**在几十亿字符里找一个几百字符的模式，怎么找得动？**',
        '- 4.3 朴素匹配的瓶颈，以及 KMP 为什么是线性的',
    ]),

    ('key', '本章的一句话',
     '模式匹配的关键不在「比得更快」，而在**不要重复比**。'
     'KMP 的全部收益，就是让目标串的下标**只增不减**。'),

    ('section', '第 1 节', '4.1 字符串的基本概念', '子串、字符编码、编码顺序'),

    ('bullets', '4.1 字符串与线性表的两点区别', [
        '**字符串**是组成元素为单个字符的线性表，简称**串**。字符个数称为**长度**',
        '尽管它是特殊的线性表，逻辑结构上有两点区别：',
        '- **① 数据对象约束为字符集**',
        '- **② 基本操作通常以「串的整体」为对象** —— 拼接、复制、抽取子串、模式匹配，'
        '动的都是一整段',
        '**子串**：若存在 i 使 `P[j] == T[i+j]` 对所有 j 成立，则 P 是 T 的子串',
        '- **空串是所有串的子串**，任何串都是自身的子串',
        '- 非空且不为自身的子串叫**真子串**',
    ]),

    ('bullets', '原书【代码4.1】的三处问题', [
        '**① 类名是小写的 `string`** —— 在任何 `using namespace std;` 的翻译单元里，'
        '它与 `std::string` 构成歧义。原书正文随后又改用大写 `String`，**同一章两个名字混用**',
        '**② `int isEmpty();` 用 `int` 表达布尔**；`int find(char, int)` **用 `-1` 表示「没找到」**',
        '- 与「位置 0」只差一个符号，调用方漏判就把「没找到」当成「匹配在开头」',
        '**③ 修改器按值返回** —— `string append(const char c);` 返回 `string` 而非引用',
        '- 代码4.1 只有声明没有函数体，所以**不能断定**原书会丢结果',
        '- **能断定的是签名含混**：调用方无从判断 `s.append(\'x\');` 是改了 s，还是返回了新串',
        '本书改为：类名大写、`bool empty()`、`std::optional<size_type> find(...)`、修改器返回 `String&`',
    ]),

    ('bullets', '4.1.1 字符编码', [
        '字符串的**逻辑元素**是字符，内存里保存的却是**编码单元**',
        '**ASCII**：单字节，低 7 位表示字符，最高位为 0。共 128 个：',
        '- 第 0–32 号与第 127 号共 34 个是控制字符或通信专用字符',
        '- 大写字母 65–90，小写字母 97–122，数字 48–57',
        'ASCII 没有考虑多语言。各语言各自发展出**互相冲突**的编码：',
        '- 中文 GB2312-80、BIG5，日文 S-JIS / JIS，韩文 Wansung —— '
        '**同一个数字在两种编码里是两个不同的字符**',
        '**UNICODE** 统一编号；今天最常见的落地形式是 **UTF-8**（1–4 字节，与 ASCII 完全兼容）',
    ]),

    ('key', '一条 2008 年的书还讲不到的结论',
     'C++ 的 `char` 是一个字节，**不等于**一个「人眼看到的字符」。'
     '`std::string::size()` 返回的是**字节数**，不能用来数 Unicode 字符。'),

    ('bullets', '4.1.2 字符的编码顺序', [
        '字符编码表遵循**偏序编码规则**：编码顺序与自然次序一致',
        '- `encode(\'0\') + 1 == encode(\'1\')`，一直到 `\'9\'`；字母同理',
        '所以任意两个字符可以直接用编码值比大小；对字母而言就是**字典编目次序**',
        '两个串按字符顺序比：`"monday" < "sunday" < "tuesday"`，`"123" < "1234" < "23"`',
        '⚠️ **这只是字典次序，不是日常理解的次序** —— 星期一在星期二之前，整数 123 比 23 大',
        '比较规则：**先比第一个不同的编码单元；一个是另一个的前缀时，短者更小**',
        '- 大小写、重音、中文排序都可能需要额外的 locale 或排序键',
    ]),

    ('code', '这里有个 C++ 特有的坑，必须点破', '''$ g++ -std=c++17 -Wall -Wextra litcmp.cpp
litcmp.cpp:7:25: warning: comparison between two arrays [-Warray-compare]
    7 |     std::cout << ("123" < "1234") << ' ' << ("1234" < "23") << '\\n';
      |                   ~~~~~~^~~~~~~~

$ ./litcmp
true true      # ("123" < std::string("1234"))、(std::string("1234") < "23")
false true     # ("123" < "1234")、("1234" < "23")  —— 第一个的答案反了''',
     '两边只要有一边是 std::string，比的就是**内容**；两边都是裸字面量，比的是**地址**'),

    ('section', '第 2 节', '4.2 字符串的存储结构和实现', '顺序存储与 String 类'),

    ('bullets', '4.2 串的顺序存储', [
        '串一般用**顺序存储**：一块连续的字符数组。两种记长度的办法：',
        '- **末尾放结束标记** —— C 风格字符串用 `\\0`。求长度是 O(n)',
        '- **单独存一个长度字段** —— 求长度 O(1)，而且**串里可以含 `\\0`**',
        '本书的 `String` 用后者：`char* data_` + `size_type size_` + `size_type capacity_`',
        '这个类同样自己管着 `new[]` 出来的缓冲区，所以三法则（乃至五法则）一条都不能少',
        '- 与第 2 章顺序表、第 3 章顺序栈**是同一课**，这已经是第三次了',
    ]),

    ('bullets', '原书 String 类的三处硬伤', [
        '**① `assert(str != \'\\0\');`** —— `str` 是 `char*`，`\'\\0\'` 是 `int`',
        '- **指针与整数比较，本身就编译不过**',
        '**② `String(char* s)`** —— 参数不是 `const char*`',
        '- 于是书中**自己的例子** `String s1 = "Hello";` 在 `-Werror` 下编译失败',
        '- C++11 起字符串字面量的类型是 `const char[N]`，不能绑到 `char*`',
        '**③ 算法4.5 越界时 `return NULL`** —— 调用方拿去 `strlen(nullptr)`，**运行期 SEGV**',
        '本书：`const char*` 参数、越界抛 `std::out_of_range`、`find` 返回 `optional`',
    ]),

    ('section', '第 3 节', '4.3 字符串的模式匹配', '朴素算法与 KMP'),

    ('bullets', '4.3 模式匹配问题', [
        '在**文本**（text）中寻找一个给定的**模式**（pattern）。通常文本很大，模式很短',
        '- 文本编辑：模式约 10 个字符，文本几百字到上百万字',
        '- DNA 分析：基因几百个字符，而**人类染色体长达 30 亿**',
        '**精确匹配** —— 哪怕只差一个字符也算失败。本节与原书一样只讨论精确匹配的单选情况',
        '- 多选（`s?t` 里的 `?` 叫**通配符**）、正则、近似匹配都不在本节',
        '要回答的是：P 是否作为 T 的**连续子串**出现，若出现，**首次**出现在哪个位置',
        '⚠️ 记住一句：**P 和 T 的第一个字符都从位置 0 开始**',
    ]),

    ('bullets', '4.3.1 朴素算法：思路与代价', [
        '思路直白：把模式的首字符对齐目标的每一个位置，逐字符比较',
        '- 一旦**失配**，就把模式向右移动**一个**字符位置，重新开始下一趟',
        '**最佳**：目标首位置就是配串，`O(|P|)`',
        '**最差**：`O(|T| · |P|)`。例如 T 是 n 个 `a`，P 是 m-1 个 `a` 加一个 `b`',
        '- 每趟都在模式的**最后一个**字符处不匹配，共 `|P|(|T|-|P|+1)` 次比较',
        '- **此类情况很少出现在文本中，却经常出现在 DNA 与图像信息中**',
        '**平均**：字符集大小为 2 时约 `2|T|` 次；一般为 `(|A|^(|P|+1) - |A|) / (|A|-1)`',
    ]),

    ('key', '朴素算法的问题出在哪里',
     '一旦失配，**无论模式的具体情况如何，都只右移一位**。'
     '于是目标 T 中的字符会被反复比较 —— 这叫**目标的回溯**。'),

    ('bullets', '一个具体的浪费', [
        '模式 `P = "abacab"`，目标 `T = "abacaabaccabacabaa"`',
        '第 1 趟发现 `P₅ ≠ T₅`，朴素算法把模式右移一位，让 `P₀` 与 `T₁` 比较',
        '**但这次比较是多余的**：',
        '- 上一趟已经比过 `T₁` 与 `P₁`，而且**相等**',
        '- 又已知 `P₁ ≠ P₀`（这是模式自身的性质）',
        '- 所以 `P₀ ≠ T₁` 是**推得出来的**，根本用不着重新比',
        '**Knuth、Morris、Pratt 的发现**：每次右移的位数**存在，且与目标串无关，'
        '仅依赖于模式本身**',
    ]),

    ('ascii', '4.3.2 特征向量：一个例子就清楚了', r"""
T = ababab d,  P = ababd,  从位置 0 开始比

位置:  0 1 2 3 4 5 6
T   :  a b a b a b d
P   :  a b a b d
                ^ 这里失配（T[4]='a'，P[4]='d'）

失配前已经匹配上了 abab。问：abab 的前缀和后缀里，最长的相同一段是多少？

     abab 的前缀:  a,  ab,  aba
     abab 的后缀:  b,  ab,  bab
     最长相同的一段:  "ab"，长度 2

所以模式可以一次右移 4 - 2 = 2 位，而且移过去之后前两个字符不必再比：

位置:  0 1 2 3 4 5 6
T   :  a b a b a b d
P   :      a b a b d
             ^ 这两个已知相同，从这里继续比
""", '整个过程中 **T 的下标从来没有往回退过** —— 这正是 KMP 优于朴素算法的地方'),

    ('code', '特征向量（next 数组）怎么算', '''[[nodiscard]] inline std::vector<next_type> build_next(std::string_view pattern) {
    const std::size_t m = pattern.size();
    std::vector<next_type> next(m);
    if (m == 0) { return next; }
    next_type i = 0;
    next_type k = -1;
    next[0] = -1;
    while (i < static_cast<next_type>(m)) {
        while (k >= 0 && pattern[i] != pattern[k]) {
            k = next[k];              // 沿已算好的特征值回退
        }
        ++i;
        ++k;
        if (i == static_cast<next_type>(m)) { break; }
        // P[i] 与 P[k] 相等时可以直接借用 next[k]，省掉一次注定失败的比较
        next[i] = (pattern[i] == pattern[k]) ? next[k] : k;
    }
    return next;
}''',
     '`next(ababd) = -1 0 -1 0 2`。最后那个三目就是「优化版」next 与朴素 next 的差别'),

    ('code', '4.3.3 KMP：与朴素算法只差失配那一步', '''next_type i = 0;    // 模式下标，可以退到 -1
std::size_t j = 0;  // 目标下标，**只增不减**

while (i < static_cast<next_type>(m) && j < n) {
    if (i == -1 || text[j] == pattern[i]) {
        ++i;
        ++j;
    } else {
        i = next[i];        // 不再退回开头，而是退到 next[i]
    }
}
return i >= static_cast<next_type>(m)
           ? std::optional<std::size_t>(j - m)     // 注意是 j - m，不是 j - m + 1
           : std::nullopt;''',
     'next 由调用方传入：同一个模式只算一次、多次匹配复用 —— 接口把这条性质显式表达出来'),

    ('bullets', '为什么 KMP 是线性的', [
        '**关键在于目标下标 `j` 只增不减。**',
        '循环体里 `++j` 至多执行 `|T|` 次，与它同处一条语句的 `++i` 也不超过 `|T|` 次',
        '能让 `i` 减少的只有 `i = next[i]`，而 **`next[i] < i`**，所以每执行一次 `i` 至少减 1',
        '一旦减到 −1，下一步必然进入 `++i, ++j` 分支',
        '因此 `i = next[i]` 的执行次数**不超过** `++i, ++j` 的次数加 1',
        '- 整个循环体至多执行 `2|T| + 1` 次 —— **与目标串长度成线性关系**',
        '加上算 next 的 `O(|P|)`，KMP 整体是 **`O(|P| + |T|)`**',
    ]),

    ('image', '图 4.12　KMP 匹配示例', FIG + 'fig-4-12.png',
     '第 1 趟第 5 次比较失配，特征值 −1，右移 4−(−1)=5 位；第 4 趟第 22 次匹配成功'),

    ('bullets', '一条能真正检出退化的测试', [
        '本书用朴素算法的**最坏情况**验证这条线性性质：',
        '- `T` = 20 万个 `a`，`P` = 1999 个 `a` 加一个 `b`',
        '**KMP 在其上瞬间完成**；而失配后不按特征值回退的实现会退化成 `O(|T|·|P|)`，'
        '**直接撞上构建闸门的超时**',
        '这就是一条有牙的测试的样子：',
        '- 不是「跑通了」，而是「实现退化了就会红」',
        '- 把复杂度的差别做成**可观测的现象**，而不是留在注释里',
    ]),

    ('bullets', '本章抓到的原书错误', [
        '**算法4.6 与 算法4.8 的返回值都差 1**：原书写 `return (j - pLen + 1);`',
        '- 在 0 起始下标体系里，正确的是 `j - pLen`',
        '- 用书中**自己的例子**当场可见：匹配始于下标 10，原书返回 11',
        '- 四组数据对拍证实。这一条同时命中两个算法，因为第二个是照抄第一个的收尾',
        '**正文的 next 数组比模式还长一位**，与图 4.11 自相矛盾',
        '原书用 `assert(m > 0)` 挡住空模式 —— 而 **`assert` 在 `NDEBUG` 下会被整个编译掉**',
        '- 本书约定空模式返回 0，与 `std::string::find("")` 一致',
    ]),

    ('bullets', '本章小结', [
        '**字符串是数据对象限定为字符集、且以整段为操作对象的线性表**',
        '**编码**：ASCII 单字节；UTF-8 用 1–4 字节兼容 ASCII。`char` ≠ 一个人眼看到的字符',
        '**比较**：按编码单元的字典序，前缀更短者更小。'
        '⚠️ 两个裸字面量之间的 `<` 比的是地址',
        '**朴素匹配**最坏 `O(|T|·|P|)`，瓶颈是**目标的回溯**',
        '**KMP** 用模式自身的前后缀信息决定右移多少，目标下标只增不减，`O(|P| + |T|)`',
        '- 特征向量**只依赖模式**，同一模式可跨多个目标复用',
        '**顺序存储 + 自管缓冲区** —— 三法则在本章第三次出现',
    ]),

    ('bullets', '习题（完整题面与参考答案见同名讲义）', [
        '**习题 2**　给出使 `s1 + s2 == s2 + s1` 成立的**所有**可能条件',
        '**习题 4**　把长 n 的串改造成「奇数位升序在前半、偶数位降序在后半」',
        '- 例：`ABCDEFGHIJKL` → `ACEGIKLJHFDB`',
        '**习题 7**　**线性时间**判断 T 是否是 T′ 的循环移位（如 `arc` 与 `car`）',
        '**习题 9**　求 `BAAABBBAA` 的特征向量，并与给定目标匹配，**画出过程**',
        '**上机 3**　求最长重复子串及其下标（`abcdacdac` → `cdac`，下标 2）',
        '**上机 4**　行编辑程序：查找必须**自己实现** KMP，不许用环境提供的查找',
    ]),
]
