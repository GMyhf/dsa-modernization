# -*- coding: utf-8 -*-
"""第9章 文件管理和外排序 —— 课件内容。素材出自 book/ch09-external-sort.md。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第9章　文件管理和外排序',
    'subtitle': '成本模型换了 · 置换选择 · 多路归并与选择树',
    'footer': '数据结构与算法 · 第9章 文件管理和外排序',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('key', '本章换掉了一件事：成本模型',
     '前八章数的是**比较次数**。这一章数的是**页 I/O 次数** —— '
     '一次磁盘 I/O 往往比一次比较贵几个数量级。'),

    ('bullets', '本章要回答三个问题', [
        '**内存装不下的数据怎么排？** 9.3 切成顺串，再反复归并',
        '**怎么让顺串更长？** 9.3.1 置换选择排序 —— 平均能把顺串做到 2M',
        '**k 路归并里怎么快速选出最小的？** 9.3.3 选择树（赢者树 / 败者树）',
        '两个目标贯穿全章，都是为了**减少归并趟数**：',
        '- **减少初始顺串个数 m**（置换选择）',
        '- **增加归并路数 k**（多路归并）',
    ]),

    ('section', '第 1 节', '9.1 主存储器和外存储器', '先把成本模型建起来'),

    ('bullets', '9.1 两种存储器', [
        '**主存**（RAM、cache）在主板上，按单元连续编址，CPU 直接访问',
        '- 一次存取可以看成很小的常数，单位是**纳秒**。快、贵、容量小、断电就丢',
        '**外存**（硬盘、磁带、U 盘）便宜、容量大、断电不丢',
        '- 一次存取以**毫秒**甚至秒计 —— **比内存慢几个数量级**',
        '外存慢的一个原因是**每次访问都要先定位再读写**：',
        '- 磁盘要把磁头移到目标磁道，再等扇区转到磁头下，定位就要几毫秒到几十毫秒',
        '- **远慢于真正把数据读出来的时间**',
        '所以外存按固定大小的**页块**存取：一次定位读写一整页，减少定位次数',
    ]),

    ('bullets', '先建立成本模型', [
        '**分析外排序时，不能再只数比较次数。**',
        '设文件有 N 个记录，每页可放 B 个记录：',
        '- 顺序扫描一遍至少要读 `⌈N/B⌉` 页',
        '- 若这一遍还要把结果写回外存，I/O 量约 `2⌈N/B⌉` 页',
        '**内存中多做几次比较，通常远比多一次整文件读写便宜。**',
        '举个数：1 亿条 16 字节记录约 1.6 GB。内存工作区只有 64 MB 时，',
        '- **任何「把全部记录放进数组再排序」的内部排序都不成立**',
        '- 外排序必须让大部分记录**始终留在外存**，内存里只留少量缓冲页和当前候选',
    ]),

    ('image', '图 9.1　置换选择算法流程', FIG + 'fig-9-1.png',
     '输入缓冲空了才整页补入，输出缓冲满了才整页写回：**工作区处理记录，真正与设备交换的是页**'),

    ('section', '第 2 节', '9.2 文件的组织和管理', '逻辑文件与物理文件'),

    ('bullets', '9.2.1 文件组织', [
        '**文件**是外存上的数据结构，由大量性质相同的**记录**组成',
        '原书区分**逻辑文件**（应用看见的记录顺序与字段）与**物理文件**'
        '（系统实际读写的页、块、地址）—— **这个区分仍然重要**',
        '四种组织方式是第 9–11 章的共同骨架：',
        '- **顺序文件** —— 连续扫描快，按中间位置更新贵',
        '- **散列文件** —— 按键定位快，范围扫描与有序输出差（第 10 章）',
        '- **索引文件** —— 用较小的目录把键映射到记录位置（第 11 章）',
        '- **倒排文件** —— 用属性值反查记录集合（搜索系统的核心）',
    ]),

    ('bullets', '一个必须澄清的说法', [
        '顺序文件的「物理顺序与逻辑顺序一致」**不能理解成文件一定连续占用磁盘扇区**',
        '- 现代文件系统可能重排块位置、使用写时复制、或者放在远端对象存储上',
        '**对本章而言真正要守的是**：',
        '- **读者可以顺序消费页，写者可以批量产生页**',
        '- 这正是归并比大量随机交换更适合外排序的原因',
        '定长记录能用「第 i 条在第几页」直接定位；',
        '变长记录通常需要**槽目录**，把稳定的槽号映射到会移动的页内偏移',
    ]),

    ('bullets', '9.2.2 C++ 的流文件：原书接口已是历史写法', [
        '**概念仍然成立**：文件是持久化的数据对象，流是程序对它顺序读写的通道',
        '但接口变了：`<fstream.h>` **不是标准 C++17 头文件**',
        '- 现在用 `<fstream>` 里的 `std::ifstream` / `std::ofstream` / `std::fstream`',
        '- 文件名用 `std::string` 或 `std::filesystem::path`，位置用 `std::streamoff`',
        '- **流对象离开作用域会自动关闭**，显式 `close()` 只在需要尽早检查时才有意义',
        '⚠️ **不能把含指针、虚函数表或依赖字节序的 C++ 对象直接写入文件**',
        '- 要定义稳定的字段格式或序列化协议',
        '⚠️ `seekg` / `seekp` 只适用于支持随机访问的对象 —— 管道、终端、网络流不能任意定位',
    ]),

    ('section', '第 3 节', '9.3 外排序', '顺串生成与归并'),

    ('bullets', '9.3 外排序的两个阶段', [
        '内存装不下时，**把外存数据划分成若干段，每段读进内存用内排序排好**',
        '- 这些已排序的段叫**顺串**（run，也叫归并段），写回外存等待归并',
        '**外排序的时间由三部分组成**：内部排序、外存读写、内部归并',
        '- 而**外存读写慢得多，所以减少外存读写次数是提高效率的关键**',
        '设有 m 个初始顺串，每次归并 k 个，**归并趟数是 `⌈log_k m⌉`**',
        '于是减少趟数只有两条路：',
        '- **减少 m** —— 9.3.1 置换选择排序；**增大 k** —— 9.3.3 多路归并',
        '**每减少一趟，省下的是约 `2N/B` 次页 I/O，而不是几个比较。**',
    ]),

    ('bullets', '9.3.1 置换选择排序：让顺串更长', [
        '它是**在堆排序的基础上演化而来的**',
        '朴素做法：内存一次装 M 条，每批排成一条长为 M 的顺串 → 顺串数 `⌈N/M⌉`，一个也少不了',
        '**置换选择的改进**：输出堆顶之后，若下一条记录**不小于**刚输出的值，'
        '它**还可以进入当前顺串**；否则**冻结**到下一趟',
        '**顺串长度并不相等，但平均情况下可以形成长度为 2M 的顺串**',
        '- 顺串数因此大约**减半**，而每减少一半，归并趟数就可能少一趟',
        '- **省下的是一整轮完整的读写**',
    ]),

    ('table', '图 9.2 的过程摊开（输入 50 49 35 45 30 25 15 60 16 27 1，M=7）', [
        ['步', '输出', '读入', '去向', '当前顺串', '冻结'],
        ['初始', '—', '前 7 条', '建堆', '—', '—'],
        ['1', '15', '60', '60 ≥ 15，**活跃**', '15', '—'],
        ['2', '25', '16', '16 < 25，**冻结**', '15 25', '16'],
        ['3', '30', '27', '27 < 30，冻结', '15 25 30', '16 27'],
        ['4', '35', '1', '1 < 35，冻结', '15 25 30 35', '16 27 1'],
        ['5–8', '45,49,50,60', '输入耗尽', '排空活跃堆',
         '**15 25 30 35 45 49 50 60**', '16 27 1'],
        ['新顺串', '1,16,27', '—', '冻结区重新建堆', '**1 16 27**', '—'],
    ], '第一条顺串长度 8，**已经超过 M=7** —— 这就是置换选择的收益'),

    ('bullets', '两个守门不变量，以及输入形态的影响', [
        '这张表给出两条必须成立的不变量：',
        '- **① 每条顺串内部非递减** —— 保证归并可以工作',
        '- **② 全部顺串拼起来包含输入的每条记录，且恰好一次** —— 防止冻结时丢记录或重复输出',
        '**输入次序决定顺串长度：**',
        '- **单调递增**输入会一直补进活跃堆 → 得到**一条**长顺串',
        '- **单调递减**输入几乎每次都冻结 → 顺串接近长度 M',
        '- 随机排列平均约 2M —— **但这是平均结论，不是最坏保证**',
        '⚠️ **若把整份输入推进一个堆再依次弹出，那是堆排序，不是置换选择**',
        '- 旧实现曾经这样做，而测试只断言 `{3,1,2} → {1,2,3}` —— **堆排序也能过**',
    ]),

    ('bullets', '9.3.2 二路外排序', [
        '对 m 个顺串两两归并，趟数是 `⌈log₂ m⌉`',
        '**归并两条顺串至少需要三个缓冲页**：两页放两路未消费的记录，一页收集输出',
        '- 比较两个输入缓冲的队首，较小者移入输出缓冲',
        '- 某个输入页耗尽就读该顺串下一页；输出页满则整页写回',
        '- **这样每个输入页恰好读一次，每个输出页恰好写一次**',
        '原书的例子：3000 条记录、10 条各 300 的初始顺串 → 需要 `⌈log₂10⌉ = 4` 趟',
        '- 归并阶段传输约 `4 × 2N = 8N` 条记录',
        '- **置换选择若把顺串数从 10 降到 5，就只要 3 趟 —— 直接省掉一次完整读写**',
    ]),

    ('key', '「增加归并路数总会更快」也不成立',
     'k 越大趟数越少，但**每一路至少要留一个输入缓冲**，还要有输出缓冲。'
     '内存固定时，每路缓冲变小、补页更频繁。选 k 是「少趟数」与「每路够缓冲」的折中。'),

    ('bullets', '9.3.3 多路归并：先算一笔账', [
        'k 路归并时，要在 k 个队首里找最小值，**朴素做法需要 k−1 次两两比较**',
        '对记录总数 n 的文件，内部归并的总比较次数是：',
        '- `⌈log_k m⌉ · (k−1) · (n−1) = ⌈log₂m⌉ / log₂k · (k−1) · (n−1)`',
        '**随着 k 增大，`(k−1)/log₂k` 也增大** —— '
        '内部归并的时间随之增大，**削减了增大 k 带来的好处**',
        '**选择树正是用来消掉这一项的**：它找最小值只要树高 `⌈log₂k⌉` 次比较',
        '- 代入后总比较次数变成 `⌈log₂m⌉ · (n−1)`',
        '**此时内部比较次数与 k 无关** —— 这就是选择树存在的全部理由',
    ]),

    ('bullets', '赢者树与败者树', [
        '选择树是**完全二叉树**，顺序存储；也称**竞赛树**，反映一系列「淘汰赛」的结果',
        '**赢者树**：n 个外部结点（选手）、n−1 个内部结点，'
        '**每个内部结点记录其左右子结点那场赛局的赢家**',
        '- 树根记录整个比赛的胜者。数组 B 里存的是数组 L 的**索引**，不是值',
        '**优点**：某个选手的分数改变时，**只需沿着从它到根的路径修改**，'
        '不必改变其他比赛的结果',
        '- 而且**爬到某一场比赛结果没变时就可以停** —— 修改次数在 0 到 log₂n 之间',
        '**败者树**：内部结点记录**败者**的下标，另加 `B[0]` 记录**冠军**',
        '- 重赛时新结点与父结点比，**败者留在父结点，赢者继续往上**',
        '- **这正是败者树比赢者树少一次「取兄弟」访问的原因**，外排序实现里更常见',
    ]),

    ('bullets', '一句限定：选择树不是通用的「取最小」工具', [
        '赢者树和败者树统称**选择树**，是**外部排序多路归并中的专用优化**',
        '**内存里一般的「取最小」任务，通常直接用二叉堆或优先队列**',
        '- 只有需要**反复从固定的 k 路输入中选冠军**时，才值得维护选择树',
        '差别在哪：堆的元素集合是变动的（任意插入删除）；',
        '- 选择树的**选手位置是固定的 k 个槽**，每次只是**替换其中一个槽的值**',
        '- 「位置固定、只换值」这个约束，正是它能只沿一条路径重赛的前提',
    ]),

    ('bullets', '本章小结', [
        '**成本模型换了**：数的是页 I/O，不是比较次数',
        '外排序 = **顺串生成 + 归并**；归并趟数 `⌈log_k m⌉`',
        '**减少趟数的两条路**：减小 m（置换选择）、增大 k（多路归并）',
        '**置换选择**在堆排序上加一条「不小于刚输出的值就还能进当前顺串」，'
        '平均顺串长 **2M**',
        '- 顺串长度不等；**单调递增输入得一条长顺串，单调递减输入接近 M**',
        '**选择树**把 k 路选最小的代价从 `k−1` 降到 `log₂k`，'
        '使内部比较次数**与 k 无关**',
        '**败者树**比赢者树少一次取兄弟访问，是外排序实现里更常见的选择',
    ]),

    ('bullets', '习题（完整题面与参考答案见同名讲义）', [
        '**成本**　N 条记录、每页 B 条、k 路归并：写出总页 I/O 的表达式，'
        '并说明减少一趟省下多少',
        '**置换选择**　对给定输入手工走一遍，写出每一步的活跃堆与冻结区',
        '**边界**　构造使置换选择只得到**一条**顺串的输入；再构造使每条顺串都恰好为 M 的输入',
        '**选择树**　对 5 路输入画出赢者树与败者树；替换一名选手，写出重赛路径',
        '**比较**　为什么败者树比赢者树少一次访问？画图说明',
        '**上机**　实现置换选择并验证两条不变量：顺串内部非递减、全部记录恰好出现一次',
    ]),
]
