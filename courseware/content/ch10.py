# -*- coding: utf-8 -*-
"""第10章 检索 —— 课件内容。素材出自 book/ch10-search.md，代码摘自 code/ch10/search_hash。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第10章　检索',
    'subtitle': '线性表检索 · 集合 · 散列方法',
    'footer': '数据结构与算法 · 第10章 检索',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写；code/ 全部单元双档编译运行通过'],
}

SLIDES = [

    ('bullets', '本章要回答三个问题', [
        '**不排序、排序、分块，检索的代价各是多少？** 10.1 三种线性表检索',
        '**「在不在里面」这个问题怎么问？** 10.2 集合与位向量',
        '**能不能不比较，直接算出地址？** 10.3 散列方法',
        '前面各章的检索都**基于关键码比较**：',
        '- 顺序、分块靠「等于 / 不等于」；二分、树形靠「大于 / 等于 / 小于」',
        '- **这些方法的平均检索长度都与 n 有关**',
        '**最理想的情况是根据关键码值直接找到存储地址** —— 这就是散列',
    ]),

    ('key', '本章的一句话',
     '散列法的平均检索长度**不依赖于表中结点的个数**，'
     '而是随**负载因子**的增大而增加 —— 前面每一种检索方法的 ASL 都是 n 的函数。'),

    ('section', '第 1 节', '10.1 基于线性表的检索', '顺序、二分、分块'),

    ('bullets', '10.1.1 顺序检索', [
        '逐个把关键码与给定值比较，相等则成功，走完还没找到就失败',
        '**表中各数据元素可以任意排列** —— 这是它唯一、也是最宝贵的优点',
        '- 不要求有序，实现最简单，**链表和数组都能用**',
        '最好比较 1 次，最坏 n 次；等概率假设下 **ASL = (n+1)/2**',
        '**监视哨**：把目标值先写进表外的一个额外槽，循环里就只问「当前元素等不等于目标」',
        '- 作用是**保证 while 循环一定终止**，于是不必再写 `i > 0` 这个条件',
        '- 原理：**当循环需要测试两个或多个条件时，应尽量减少测试条件** —— 常数小一点，阶不变',
        '⚠️ 只读输入、并发共享数组、或没有额外槽时，**不应为省一个边界判断去改数据**',
    ]),

    ('bullets', '10.1.2 二分检索', [
        '前提两条：**表必须按关键码有序**，而且**能按下标随机访问** —— 所以适合数组、不适合链表',
        '每次取当前区间的中点：相等则停；目标更大丢掉左半，否则丢掉右半',
        '- 区间长度每次至少减半，比较次数 `O(log n)`。**它是比较典型的分治算法**',
        '实现上用**半开区间** `[first, last)`：`middle = first + (last-first)/2`',
        '- 走左半时 `last = middle`，走右半时 `first = middle + 1`',
        '- 这样不必写 `middle - 1`，**无符号下标不会在 `middle == 0` 时下溢**',
        '不变量：**若目标存在，它一定还在 `[first, last)` 里**',
    ]),

    ('image', '图 10.2　二分检索的决策树', FIG + 'fig-10-2.png',
     '这棵决策树其实就是一棵 BST；比较次数 = 结点所在层数 + 1'),

    ('bullets', '从决策树读出来的两件事', [
        '**① 对同一长度的有序数组，决策树的形状是固定的**',
        '- 不管数组里的数值是什么，**下标位置的 BST 保持不变**',
        '**② 代价可以从代数角度精确算出来**：',
        '- 只比较 1 次成功的结点有 `2⁰` 个，2 次的有 `2¹` 个，j 次的有 `2^(j-1)` 个',
        '- **最大检索长度 = `⌈log₂(n+1)⌉`**',
        '- 等概率时 `ASL ≈ log₂(n+1) − 1` —— **平均与最大相近，因而效率高**',
        '**两条硬约束**：要事先排序（**排序本身很费时**）；只适用顺序存储（插删困难）',
        '- **所以二分检索特别适用于「一经建立就很少改动、而又需要经常检索」的线性表**',
    ]),

    ('bullets', '10.1.3 分块检索（索引检索）', [
        '把 n 个元素分成 b 块：**块与块之间有序，块内部可以无序**',
        '另造一张**块索引**，每项记下该块的最大关键码和块的起始位置',
        '检索分两个阶段：① 确定在哪一块；② 在块内检索',
        '- **第二阶段只能顺序检索**；第一阶段可以顺序，也可以二分',
        '顺序查索引时 `ASL = (b+1)/2 + (s+1)/2`，**s 取 √n 时最小，为 √n + 1**',
        '- 原书的例子：10000 个结点分成 100 块，分块检索平均 101 次比较',
        '- 顺序检索约 5000 次，二分检索最多 14 次 —— **分块高于顺序、低于二分**',
        '**它的优点在插入删除上**：插入可以放在块尾；删除时把块内最后一个记录移过来填空',
    ]),

    ('ascii', '分块检索：块间有序只负责排除别的块', r"""
块 0:  22  12  13   9   8          索引项 (max=22, start=0)
块 1:  33  42  44  24  48          索引项 (max=48, start=5)
块 2:  60  80  74  49  86  53      索引项 (max=86, start=10)

查 44:  索引里比较 22、48  ->  只能在块 1
        块内顺序比较 33、42、44     命中

查 23:  索引里同样落到块 1（23 < 48）
        块内扫完 33 42 44 24 48     失败
""", '⚠️ **不能因为 23 小于块最大值 48 就断言它存在。** 块间有序只负责排除别的块，块内仍然无序'),

    ('section', '第 2 节', '10.2 集合的检索', '成员关系与位向量'),

    ('bullets', '10.2.1 集合的数学特性', [
        '**集合由若干确定的、相异的对象构成**；一个集合中不包含两个完全相同的元素',
        '- `{1,4}` 与 `{4,1}` 是同一个集合 —— **元素的次序无关紧要**',
        '基本关系与运算：成员关系 `x ∈ A`；子集 / 超集；并、交、差；幂集',
        '- **每个集合都是其本身的子集，空集是任何集合的子集**',
        '因为元素互异，所以：',
        '- **插入一个已在里面的键、删除一个不在里面的键，都是正常、可预期的失败**',
        '- 应当返回「没做成」，而不是抛异常或打印一行 —— 与全书接口口径一致',
    ]),

    ('bullets', '可拿来测试的代数性质', [
        '集合运算应满足一组**能写成断言**的性质：',
        '- `A ∩ B = B ∩ A`（交换律）；`A ∩ A = A`（幂等）；`A ∩ ∅ = ∅`',
        '- 包含关系：`A ⊇ A`；**空集被任何集合包含**',
        '**示例测试不只核对一次结果，还要用这些性质** ——',
        '- 防止实现「碰巧对一组输入是对的」',
        '本章的 `IntSet` 故意用**线性表**实现，把接口先钉死：',
        '- 每个运算最后都落到 `contains`，而 `contains` 是顺序检索 `O(n)`',
        '- **所以交集是 `O(n·m)`。10.3 节的散列就是来把这个 O(n) 压成 O(1) 的**，接口一个字不改',
    ]),

    ('ascii', '有限全集上的位表示', r"""
全集 {a, b, c, d, e, f, g, h}，从低位到高位依次对应

    A = {a,c,f}      ->  00100101
    B = {b,c,f,g}    ->  01100110

    A 交 B           ->  00100100  = {c,f}        按位与
    A 并 B           ->  01100111  = {a,b,c,f,g}  按位或
    A 减 B           ->  00000001  = {a}          A & ~B

原书例 10.1（0..15 之间的奇素数）：

    素数 {13,11,7,5,3,2}          0010100010101100
    奇数 {15,13,11,9,7,5,3,1}     1010101010101010
    按位与                        0010100010101000  =  {13,11,7,5,3}
""", '一次机器指令并行处理 32 或 64 个元素。**一次按位与，就完成了两个集合的求交**'),

    ('bullets', '位向量的代价与前提', [
        '**空间与全集大小成正比** —— 学号全集可能有十亿个值、实际只出现几十个，就很浪费',
        '- 这时散列表或树更合适',
        '**还要求先固定「元素到位号」的映射**',
        '- 若映射随运行改变，两个看起来同为 `00100101` 的位串可能代表不同集合，'
        '**按位运算就没有意义**',
        '第 11 章的位图索引会把同一思想扩展到数据库记录列',
    ]),

    ('section', '第 3 节', '10.3 散列方法', '散列函数、冲突解决、效率分析'),

    ('bullets', '10.3.1 散列函数：三条选择原则', [
        '散列函数 h 把关键码映到 `{0, 1, …, M−1}`。三条原则：',
        '- **① 运算尽可能简单；② 值域必须在散列表范围内；③ 尽可能使结点均匀分布**',
        '**除余法** `h(x) = x mod M` 几乎最简单，**但挑 M 值要格外小心**：',
        '- M 取偶数 → 偶数关键码映到偶数地址，分布不匀',
        '- M 取 `2^k` → `h(x)` 只是 x 的最右 k 个二进制位，**不依赖 x 的全部比特位**',
        '- **所以通常选一个质数作为 M** —— 这样 `x mod M` 依赖 x 的所有位',
        '其他方法：乘余取整（A 取黄金分割 0.618 最理想）、平方取中、数字分析、基数转换、折叠',
    ]),

    ('bullets', '10.3.2 开散列（拉链法 / 分离链接）', [
        '**开散列把发生冲突的关键码存储在散列表主表之外**：每个槽挂一条链',
        '- 插入是算地址再接到链上；查找、删除只在那一条链上走',
        '- **删掉一个结点不会影响别的链**，也不存在「探测序列被截断」的问题',
        '**装载因子 α = n/m 可以大于 1**，链只是变长',
        '- 成功查找的期望比较次数约 `1 + α/2`；失败查找要走完整条链，约 α',
        '实现简单，空间上每个结点多一个指针',
        '⚠️ 名字容易误解：「开」指**冲突元素移到槽外**，不是开放地址法',
    ]),

    ('bullets', '10.3.3 闭散列（开放地址法）：探查序列', [
        '所有记录**直接存储在散列表中**。关键码 K 有一个**基位置** `h(K)`',
        '冲突时按**探查序列** `d_i = (d₀ + p(K,i)) mod M` 依次找空位',
        '**检索时沿同一序列查；一旦遇到开放的空闲地址，就说明表中没有待查的关键码**',
        '- ⚠️ **这一句正是「删除为什么不能标空」的根据**',
        '四种探查函数：',
        '- **线性探查** `p(K,i) = i`；**二次探查** `1², -1², 2², -2², …`',
        '- **伪随机探查**；**双散列探查** `p(K,i) = i · h₂(K)`',
    ]),

    ('bullets', '线性探查的毛病叫「聚集」', [
        '**散列基地址不同的结点，可能争夺相同的后继散列地址** —— 这叫**基本聚集**',
        '理想情况下每个空槽都该有相同机会接收下一个记录；',
        '- 但原书例 10.7 那张表里，下一条记录落在第 11 槽的概率是 `2/15`，'
        '落到第 7 槽却是 `11/15`',
        '- **小的聚集可能汇合成大的聚集，导致很长的探查序列**',
        '改进：每次跳 c 个槽。**但 c 必须与 M 互素**，否则走不遍所有槽',
        '- c=2 且槽数为偶数时，基位置在偶数槽的关键码**只能走遍所有偶数槽**',
        '- 而且仍不能解决聚集：`h=3` 与 `h=5` 的探查序列 3,5,7,9… 和 5,7,9… **仍然纠缠**',
        '**双散列**让探查序列成为**关键码的函数**而不是基位置的函数，才真正岔开',
    ]),

    ('table', '删除是闭散列的难点：为什么要墓碑（表长 5，键 1、6、11 同址）', [
        ['操作', '槽 0', '槽 1', '槽 2', '槽 3', '槽 4', '说明'],
        ['插入 1', '空', '**1**', '空', '空', '空', '`home(1)=1`'],
        ['插入 6', '空', '1', '**6**', '空', '空', '1 冲突，探测到 2'],
        ['插入 11', '空', '1', '6', '**11**', '空', '连续冲突，探测到 3'],
        ['删除 1', '空', '**墓碑**', '6', '11', '空', '**探测链不能截断**'],
        ['查找 11', '空', '墓碑', '6', '11', '**命中**', '越过墓碑，在 3 命中'],
        ['插入 16', '空', '**16**', '6', '11', '空', '记住首个墓碑，**确认无重复后**才复用'],
    ], '最后一步不能一看到墓碑就立刻插入 —— 万一同一个 16 已在更后面的槽，提前写入就制造了重复键'),

    ('bullets', '10.3.5 效率分析：衡量的是什么', [
        '性能按「完成一次插入、删除或检索所需的**记录访问次数**」衡量',
        '**插入和删除都基于检索**：',
        '- **删除前必须先找到它** → 删除的访问数 = **成功检索**的访问数',
        '- **插入必须找到探查序列的尾部**（才能确定没有重复记录）→ = **不成功检索**',
        '所以**散列表的效率问题实质上还是平均检索长度问题**，而且要区别对待成功与不成功',
        '期望代价与**负载因子 `α = N/M`** 有关：',
        '- 插入 / 不成功检索的探查次数期望 `1 + Σ(N/M)^i = 1/(1−α)`',
        '- 成功检索对 0 到 α 积分：`(1/α)·ln(1/(1−α))`',
    ]),

    ('table', '三种策略放在一起比（原书表 10.2）', [
        ['冲突解决策略', '成功检索（删除）', '不成功检索（插入）'],
        ['**开散列法（拉链）**', '`1 + α/2`', '`α + e^(−α)`'],
        ['双散列探查法', '`(1/α)·ln(1/(1−α))`', '`1/(1−α)`'],
        ['线性探查法', '`(1 + 1/(1−α)) / 2`', '`(1 + 1/(1−α)²) / 2`'],
    ], '**开散列方法的效率最高，实际系统中使用的散列大多都是开散列** —— 简单、不产生聚集、删除方便'),

    ('bullets', '0.5 是一条经验阈值', [
        '**α < 0.5（散列表将近半满）时，大部分情况下检索长度小于 2**',
        '**实际经验表明：负载因子超过 0.5，散列表的性能就会急剧下降**',
        '所以要**先知道最大负载下表中可能有多少条记录**，据此选表长',
        '动态性强（大量插删）时性能会下降。两种应对：',
        '- 超过阈值时**重新散列整个表**；或者周期性重散列',
        '- **并把记录按访问频率从高到低插入** —— '
        '让上一周期中被频繁访问的记录更有机会接近基位置',
        '**为什么教材用大篇幅讲闭散列**：它需要考虑的因素更多，因而更需要精心设计；'
        '在不能动态分配的受限系统里有独到用途，精心设计后效率比开散列**更稳定**',
    ]),

    ('bullets', '本章小结', [
        '**顺序检索**限制最少（不要求有序）、效率最低，`ASL = (n+1)/2`',
        '**二分检索**效率最高 `O(log n)`，但要求有序 + 随机访问 —— '
        '适合「一经建立就很少改动」的表',
        '**分块检索**介于两者之间；`s = √n` 时 `ASL` 最小；**优点在插入删除上**',
        '**集合**只关心成员关系；全集固定且不大时用**位向量**，交并差变成按位运算',
        '**散列**不比较，直接算地址。冲突解决分**开散列**（链）与**闭散列**（探查序列）',
        '- 闭散列的删除必须用**墓碑**，否则探测链被截断',
        '- **平均检索长度不依赖 n，只依赖负载因子 α** —— 这是散列法最重要的特征',
    ]),

    ('bullets', '习题选讲', [
        '**二分**　写出查某个值的每一轮区间；说明**半开区间写法**为什么能避免下溢',
        '**分块**　推导 `s = √n` 时 ASL 最小；举例说明「块内无序」意味着什么',
        '**位向量**　用位运算实现交、并、差，并验证 `A ∩ B = B ∩ A` 等代数性质',
        '**散列函数**　为什么除余法的 M 通常取质数？M 取 `2^k` 会怎样',
        '**聚集**　举例说明 c=2、M 为偶数时探查序列走不遍全表',
        '**墓碑**　构造一个「删除后标空 → 后续查找误判不存在」的最小例子',
        '**上机**　把 `IntSet` 的线性表换成散列表，**接口不变**，比较交集运算的耗时',
        '**这一页和讲义里都是选讲。** 原书全部 **345 道**习题与上机题的参考答案，在 `book/习题与参考答案.md`',
    ]),
]
