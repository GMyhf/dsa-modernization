# -*- coding: utf-8 -*-
"""第1章 概论 —— 课件内容。

素材出自 book/ch01-adt.md（1.1–1.4 + 本章小结 + 习题）。
代码页里的 C++ 片段全部摘自 code/ch01/adt/，未作改写。
"""

FIG = '../book/assets/scan/'

META = {
    'title': '第1章　概论',
    'subtitle': '问题求解 · 逻辑结构与存储结构 · 抽象数据类型 · 渐进分析',
    'footer': '数据结构与算法 · 第1章 概论',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写，全部可编译、可运行'],
}

SLIDES = [

    ('bullets', '本章要回答四个问题', [
        '**一个现实问题怎么变成能写的程序？**',
        '- 1.1 股市传言 —— 抽象成图，选 Floyd，写成 100 行可运行代码',
        '**数据之间的关系怎么描述、又怎么落到内存里？**',
        '- 1.2 逻辑结构 `(K, r)`；顺序 / 链接 / 索引 / 散列四种存储映射',
        '**「有哪些运算」和「运算怎么实现」怎么分开？**',
        '- 1.2.3 抽象数据类型 ADT `(D, R, P)`',
        '**两个算法谁更快，凭什么下这个结论？**',
        '- 1.3 算法的性质与六种设计方法；1.4 渐进分析 O / Ω / Θ',
    ]),

    ('key', '本章的一句话',
     '程序 = 数据结构 + 算法（Wirth）。结构选错了，算法再巧也补不回来。'),

    ('section', '第 1 节', '1.1 问题求解', '从股市的传言到一个可以跑起来的程序'),

    ('bullets', '1.1.1 问题描述：股市的传言', [
        '若干股票经纪人之间互相传消息。**谁能直接传给谁、要花多久，都是已知的。**',
        '问：**从哪一个人开始散布消息，能让所有人最快都知道？**',
        '注意题面不是“出边最多的人”——',
        '- 要的是让**最后一个**收到消息的人也尽可能早收到',
        '五个经纪人 B1…B5，共 10 条**有向**边；方向和权都重要：',
        '- B1→B5 要 3，B5→B1 要 5 —— 同一对人，两个方向不一样',
    ]),

    ('image', '图 1.1　经纪人间的消息传递图', FIG + 'fig-1-1.png',
     '顶点是人，有向边 <Bi, Bj> 表示 i 能把消息直接传给 j，边上的数是所需时间'),

    ('table', '1.1.2 抽象：顶点 + 有向带权边', [
        ['起点', '出边（终点 / 耗时）'],
        ['B1', '→B2 (8)　→B4 (4)　→B5 (3)'],
        ['B2', '→B5 (8)'],
        ['**B3**', '→B1 (6)　→B2 (7)　→B4 (10)　→B5 (2)'],
        ['B4', '（没有出边）'],
        ['B5', '→B1 (5)　→B2 (5)'],
    ], 'B3 是唯一能到达其余所有人的起点 —— 但“能到达”还不等于“最快”'),

    ('table', '图 1.2　相邻矩阵：这就是算法的输入', [
        ['', 'B1', 'B2', 'B3', 'B4', 'B5'],
        ['**B1**', '0', '8', '∞', '4', '3'],
        ['**B2**', '∞', '0', '∞', '∞', '8'],
        ['**B3**', '6', '7', '0', '10', '2'],
        ['**B4**', '∞', '∞', '∞', '0', '∞'],
        ['**B5**', '5', '5', '∞', '∞', '0'],
    ], '第 i 行第 j 列是边 <Bi, Bj> 的权；没有边记 ∞，对角线是 0'),

    ('bullets', '解法拆成三步', [
        '**① Floyd**：先算出任意两人之间的最短传播时间',
        '**② 每行取最大**：消息要传遍所有人，就得等**最慢**的那个人收到',
        '**③ 各行的最大值里取最小**：这个起点就是答案',
        '某一行还留着 `∞` → 有人永远收不到 → 这个起点不合格',
        '对照原书算法 1.1 的记号：',
        '- `D` 就是代码里的 `shortest` 矩阵；`max[i]` 是每行的 `largest_distance`',
        '- `pos` 是 `best_source()` 的返回值。下标从 0 起，所以 B3 返回 2',
    ]),

    ('key', '为什么取“最慢的一项”，而不是和、也不是最小值',
     '从 B3 出发：到 B1 是 6、到 B2 是 7、到 B4 是 10、到 B5 是 2。'
     '消息要传遍所有人，就必须等最慢的 B4 —— B3 的完成时间是 10。'),

    ('table', '把每个人都当一次起点（图 1.3 的结果矩阵）', [
        ['起点', '到 B1', '到 B2', '到 B3', '到 B4', '到 B5', '最慢的最短时间', '可选'],
        ['B1', '0', '8', '∞', '4', '3', '∞', '否'],
        ['B2', '13', '0', '∞', '17', '8', '∞', '否'],
        ['**B3**', '6', '7', '0', '10', '2', '**10**', '**是**'],
        ['B4', '∞', '∞', '∞', '0', '∞', '∞', '否'],
        ['B5', '5', '5', '∞', '9', '0', '∞', '否'],
    ], '答案是 B3。这类目标叫「最小化最大距离」，后面第 7 章还会再遇到'),

    ('code', '1.1.3 用起来：demo.cpp', '''#include "modern.hpp"

#include <iostream>

int main() {
    // B1 ... B5 对应下标 0 ... 4。
    dsa::adt::RumorNetwork network(5);
    network.add_route(0, 3, 4);   // B1 -> B4，耗时 4
    network.add_route(0, 4, 3);   // B1 -> B5，耗时 3
    network.add_route(0, 1, 8);   // B1 -> B2，耗时 8
    network.add_route(1, 4, 8);   // B2 -> B5，耗时 8
    network.add_route(2, 0, 6);   // B3 -> B1，耗时 6
    network.add_route(2, 1, 7);   // B3 -> B2，耗时 7
    network.add_route(2, 3, 10);  // B3 -> B4，耗时 10
    network.add_route(2, 4, 2);   // B3 -> B5，耗时 2
    network.add_route(4, 0, 5);   // B5 -> B1，耗时 5
    network.add_route(4, 1, 5);   // B5 -> B2，耗时 5

    const auto source = network.best_source();
    if (source) {
        std::cout << "最佳传播起点是 B" << *source + 1 << '\\n';
    } else {
        std::cout << "不存在能到达全部经纪人的起点\\n";
    }
}''', 'demo.cpp 只管输入输出；数据和计算全在 modern.hpp —— 展示与计算分开'),

    ('code', '模型：RumorNetwork 的对外接口', '''class RumorNetwork {
public:
    // 取 int 上限的 1/4，给 a + b 留余量：用 max() 相加就是整数溢出
    static constexpr int infinity = std::numeric_limits<int>::max() / 4;

    explicit RumorNetwork(std::size_t people);                    // 建 n x n 距离矩阵
    void add_route(std::size_t from, std::size_t to, int cost);   // 加一条有向边
    [[nodiscard]] std::optional<std::size_t> best_source() const; // 算答案

private:
    std::vector<std::vector<int>> distance_;    // 相邻矩阵；末尾下划线 = 私有成员
};''', '这一屏就是这个 ADT 的全部：三个运算 + 一份内部表示。调用者不该关心后者'),

    ('code', 'Floyd：三重循环到底在循环什么', '''auto shortest = distance_;          // 复制一份，多次调用不改动原网络

for (std::size_t via = 0; via < shortest.size(); ++via) {          // 允许经过 via
    for (std::size_t from = 0; from < shortest.size(); ++from) {   // 固定起点
        for (std::size_t to = 0; to < shortest.size(); ++to) {     // 固定终点
            if (shortest[from][via] != infinity &&                 // 两段都可达
                shortest[via][to] != infinity &&
                shortest[from][to] > shortest[from][via] + shortest[via][to]) {
                shortest[from][to] = shortest[from][via] + shortest[via][to];
            }
        }
    }
}''', 'B2→B4 没有直接边，但 B2→B5 (8) + B5→B1 (5) + B1→B4 (4) = 17'),

    ('code', '选答案：每行取最大，再在其中取最小', '''std::optional<std::size_t> result;          // 空 = 还没找到合格的起点
int smallest_eccentricity = infinity;

for (std::size_t from = 0; from < shortest.size(); ++from) {
    int largest_distance = 0;
    for (int distance : shortest[from]) {   // 这一行的最大值 = 该起点的完成时间
        largest_distance = distance > largest_distance ? distance : largest_distance;
    }
    if (largest_distance < smallest_eccentricity) {
        smallest_eccentricity = largest_distance;
        result = from;
    }
}
return result;                              // 全都不可达时返回 std::nullopt''',
     'eccentricity（离心率）= 某点到所有点的最短距离中的最大值。时间 O(V³)，空间 O(V²)'),

    ('two', '「可能没有答案」怎么返回',
     '原书 / 传统写法', [
         '`bool find(size_t& out);`',
         '答案和“有没有答案”走两条通道',
         '忘了看返回值 → 读到未初始化的 out',
         '容器里直接 cout 打中文提示',
         '于是这段逻辑没法被自动测试',
     ],
     '本书写法（风格公约 D-001）', [
         '`optional<size_t> best_source();`',
         '一个“可能装着下标的盒子”',
         '取值前必须先判空，漏不掉',
         '容器内零 I/O，真错误抛标准异常',
         '`[[nodiscard]]`：返回值丢不掉',
     ]),

    ('code', '编译并运行（在仓库根目录）', '''$ c++ -std=c++17 -Wall -Wextra -Werror -Icode/ch01/adt \\
      code/ch01/adt/demo.cpp -o /tmp/rumor-demo
$ /tmp/rumor-demo
最佳传播起点是 B3

# 试着删掉 B3 的一条出边：network.add_route(2, 1, 7);
# 于是没有任何起点能覆盖全部顶点，best_source() 返回空值：
不存在能到达全部经纪人的起点''',
     '-Werror 把警告当错误；刚开始调试时可以先去掉它'),

    ('section', '第 2 节', '1.2 数据结构', '逻辑结构 · 存储结构 · 定义在其上的运算'),

    ('key', '数据结构是三件事，必须分开看',
     '逻辑结构（关系是什么）+ 存储结构（关系怎么落到内存里）+ 运算。'
     '后面十一章，都是在这三层之间做选择。'),

    ('bullets', '1.2.1 逻辑结构：二元组 B = (K, R)', [
        '`K` —— 结点的**有穷集合**。一个结点就是一个数据单位，类型任意',
        '- 数据结构课只关心结点之间的关系，所以通常把结点当黑盒',
        '`R` —— 定义在 `K` 上的一组二元关系。本书里 `R` 一般只含一个关系 `r`，记作 `(K, r)`',
        '`<k, k′> ∈ r`：`k` 是**前驱**，`k′` 是**后继**',
        '- 无前驱的叫**开始结点**，无后继的叫**终止结点**',
        '例：教学计划。每门必修课是一个结点，`<程序设计, 数据结构>` 表示必须先修',
        '**逻辑结构只谈关系，不谈占几个字节。**',
    ]),

    ('table', '按关系 r 的性质，逻辑结构分三类', [
        ['结构', '前驱', '后继', '本书在哪几章'],
        ['**线性结构**', '至多 1 个', '至多 1 个', '线性表、栈、队列、串（第 2–4 章）'],
        ['**树形结构**', '根无前驱，其余恰好 1 个', '**不限**', '二叉树、树、B 树（第 5、6、11 章）'],
        ['**图结构**', '**不限**', '**不限**', '最短路、最小生成树（第 7 章）'],
    ], '树 vs 图：结点是否只从属于一个直接前驱。线性 vs 树：结点是否只有一个直接后继'),

    ('bullets', '1.2.2 存储结构：四种基本映射方法', [
        '**顺序方法** —— 存进地址相邻的单元，逻辑顺序就是地址顺序。按下标访问 `O(1)`',
        '- 又叫**紧凑存储**：除数据本身没有附加信息，用**存储密度**度量',
        '- 完全二叉树也能顺序存：孩子下标 `2i+1`、`2i+2` 算得出来（第 5 章）',
        '**链接方法** —— 每个结点附指针域存后继地址。增删方便；按位置访问不再是 `O(1)`',
        '**索引方法** —— 在结点空间**之外**另建一张表 `Y : Z → D`，把索引值映射到存储地址',
        '- 主要作用是减少读写磁盘的数据量（第 11 章）',
        '**散列方法** —— 索引法的延伸：不查表，用散列函数把关键码直接**算**成地址（第 10 章）',
    ]),

    ('ascii', '索引：上下两行的错位就是它的全部意义', r"""
存储区的数据 —— 物理无序，而且每块长度不等
+----------------+----------+------------+--------------+--------+
| [1] 73         | [2] 52   | [3] 42     | [4] 98       | [5] 37 |
+----------------+----------+------------+--------------+--------+

线性索引 —— 附加在数据之外的一张小表
+------+------+------+------+------+
|  37  |  42  |  52  |  73  |  98  |   关键码：升序  -> 逻辑有序
+------+------+------+------+------+
| [5]  | [3]  | [2]  | [1]  | [4]  |   指向第几块：乱序 -> 物理没搬过
+------+------+------+------+------+
""", '找 52：在索引里二分到第 3 列，读出 [2]，直接取第 2 块 —— 只读了一块（图 1.4）'),

    ('bullets', '为什么索引函数不是数组那样的线性函数', [
        '看一眼上一页每块的宽度：**五块长度各不相同**',
        '正因为长度不等，“第 k 块在哪”就没法用 `起始地址 + k × 块长` 算出来 ——',
        '- **只能查表**。这才是原书那句“索引函数并非简单线性函数”的意思',
        '上面一行升序，所以可以在索引里**二分**；下面一行完全乱序，说明**数据一块都没搬过**',
        '代价是索引表本身的空间 —— 这正是 1.4.3 要讲的**时空折衷**',
    ]),

    ('bullets', '1.2.3 抽象数据类型 ADT', [
        'ADT = **定义了一组操作的抽象模型**。数据和操作封在一起，外部只能通过操作访问',
        '一般写成三元组 `(D, R, P)`：数据对象 `D`、数据关系 `R`、基本操作 `P`',
        '- `D`、`R` 给出数据模型，是对**数据**的抽象；`P` 是对**操作**的抽象',
        '于是讨论一种结构时可以先不问它是数组还是链表，只问：',
        '- **它提供哪些运算？这些运算有什么约定？**',
        '整数连同 `+ - × ÷`，就是一个最简单的 ADT',
        'C++ 用类（类模板）的**声明**表示 ADT，用类的**定义**实现它',
    ]),

    ('code', 'ADT 的写法，与原书【代码1.2】的空模板', '''ADT 名字 {
    数据对象 D：结点是什么
    数据关系 R：结点之间有什么关系
    基本操作 P：对外提供哪些运算
}

// 原书【代码1.2】：先把接口写出来，再谈实现
template <class Type>
class ClassName {
private:
    Type data;          // 取值类型和存储 —— 实现细节，外部看不到
public:
    void method();      // 对外运算 —— 这一行才是 ADT 的内容
};''', '概念层的 ADT 说“问题需要什么运算”；实现层的类说“这些运算在内存里怎么做”'),

    ('bullets', '本书的一处改动：不写抽象基类', [
        '原书先写一个纯声明的抽象基类 `List<T>`，再让 `arrList` 继承它。**本书不这么做：**',
        '**它给不了多态。** 各章的容器都是**模板**，元素类型编译期就定了，彼此不需要互相替换',
        '- 那个基类从头到尾没有一处用到',
        '**它还带一个陷阱。** 基类析构函数不是 `virtual` 时，',
        '- `List<int>* p = new ArrayList<int>; delete p;` 是**未定义行为**',
        '- 常见后果：只析构了基类那层，派生类 `new` 的缓冲区没人释放 → 内存泄漏；也可能直接崩',
        '**「未定义行为」= 标准不再对程序行为作任何承诺**，而不是“结果不确定但还能用”',
    ]),

    ('section', '第 3 节', '1.3 算法', '概念、四条性质与六种设计方法'),

    ('bullets', '1.3.1 算法的四条性质', [
        '**通用性** —— 对**任意**符合输入类型的数据都成立，不是只对书上那组样例',
        '**有效性** —— 由有限条能被确切执行的指令组成，每步结果的类型可预期',
        '**确定性** —— 每执行一步之后，下一步怎么走有明确指示；不能含糊，也不能被锁住',
        '**有穷性** —— 必须在有限步内结束，不能含死循环',
        '- ⚠️ **指令条数有限 ≠ 执行有穷**。循环条件写错，有限条指令照样能转个没完',
        '- 所以设计算法时要专门盯住**结束条件**',
        '程序是算法的一种实现：计算机按程序逐步执行算法，完成求解',
    ]),

    ('table', '1.3.2 六种算法设计方法', [
        ['方法', '要点', '本书在哪儿用'],
        ['**穷举法**', '把问题空间里的对象一一列举、逐个检验；范围要有限、要有列举规则',
         '`switch` 就是一种穷举'],
        ['**回溯法**', '逐一枚举候选解，走不通就退回上一步换一个（回溯 / 向前试探）',
         '深度优先搜索（第 5–7 章）、背包（第 3 章）'],
        ['**分治 + 递归**', '大问题切成同形的小问题，各个击破再合并；**自顶向下**',
         '快排、归并（第 8 章），二分检索（第 10 章）'],
        ['**贪心法**', '每步按某个贪心标准取局部最优，寄望于拼出全局最优',
         'Prim、Kruskal、Dijkstra（第 7 章）'],
        ['**动态规划**', '子问题**不独立**：把已解的答案记进一张表，避免重复计算',
         'Floyd（第 7 章）、最佳二叉搜索树（第 12 章）'],
    ], '1.1 节要的是任意两点间的最短路，所以选 Floyd，而不是对每个起点各跑一遍 Dijkstra'),

    ('section', '第 4 节', '1.4 算法分析', '渐进分析 · 最佳 / 最差 / 平均 · 时空折衷'),

    ('bullets', '为什么不能拿秒来衡量算法', [
        '同一个算法，Cray 巨型机上跑和 PC 上跑差很多',
        '- 反过来，差算法在 Cray 上也可能比好算法在 PC 上更快',
        '同一台机器，编译型语言比解释型快很多 —— 原书举例：C 比同算法的 LISP 快近 20 倍',
        '**所以衡量的不是时间，而是「输入规模 n」与「基本操作次数」之间的关系。**',
        '- **规模**：输入量的数目。排序问题里就是待排元素的个数',
        '- **基本操作**：完成它所需的时间**与被操作的具体数值无关**，例如两个整数相加',
        '`t = cn` 时规模翻 5 倍时间也翻 5 倍；`t = log₂n` 时规模翻倍只多花 1 个单位',
    ]),

    ('table', '1.4.1 渐进分析：只留增长最快的那一项', [
        ['n', 'n²', '100n', 'log₁₀n + 1000', '谁说了算'],
        ['1', '1', '100', '1000', '常数项贡献最大'],
        ['10', '100', '1000', '1001', '100n 与常数项持平'],
        ['100', '10 000', '10 000', '1002', '前两项持平'],
        ['1000', '**1 000 000**', '100 000', '1003', '**n² 一家独大**'],
    ], 'f(n) = n² + 100n + log₁₀n + 1000。⚠️ 但 n 很小时系数和常数项举足轻重 —— '
       '排上万个数的算法未必适合排 10 个数'),

    ('bullets', '大 O 表示法', [
        '**定义 1**　若存在正数 `c` 和 `N`，使任意 `n ≥ N` 都有 `f(n) ≤ c·g(n)`，'
        '则称 `f(n)` 是 `O(g(n))` 的',
        '它说的是**上限**：`f` 的增长最终**至多**趋同于 `g` —— 即“这个算法最多差到什么程度”',
        '上限并不唯一：在 `O(n)` 里的函数也在 `O(n²)`、`O(n³)` 里',
        '- **大 O 取的是所有上限中最小的那一个**',
        '常用性质：`an^k` 是 `O(n^k)`；`f = c·g` ⇒ `f` 是 `O(g)`；`O` 可传递、可相加',
        '任何底数的对数增长率相同：`log_a n` 是 `O(log_b n)`。本书把 `log₂n` 简写成 `log n`',
    ]),

    ('table', '常见增长率：n 稍大就是天文数字', [
        ['阶 g(n)', '名称', 'n = 16', 'n = 256', '长这样的算法'],
        ['1', '常数', '1', '1', '不依赖数据规模'],
        ['log n', '对数', '4', '8', '二分'],
        ['n', '线性', '16', '256', '扫一遍'],
        ['n log n', '介于一、二阶之间', '64', '2048', '快排 / 归并，常出现在树形结构里'],
        ['n²', '二阶', '256', '65 536', '双重循环'],
        ['2ⁿ', '指数', '65 536', '—', '穷举子集，递归定义的函数'],
    ], 'n=1000、单位 1μs：n 是 1ms，n² 是 1s，n³ 约 16 分钟，2ⁿ 约 10²⁸⁶ 年 —— '
       '地球年龄还不到 10¹⁰ 年'),

    ('image', '图 1.5　常用函数的增长趋势', FIG + 'fig-1-5.png',
     '指数增长比任何高次多项式（n³、n⁴）都快 —— 指数爆炸型算法，用之前先算一算'),

    ('bullets', 'Ω 与 Θ：下限，以及上下限同阶', [
        '**Ω（欧米伽）** —— 存在 `c`、`N`，使 `n ≥ N` 时 `f(n) ≥ c·g(n)`',
        '- 这是**下限**：`f` 的增长最终**至少**趋同于 `g`',
        '- 与大 O 唯一的区别是不等号方向。大 O 取所有上限中**最小**的，Ω 取所有下限中**最大**的',
        '**Θ（希塔）** —— 既是 `O(g(n))` 又是 `Ω(g(n))`，即 `c₁g(n) ≤ f(n) ≤ c₂g(n)`',
        '- 例：`f(n) = 100n² + 5n + 500`，取 `c₁ = 100`、`c₂ = 105`、`N = 10`',
        '- 于是 `f(n)` 是 `Θ(n²)` —— 这是比“是 O(n²)”更强的一句话',
    ]),

    ('code', '数一数赋值操作（一）：O(n)', '''for (i = sum = 0; i < n; i++)    // 循环开始前 2 次赋值：i、sum
    sum += a[i];                 // 每轮 2 次赋值：sum、i

// 合计 2 + 2n 次赋值  ->  O(n)''',
     '主要开销落在赋值上，所以就数赋值的次数'),

    ('code', '数一数赋值操作（二）：嵌套循环 O(n²)', '''for (i = 0; i < n; i++) {                        // 循环前 1 次：i
    for (j = 1, sum = a[0]; j <= i; j++)         // 每轮外层 3 次：i、j、sum
        sum += a[j];                             // 内层每轮 2 次，共执行 i 次
    cout << "sum for subarray 0 through " << i << " is " << sum << endl;
}

//  1 + 3n + Σ(i=1..n-1) 2i
//    = 1 + 3n + 2(1 + 2 + ... + n-1)
//    = 1 + 3n + n(n-1)
//    = O(n) + O(n²)  ->  O(n²)''',
     '依次求出所有前缀子数组之和'),

    ('code', '数一数赋值操作（三）：嵌套 ≠ 平方', '''for (i = 4; i < n; i++)                           // 外层 n - 4 次
    for (j = i - 3, sum = a[i - 4]; j <= i; j++)  // 内层恒定 4 次，与 i 无关
        sum += a[j];                              // 每个 i 合计 8 次赋值

// O(1) + 8(n - 4)  ->  O(n)''',
     '只对每个子数组的前 5 个元素求和。**看的是内层跑几次，不是有几层循环**'),

    ('table', '1.4.2 最佳 / 最差 / 平均：在数组里顺序检索一个 K', [
        ['情况', 'K 在哪儿', '比较次数'],
        ['**最佳**', '第 1 个元素', '1'],
        ['**最差**', '最后一个元素', 'n'],
        ['**平均**（每个位置等概率 1/n）', '任意位置', '(1+2+…+n)/n = **(n+1)/2**'],
        ['**平均**（第 1 位 1/2、第 2 位 1/4、其余均分）', '偏向前面',
         '1 + (n+3)/8，**快近 4 倍**'],
    ], 'C_avg = Σ p(inputᵢ)·steps(inputᵢ)。平均情况分析并不总是可行 —— 它要求你知道输入的概率分布'),

    ('bullets', '那该看哪一种情况？', [
        '**最佳情况**发生的概率太小，代表不了算法性能；但能说明这个算法在什么场合适用',
        '**最差情况**告诉你“这个算法至少能做多快”—— 实时系统里这一条最要紧',
        '- 空运处理系统里，一个绝大多数时候能管 n 架飞机、但偶尔超时的算法是**不可接受的**',
        '多数算法的最坏与平均，往往只差一个常数因子或常数项',
        '本书后面每讲一个算法，都会分别交代三种情况：',
        '- 例如第 8 章的快速排序平均是 `O(n log n)`，输入已经有序时退化成 `O(n²)`',
    ]),

    ('key', '算法分析的第一条纪律',
     '只报一个数字、不说是针对哪一种输入，这个数字就没有意义。'),

    ('bullets', '1.4.3 时间和空间的折衷', [
        '**静态数据结构** —— 规模一旦定下，占用空间就不变；空间好估',
        '- 本书对这类算法一般只讨论时间代价',
        '**动态数据结构** —— 运行中空间会数量级地涨落，必须单独分析',
        '**时空折衷**：为改善时间开销，往往要以增大空间开销为代价；反之亦然',
        '- **空间换时间**：黄页查电话另建一张散列索引表 —— 时间从线性降到常数，空间多出线性一份',
        '- **时间换空间**：第 6.3 节把树压成「带右链」序列，省下全部指针域，代价是恢复结构要重扫一遍',
        '**没有免费的加速**：空间紧时间松、和空间松时间紧，选出来的结构会不一样',
    ]),

    ('bullets', '1.4.4 选数据结构的四条原则', [
        '先分析**问题本身**：涉及哪些数据类型、数据之间是什么逻辑关系',
        '数据结构的初步设计通常**先于**算法设计 —— 结构定了，算法往往水到渠成',
        '注意**可扩展性**：输入规模变了、问题演变了，这个结构还撑得住吗',
        '比较时空开销的优劣，具体情况具体分析',
        '回到本章的例子：5 个顶点、要的是任意两点最短路 → **相邻矩阵 + Floyd**',
        '- 顶点多、边少、只问单源 → 邻接表 + Dijkstra（第 7 章）',
        '- 数据在外存 → 一次磁盘 I/O 比一次比较贵几个数量级（第 9、11 章）',
    ]),

    ('bullets', '本章小结', [
        '用计算机求解问题 = **问题抽象 → 数据抽象 → 算法抽象 → 编码实现**',
        '**数据结构** = 逻辑结构 + 存储结构 + 运算',
        '- 逻辑结构 `(K, r)` 按关系性质分**线性 / 树形 / 图**三类',
        '- 存储映射四法：**顺序、链接、索引、散列**',
        '**ADT** 把“需要哪些运算”和“运算怎么实现”分开，达到数据抽象与信息隐蔽',
        '**算法**四性质：通用性、有效性、确定性、有穷性；设计方法六种',
        '**算法分析**不看秒表，看基本操作次数随 n 怎么长：用 `O` / `Ω` / `Θ` 说话，'
        '并分清最好、最坏与平均',
    ]),

    ('bullets', '习题（完整题面见同名讲义）', [
        '**计数**　`for(i=1;i<n;++i) for(j=n;j>=i;--j) a[i][j]=1;` 的精确执行次数与量级',
        '**证明**　对非负渐进函数证明 `max(f(n), g(n)) = Θ(f(n) + g(n))`',
        '**递推**　求 `T(n) = 2T(⌊n/2⌋) + n` 的渐进阶，并用数学归纳法证明',
        '**判断**　已知 `f` 是 `O(g)`，问 `log₂f`、`2^f`、`f²` 是否分别是 '
        '`O(log₂g)`、`O(2^g)`、`O(g²)`',
        '**上机 1**　`f(m, n)` = 把 m 写成若干个不超过 n 的自然数之和的方案数（`f(5,3)=5`）',
        '**上机 2**　用 C++ 类定义「复数」ADT：3 个构造函数、取/改实部虚部、'
        '`+ - * /`、重载流输出',
    ]),
]
