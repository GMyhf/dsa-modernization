# -*- coding: utf-8 -*-
"""第6章 树 —— 课件内容。素材出自 book/ch06-tree.md，代码摘自 code/ch06/general_tree。"""

FIG = '../book/assets/scan/'

META = {
    'title': '第6章　树',
    'subtitle': '一般树与森林 · 与二叉树的等价转换 · 左子右兄 · 并查集',
    'footer': '数据结构与算法 · 第6章 树',
    'info': ['《数据结构与算法》张铭、王腾蛟、赵海燕　高等教育出版社 2008',
             '现代化重制版讲义　代码按 C++17 重写；code/ 全部单元双档编译运行通过'],
}

SLIDES = [

    ('bullets', '本章要回答三个问题', [
        '**孩子个数不固定的树，怎么存？**',
        '- 6.2 四种链式表示，主实现是**左子 / 右兄二叉链表** —— 把任意度压成两个指针',
        '**为什么一般树没有「中序」？**',
        '- 6.1.4 一个结点可能有多于两棵子树，「根排在中间」无从定义',
        '**「这两个元素在不在同一组」怎么问得快？**',
        '- 6.2.5 并查集：父指针表示法的一个应用，两条改进让它接近常数',
    ]),

    ('key', '本章的一句话',
     '**树与二叉树、森林与二叉树之间存在一一对应的关系。** '
     '所以一般树的问题几乎都能搬到二叉树上去做 —— 这就是 6.1.2 那三步转换的价值。'),

    ('section', '第 1 节', '6.1 树的定义和基本术语', '定义、转换、周游'),

    ('bullets', '6.1.1 树的两种等价定义', [
        '**递归定义**：树是 n（n ≥ 1）个结点的有限集合，使得：',
        '- ① 有且仅有一个称为**根**的结点；',
        '- ② 除根以外的结点被分成 m 个**不相交**的有限集合，每一个又都是树 —— 称为根的**子树**',
        '**二元关系定义**：树是有穷集合 K 加上关系 r，满足：',
        '- 有且仅有一个结点没有前驱（根）；除根外每个结点**有且仅有一个前驱**；',
        '- 从根到任一结点都存在一条**路径**',
        '**除了定义的角度不同外，两种描述是等价的。**',
    ]),

    ('bullets', '术语与一个关键区分', [
        '术语基本沿用二叉树：父结点、子结点、边、兄弟、树叶、祖先、子孙、路径、层数',
        '- **结点的度**是它的子树数目；**树的度**是各结点度的最大值（二叉树的度是 2）',
        '- 根为第 0 层，非根结点的层数是其父结点的层数加 1',
        '子结点次序不加区别的叫**无序树**；按从左到右编号的叫**有序树**',
        '⚠️ **度为 2 的有序树并不是二叉树。**',
        '- 有序树中第一子结点被删除后，**第二子结点自然顶替成为第一子结点**',
        '- **度为 2 并且严格区分左右两个子结点的有序树才是二叉树** —— 它要能表示「左空右不空」',
        '**森林**是零棵或多棵不相交的树的（通常有序的）集合',
    ]),

    ('image', '图 6.1　树形表示法', FIG + 'fig-6-1.png',
     '树形表示其实是根在上的倒挂树。另有凹入表、文氏图、嵌套括号三种等价表示'),

    ('ascii', '6.1.2 森林与二叉树的等价转换：连线、切线、旋转', r"""
原树：A 的孩子是 B、C、D；B 的孩子是 E、F

  (1) 连线   把兄弟结点用线连起来
  (2) 切线   只保留父结点到**第一个**孩子的连线，砍掉到其余孩子的
  (3) 旋转   以根为轴顺时针转一下

转换之后：

     A                             A
   / | \          ==>             /
  B  C  D                        B
 / \                            / \
E   F                          E   C
                                \    \
                                 F    D

  左孩子 = 原树里的**第一个孩子**       左枝上是父子关系
  右孩子 = 原来的**下一个兄弟**         右枝上是兄弟关系
""", '单棵树的根没有兄弟，所以转成二叉树后**根的右孩子一定为空**'),

    ('bullets', '转换的形式定义，以及它是可逆的', [
        '森林 `F = {T₁, T₂, …, Tₙ}` 转成二叉树 `B(F)`：',
        '- F 空则 `B(F)` 空；否则 `B(F)` 的根是 T₁ 的根',
        '- `B(F)` 的**左子树**是 T₁ 的**子树森林**转成的二叉树',
        '- `B(F)` 的**右子树**是 `{T₂, …, Tₙ}` 转成的二叉树',
        '反过来是三步的逆：逆时针旋转 → 把右链上的结点补连到父 → 删掉到右孩子的边',
        '**这两种转换互逆**，所以树与二叉树、森林与二叉树是一一对应的',
        '于是：一般树的算法几乎都可以搬到二叉树上做',
    ]),

    ('bullets', '6.1.4 树的周游：为什么没有「中序」', [
        '**对于树或森林，一个结点可能具有多于两棵子树**',
        '- 「先左、访问根、再右」里的「中间」无从定义 —— **所以给不出中根次序**',
        '仿照二叉树的前序、后序，可以定义**先根次序**和**后根次序**：',
        '- **先根**：访问第一棵树的根 → 先根周游它的子树森林 → 先根周游其余的树',
        '- **后根**：后根周游第一棵树的子树森林 → 访问它的根 → 后根周游其余的树',
        '**广度优先**（层次周游）：从第 0 层开始自上而下，同层从左到右',
    ]),

    ('key', '一条值得单独记住的对应',
     '按**先根次序**周游森林 = 对应二叉树的**前序**序列；'
     '按**后根次序**周游森林 = 对应二叉树的**中序**序列。'),

    ('bullets', '为什么后根对上的是中序', [
        '理由就在转换关系里：',
        '- 第一棵树 T₁ 的诸子树对应 `B(F)` 的**左子树**',
        '- 其余的树 T₂…Tₙ 对应 `B(F)` 的**右子树**',
        '森林的后根次序是：先周游 T₁ 的子树森林 → 访问 T₁ 的根 → 周游其余的树',
        '- 翻译成二叉树就是：**左子树 → 根 → 右子树** —— 这正是中序',
        '以图 6.5 的森林为例：先根 `A B C E F D G H J I`，后根 `B E F C D A J H I G`',
        '⚠️ **森林的层次序列与其对应二叉树的层次次序不同** —— 这一条不能类推',
    ]),

    ('section', '第 2 节', '6.2 树的链式存储结构', '四种表示法，一条主线'),

    ('bullets', '为什么一般树的存储比二叉树麻烦', [
        '**要求存储结构不但能存结点本身的数据，还要能准确反映结点之间的逻辑关系**',
        '二叉树好办：每个结点固定两个指针域。一般树的**度不固定**，这就是全部难处',
        '四种链式表示法，本章的主实现是第四种：',
        '- **① 子结点表** —— 每个分支结点存一张孩子链表',
        '- **② 静态左子/右兄** —— 数组 + 下标代替指针，每结点四个域',
        '- **③ 动态表示法** —— 每结点单独分配一张长度可变的孩子指针表',
        '- **④ 动态左子/右兄二叉链表** —— 直接用第 5 章二叉树的链式存储',
    ]),

    ('table', '四种链式表示法的取舍', [
        ['表示法', '优点', '代价'],
        ['**子结点表**', '最左孩子可直接找到，顺链找到所有孩子',
         '度不固定时数组要扩容；中间插入要搬指针'],
        ['**静态左子/右兄**', '每结点存储空间**大小固定**；访问右兄弟方便；两树合并只改 3 个链接',
         '仍需一整块数组'],
        ['**动态表示法**', '子结点数变化时只需重新分配该结点的空间，**更灵活**',
         '本质同子结点表；结点分散'],
        ['**动态左子/右兄**', '**只要两个指针域**，直接复用二叉树的实现',
         '取「第 k 个孩子」要沿兄弟链走 k 步'],
    ], '「左子/右兄二叉链表是采用得最多的表示法」—— 因为它把「度不固定」这件事彻底消掉了'),

    ('image', '图 6.8　静态「左子/右兄」里的树归并', FIG + 'fig-6-8.png',
     '把以 A 为根的树变成 H 的最左子树：只需调整 **3 个链接**，其余结点的值和链接都不变'),

    ('section', '第 3 节', '6.2.5 父指针表示法与并查集', '两条改进，接近常数'),

    ('bullets', '等价类与并查集', [
        '**并查集**（union/find）：由若干互不相交子集组成的集合抽象。两个基本操作：',
        '- `find` 判断两个元素是否属于同一集合；`union` 把两个集合归并为一个',
        '集合 S 上的关系 R 是**等价关系** ⟺ 自反、对称、传递',
        '- 元素 x 的**等价类** `[x]ᴿ = {y ∈ S : (x,y) ∈ R}`',
        '- **所有等价类互不相交，它们的并正好是 S** —— 等价关系就是对 S 的一种划分',
        '用**父指针**表示：每个子集是一棵树，**树根就是该集合的标识符**',
        '- `find` 沿父链找到根；`union` 只需**把一棵树的根指向另一棵树的根**',
        '- **不必搬动集合中的所有元素** —— 这是父指针表示法在这里的全部价值',
    ]),

    ('bullets', '两条改进：要防的是树退化成一条链', [
        '**改进一：重量权衡合并规则**（weighted union rule）',
        '- 合并时比较两个集合的**元素个数**，让**含元素少的树根指向含元素多的根**',
        '- 每次合并树高最多加 1，而元素个数至少翻倍，所以深度最多增加 `log n` 次 → `O(log n)`',
        '⚠️ **别和「按秩合并」混了**：按秩比的是**树高**，按重量比的是**元素个数**',
        '- 两者复杂度同阶，但在同一组等价对上会长出**形状不同**的树',
        '**改进二：路径压缩**（path compression）',
        '- `find` 走过的路径上，把每个结点都直接挂到根下 —— 下次再问就是一步',
        '两条一起用，均摊代价接近常数（反 Ackermann 函数）',
    ]),

    ('section', '第 4 节', '6.3 / 6.4 顺序存储与 K 叉树', '用附加信息还原树形'),

    ('bullets', '6.3 顺序存储：把结点排进数组，靠附加信息还原', [
        '**链式存储**每个结点单独分配，指针占空间，也不利于整块读写',
        '**顺序存储**把结点按某一种周游次序排进数组，再用**很少的附加信息**还原树形',
        '基础是先根序列的一条性质：',
        '- **任何结点的子树的所有结点都直接跟在该结点之后**',
        '- **任何一个分支结点后面紧跟的是它的第一个子结点**',
        '原书给了四种：带右链的先根次序、带双标记的先根次序、'
        '**带度数的后根次序**、带双标记的层次次序',
    ]),

    ('bullets', '6.4 K 叉树', [
        '有些应用里每个结点的孩子数有**固定上限 K**：三子棋的博弈树、B 树的内存模拟',
        '**满 K 叉树**：每个结点要么是叶，要么恰好 K 个孩子',
        '**完全 K 叉树**：只有最下两层的度可以小于 K，且最下层靠左对齐',
        '按层从 0 编号时：结点 i 的孩子是 `Ki+1 … Ki+K`，父结点是 `⌊(i-1)/K⌋`',
        '- **K = 2 就回到二叉树** —— 第 5 章那组公式是这组的特例',
        '完全 K 叉树按编号存进数组，和完全二叉树是同一套办法',
        '计算机图形学里的 **4 叉树、8 叉树**就是常见的应用',
    ]),

    ('bullets', '本章小结', [
        '树有两种等价定义：**递归**（根 + 子树）与**二元关系**（K 和 r）',
        '**树与二叉树、森林与二叉树一一对应** —— 连线、切线、旋转，可逆',
        '**先根 = 对应二叉树的前序；后根 = 对应二叉树的中序**。'
        '⚠️ 层次序列**不**对应',
        '一般树没有中序，因为「多于两棵子树」时「中间」无从定义',
        '四种链式表示里，**左子/右兄二叉链表用得最多** —— 把任意度压成两个指针',
        '**父指针表示法**方便实现并查集；**重量权衡合并 + 路径压缩**让大量操作接近常数',
        '**顺序存储**靠周游次序 + 附加信息还原树形；K 叉树是二叉树公式的推广',
    ]),

    ('bullets', '习题选讲', [
        '**补充 1**　高度 h 的满 k 叉树按层编号：推导第 l 层结点数、结点 i 的第 m 个孩子编号',
        '**补充 2**　用并查集判断 `a==b` / `a!=b` 方程组是否有解，'
        '并分析**三种**优化下的复杂度',
        '**习题 1**　三棵树的森林 → 二叉树 → 再转回来，**验证互逆**',
        '**习题 2**　对 `A(B(E,F),C(G),D(H,I,J))` 写出先根、后根、层次序列',
        '**习题 3**　度为 2 的有序树和二叉树差在哪？举一个「左空右不空」的例子',
        '**习题 5**　依次 `unite(0,1)`、`unite(1,2)`、`unite(3,4)`，画父指针；'
        '再 `find(0)` 画路径压缩后的结果',
        '**习题 6**　完全 3 叉树按层编号，写出结点 4 的父和孩子们',
        '**这一页和讲义里都是选讲。** 原书全部 **345 道**习题与上机题的参考答案，在 `book/习题与参考答案.md`',
    ]),
]
