# 原书勘误

> 《数据结构与算法》（张铭、王腾蛟、赵海燕，高等教育出版社 2008）印刷清单里，
> **按原样进编译器会失败，或对拍后算法结果确定是错的**条目。
> 每条都指向某个单元的 `legacy.md`，那里有命令和真实输出。
>
> **不收录**：OCR 把 `}` 认成 `1`、把 `++` 拆成 `+ +`、全角分号——那是扫描损伤，
> 不是原书设计。也不收录「用了 `cout`」「没用 `optional`」这类现代化取舍。
>
> 底稿 `dsa_raw.md` 只读，本表是给对照纸书阅读的人用的。
>
> 本表每一条都在 `collab/errata.json` 里有编号，「跑起来是错的」那几条**各自对应一句
> 会变红的断言**——实现要是退回原书那种写法，测试当场失败。映射由 `tools/errata.py`
> 校验，已接进闸门；「已吸收」不再是一句需要人相信的话。

## 怎么读

「印刷即错」指：把 OCR 噪声修回之后，逻辑或语法仍然不能通过现代 C++ 编译器，
或与书中自己的约定对拍失败。现代实现写在 `code/`，书稿 `book/` 印的是那份能跑的代码。

## 编译不过

| 清单 | 错在哪 | 证据 |
| --- | --- | --- |
| 代码2.1 / 代码2.2 / 算法2.5 | 成员函数名叫 `delete`，这是关键字 | `error: expected unqualified-id before ‘delete’` · [array_list](../code/ch02/array_list/legacy.md) |
| 代码2.1 | `class List` 通篇没有 `public:`，ADT 的每个运算都是私有的 | `error: ‘void List<T>::clear()’ is private` · 同上 |
| 算法2.3 | 循环上界 `n` 从未声明 | `error: ‘n’ was not declared in this scope` · 同上 |
| 代码2.6 | `const Link*` 赋给 `Link* next`，丢弃 const | `error: assigning to 'Link<int> *' from 'const Link<int> *'` · [linked_list](../code/ch02/linked_list/legacy.md) |
| 代码3.2 | `int top` 与 `bool top(T&)` 重名，整类编译不过 | `error: ‘bool arrStack<T>::top(T&)’ conflicts with a previous declaration` · [array_stack](../code/ch03/array_stack/legacy.md) |
| 算法3.3 | 循环变量 `i` 从未声明 | `error: ‘i’ was not declared in this scope` · 同上 |
| 代码3.4 | 又是 `Link<T>* top` 与 `bool top(T&)` 重名 | 与代码3.2 同一处错误，两个存储结构都犯 · [linked_stack](../code/ch03/linked_stack/legacy.md) |
| 算法3.5 / 算法3.9 | `s.pop(&tmp)` 传指针，本书自己的栈 ADT 要的是引用 | `error: cannot convert ‘double*’ to ‘double&’` · [expression_eval](../code/ch03/expression_eval/legacy.md) |
| 算法3.11 | `enum rdType {0,1,2}`（枚举值不能是字面量）；`public class knapNode`（Java 语法，出现两次） | `error: expected identifier before numeric constant` / `error: expected unqualified-id before ‘public’` · [knapsack](../code/ch03/knapsack/legacy.md) |
| 算法3.12 | 同一个 `stack.top` 既当数据成员又当成员函数——**依赖代码3.2/3.4 的重名缺陷才可能存在** | [knapsack](../code/ch03/knapsack/legacy.md) |
| 算法4.4 | `assert(str != '\0')` 是指针与字符比较 | `error: ISO C++ forbids comparison between pointer and integer` · [string_class](../code/ch04/string_class/legacy.md) |
| 算法4.4 | `String(char* s)` 使书中自己的例子 `String s1 = "Hello"` 在 C++11 起非法 | 字符串字面量是 `const char*` · 同上 |
| 代码10.1 | 成员初始化写 `key`，数据成员却是 `Key` | `error: member initializer 'key' does not name a non-static data member` · [search_hash](../code/ch10/search_hash/legacy.md) |

## 能编译、跑起来是错的

| 清单 | 错在哪 | 证据 |
| --- | --- | --- |
| 算法3.6 / 3.8 / 3.9 | 阶乘不查溢出：`factorial(21)` 返回负数，`factorial(66)` 返回 0 | UBSan：`signed integer overflow: 21 * 2432902008176640000` · [recursion_and_stack](../code/ch03/recursion_and_stack/legacy.md) |
| 算法3.6 | 负数静默返回 1 | 定义域错误，不是「空阶乘」 · 同上 |
| 算法4.5 | 越界 `return NULL`，随后 `strlen(nullptr)` | 运行期 SEGV · [string_class](../code/ch04/string_class/legacy.md) |
| 算法4.6 / 算法4.8 | 0 起始下标下返回 `j - pLen + 1`，**一律差 1** | 四面数据对拍 `std::string::find`，含图4.12 自己的例子 · [pattern_matching](../code/ch04/pattern_matching/legacy.md) |
| 算法10.9 | 散列表析构写 `delete HT`，`HT` 是数组 | 应为 `delete[]` · [search_hash](../code/ch10/search_hash/legacy.md) |

## 书内自相矛盾

| 位置 | 矛盾 | 说明 |
| --- | --- | --- |
| 算法3.5 正文 vs 代码 | 正文自己说 `operand1 == 0.0` 判断除零是错的，印出来的代码照旧 | 见第 3 章表达式一节 |
| 代码3.1 vs 算法3.5 / 3.9 | 栈 ADT 是 `pop(T&)`，算法写成 `pop(&x)` | 两处清单从未放进同一个翻译单元 |
| 第 4.3 节 vs 算法4.6 / 4.8 | 正文明写下标从 0 起，返回值却按 1 起始算 | 图4.12 只画过程、没给返回值，所以印出来没人发现 |

## 反复出现、但单独一条通常还能「跑两下」

这些不是某一条清单独有，而是同一类所有权错误在多章重复。编译能过，拷贝一次就二次释放。

- 有析构、无拷贝构造 / 拷贝赋值：顺序表、链表、顺序栈、链式栈、字符串、最小堆、图的裸数组。
- 容器里 `cout` 报错、用 `bool + 出参` 表达空状态：第 2、3、4 章通病。
- `int` 当下标与容量：有符号溢出是未定义行为。

现代实现按 [D-001](../collab/DECISION_LOG.md) 处理：显式五法则、`std::optional` 表达空、真错误抛标准异常。

## 作者代码包里有没有

对照原书配套的作者代码包（2025 秋期末机考的考场资料之一，见附录「考场代码包」），上面每条编译不过或跑起来错的勘误都能再问一句：**作者自己的代码里也这样吗？**

- **包里也有**：作者的代码本来就这样，不是排印或 OCR 造成的；
- **包里没有**：印出来的与作者的代码不一致，包里是另一种（通常能编译的）写法。包与原书同为 2008 年 6 月，谁先谁后不可考，所以只说「不一致」，不说「排印时引入」；
- **包里无此代码**：包里没有对应的实现，无从比较。

| 勘误 | 清单 | 包里 | 说明 |
| --- | --- | --- | --- |
| E01 | 代码2.1 / 代码2.2 / 算法2.5 | 包里没有 | 包里在用的顺序表与单链表都叫 `del(int p)`。只有 `alg2.1-5/lnkList.h` 这份没被 `main.cpp` 包含、从不参与编译的旧稿还写着 `bool delete(int p)`——原书印的正是这种写法 |
| E02 | 代码2.1 | 包里也有 | 包里的 `class List` 同样通篇没有 `public:`——只是包里没有任何代码通过 `List` 调用这些运算，所以编译得过 |
| E03 | 算法2.3 | 包里没有 | 包里 `getPos` 的循环上界是成员 `curLen`，编译得过；原书印成未声明的 `n` |
| E04 | 代码2.6 | 包里没有 | 包里 `Link` 的构造参数是 `Link* nextValue`，没有 `const`，不存在丢弃 `const` 的赋值 |
| E05 | 代码3.2 | 包里没有 | 包里取栈顶叫 `getTop(T*)`，与 public 数据成员 `int top` 不重名，整个文件编译得过；原书印成 `bool top(T&)` |
| E06 | 算法3.3 | 包里无此代码 | 包里三份 `arrStack.h` 的 `push` 都是栈满就打印「栈满溢出」返回 `false`，没有翻倍扩容的版本，无从比较 |
| E07 | 代码3.4 | 包里没有 | 包里链式栈同样用 `getTop(T*)`，与 `Link<T>* top` 不重名 |
| E08 / E20 | 算法3.5 / 算法3.9 | 包里没有 | 包里栈的 ADT 就是 `pop(T* item)`，算法里写 `s.pop(&x)` 与之一致、编译得过；原书把 ADT 印成了 `pop(T&)`，算法却照抄了包里的 `pop(&x)` |
| E09 | 算法3.11 | 包里没有 | 包里是 `enum rdType {a, b, c}` 与普通的 `class knapNode`，没有字面量枚举值，也没有 Java 式的 `public class` |
| E10 | 算法3.12 | 包里没有 | 包里 `t = st.top` 读的是 public 数据成员，取栈顶用 `st.getTop(&tmp)`，两者不重名 |
| E11 | 算法4.4 | 包里也有 | 包里就是 `assert(str != '\0')`——包里 `alg4-5/main.cpp` 今天编译不过，报的正是这一句（见附录「考场代码包」） |
| E12 | 算法4.4 | 包里也有 | 包里的构造函数就是 `String(char *s)` |
| E13 | 代码10.1 | 包里没有 | 包里初始化列表与数据成员都叫 `key` |
| E14 | 算法3.6 / 3.8 / 3.9 | 包里也有 | 包里三版阶乘都是 `long` 连乘，不查溢出 |
| E15 | 算法3.6 | 包里也有 | 包里（整段注释掉的）递归版同样是 `if (n <= 0) return 1` |
| E16 | 算法4.5 | 包里也有 | 包里 `Substr` 越界同样 `return NULL`（经 `String(char*)` 构造，`strlen(NULL)`） |
| E17 / E21 / R10 / R13 | 算法4.6 / 算法4.8 | 包里也有 | 包里朴素匹配与 KMP 都返回 `j - pLen + 1`：作者的代码本来就按 1 起始报位置，不是排印问题。对拍 407 组，找到时作者恒比 0 起始位置大 1 |
| E18 | 算法10.9 | 包里也有 | 包里三份 `HASHDICT.H` 的析构都是 `delete HT`（`HT` 由 `new Elem[sz]` 分配） |
| E19 | 算法3.5 | 包里也有 | 包里同样用 `operand1 == 0.0` 判除零 |
| E22 | 顺序表/链表/顺序栈/链式栈/最小堆 | 包里也有 | 包里这些类都有析构、都没有拷贝构造与拷贝赋值，拷贝一次即二次释放。（包里的 `String` 连析构都没有：不二次释放，但每个串都泄漏。） |
| R04 | 算法2.9 | 包里也有 | 包里 `setPos` 与印刷版逐字相同：`p = new Link<T>(head->next)`。不止泄漏——`setPos(k)` 返回第 k-1 个结点，`insert(1, v)` 静默丢失、`insert(i, v)`（i ≥ 2）早一格；对拍 2400 次随机插入逐项吻合这个错位 |
| R07 | 算法3.12 | 包里没有 | 扫描件第 67 页印的 `while` 条件少一个左括号（OCR 又把 `&&` 读成了 `&`，那是另一回事）；包里同一句以 `while ((tmp.s >= 0) &&` 开头，括号配平、编译得过 |
| R12 | 算法4.7 | 包里没有 | 包里写的是 `while (i < m)` 加 `if (i == m) break`——`break` 是必需的；原书把条件印成 `i < m-1` 又留着 `break`，才显得多余 |
| R15 | 算法5.x Parent | 包里也有 | 包里 `Parent` 的循环走完后直接落到函数末尾，没有 `return NULL`（找不到父结点时返回值未定义） |

只在包里、原书没有的缺陷见附录「考场代码包」的「包里的坑」。

## 曾经误判、已撤回

第 1 章股市传言的印刷矩阵里，B1 / B5 的最大最短路一度被读成 8，并据此声称「原书应返回 B1 而非正文的 B3」。那些 `8` 是 OCR 把 `∞` 认错；选起点用的图1.3 是图片，从未被 OCR。按正文还原，**原书这个样例是对的**。详见 [adt/legacy.md](../code/ch01/adt/legacy.md)。

## 还没收进本表的

- 算法2.11、代码3.1、算法7.6、算法7.9 的结束标记被 OCR 吃掉，切片边界仍是手定（T-008）。
- 第 6–12 章不少「标识符被空格切断」「全角分号」更像 OCR 而不是作者写错，故不列入上表。

## 参考勘误表补充（前六章）

教材《 1–6 章勘误表》还记录了几处不影响本书现代实现、但值得在对照纸书时知道的文字和
公式问题。本书已按语义处理，列在这里是为了让读者能把不同版本的页码对上：

| 原书位置 | 勘误要点 | 本书处理 |
| --- | --- | --- |
| 第 1.4.2 节，第 19 页 | “有序子数组”应明确为原数组中相邻连续的有序子数组；长度比较应使用 `j2 - j1 + 1` | 第 1 章把连续性和边界写成文字约定，并用半开区间表示长度 |
| 习题 1.4.2(3)，第 24 页 | `T(n)2=...` 是排印错误，应为 `T(n)=...` | 第 1 章复杂度只使用校正后的递推式 |
| 算法 2.9，第 37 页（勘误表错误 4） | `p` 应直接指向 `head->next`；模板拼写应修正。原书写的是 `p = new Link<T>(head->next)`：定位从一个新分配的多余结点起步，不止泄漏，`setPos(k)` 还返回第 k-1 个结点——`insert(1, v)` 静默丢失、之后的插入早一格（作者代码包里逐字相同，已真跑复现） | 第 2 章链表使用 RAII 结点和明确的循链定位 |
| 算法 2.9，第 37 页（勘误表错误 9） | 「多余分号」：分号在循环体结束的 `}` 之后（`};`），是一条空语句，**不影响循环语义**，可作排印清理 | 第 2 章链表的循环不带这个分号 |
| 算法 4.6/4.8，第 91、97 页 | 0 起始下标的匹配位置返回 `j-pLen`，不能加 1 | 第 4 章朴素匹配和 KMP 均按 0 起始并有对拍测试 |
| 算法 4.7，第 95 页 | `while (i<m-1)` 已包含边界，不需额外 `if(i==m) break` | 第 4 章 next 表构造采用单一边界条件 |
| 第 5、6 章图示 | Parent 失败分支需返回空；树的子结点链表编号有印刷错误 | 第 5、6 章接口用 `optional`/空指针表达失败，并以代码测试为准 |

这些条目是参考勘误的内容性摘要，不替代原 PDF 的页码和完整记录。
