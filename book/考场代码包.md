# 考场代码包

2025 秋「数据结构与算法」期末机考（`dsa.openjudge.cn/2025final`）的考场说明列出四份**考场可以使用**的资料：
本书作者的 C++ 代码包、一份 C 语言代码包、C++ 离线手册（cppreference）和 Weiss 的英文教材。
本附录只讲与本书直接相关的第一份：`DSCode_ZhangWangZhao2008_06.zip`。

原书前言说：「教材中只给出了最主要的算法和代码，完整的实现方法可参考教学网站上提供的与本书配套的代码包。」
这就是那个包。它是作者**实际编译运行过**的工程，每章一个目录，每个算法一两个文件。

## 它和书上印的不是同一份

包名里的 `200806` 与原书「2008 年 6 月第 1 版第 1 次印刷」是同一个月，但包里的代码和书上印的**不总是一致**，
而且两边各有对错：

- **多数时候包里是对的。** 原书第 3 章的顺序栈按印刷原样编译不过——数据成员 `int top` 与成员函数 `bool top(T&)` 重名；
  包里那个函数叫 `getTop(T*)`，编译得过。第 2 章的 `delete` 作函数名、第 3 章背包问题的 `enum rdType {0,1,2}` 也是这样。
- **也有书上对、包里错的时候。** 原书算法8.5 冒泡排序的内层循环写 `j > i`，包里写成 `j >= i`，第一趟就读到 `Array[-1]`；
  原书算法2.11 的单链表删除写对了，包里删尾结点时会把同一个结点释放两次。
- **还有两边都错的。** 算法2.9 的 `setPos` 用 `new` 出一个多余的结点起步，书上和包里逐字相同；
  算法4.6、4.8 的模式匹配都按 1 起始报位置，而正文说下标从 0 起。

所以考场上拿包里的代码来用，**先在本书里查一下那一条**：原书 105 条清单，本书逐条登记了它在包里的位置；
每条「编译不过」或「跑起来是错的」勘误，都查过包里有没有；第 2、3、4、8 章还把包里的实现与本书的实现
在同一批输入上真跑了对拍。逐条结论见本附录后三节与书末「原书勘误」。

## 用之前要知道的三件事

1. **编码。** 包里大多数文件是 GBK 编码、Windows 换行（2008 年 VC6 的默认），第 1、10、12 章另有几个带 BOM 的 UTF-8 文件。
   用 UTF-8 打开 GBK 文件，中文注释会是乱码——换成 GBK 重新打开即可，代码本身不受影响。
2. **编译器。** 包是 VC6 年代写的，有些写法今天的 g++ 不再接受，最常见的是 `void main()`。
   OpenJudge 的 C++ 用 g++ 编译，交题前按「今天的 g++ 编译得过吗」一节改掉。
3. **容器会自己打印。** 包里的栈、队列、线性表在出错时直接 `cout` 一句中文提示（「栈满溢出」「栈为空，不能出栈操作」），
   题目要求严格输出格式时，这些提示会让答案错。

## 清单在包里的位置

原书 105 条清单在包里的去处。符号是函数或类名；同一文件里有几个同名版本时注明第几个。

| 清单 | 包里的文件 | 符号 |
| --- | --- | --- |
| 算法1.1 | `ch01_Intro/alg1.1/stockbroker.cpp` | `BrokerToPassRomour` |
| 代码1.2 | 包里没有 | 抽象数据类型的通用模板（className/Type 占位符），不是可编译程序，包里无对应文件 |
| 代码2.1 | `ch02_LinearList/alg2.1-5/myList.h` | `class List` |
| 代码2.2 | `ch02_LinearList/alg2.1-5/arrList.h` | `class arrList` |
| 算法2.3 | `ch02_LinearList/alg2.1-5/arrList.h` | `getPos` |
| 算法2.4 | `ch02_LinearList/alg2.1-5/arrList.h` | `insert` |
| 算法2.5 | `ch02_LinearList/alg2.1-5/arrList.h` | `del`；包里函数名是 `del`；原书印作 `delete`（C++ 关键字，不能作函数名） |
| 代码2.6 | `ch02_LinearList/alg2.6-11/lnkList.h` | `class Link` |
| 代码2.7 | `ch02_LinearList/alg2.6-11/lnkList.h` | `class lnkList` |
| 算法2.8 | `ch02_LinearList/alg2.6-11/lnkList.h` | `lnkList`、`~lnkList` |
| 算法2.9 | `ch02_LinearList/alg2.6-11/lnkList.h` | `setPos` |
| 算法2.10 | `ch02_LinearList/alg2.6-11/lnkList.h` | `insert` |
| 算法2.11 | `ch02_LinearList/alg2.6-11/lnkList.h` | `del`；包里函数名是 `del`；原书印作 `delete`（C++ 关键字，不能作函数名） |
| 代码2.12 | 包里没有 | 双链表：包里 ch02 只有顺序表与单链表，没有双链表结点 |
| 代码3.1 | `ch03_StackQueue/alg3.5/myStack.h` | `class Stack` |
| 代码3.2 | `ch03_StackQueue/alg3.5/arrStack.h` | `class arrStack`；包里取栈顶叫 `getTop(T*)`，与成员变量 `int top` 不重名；原书印作 `top(T&)`，与之重名，编译不过 |
| 算法3.3 | 包里没有 | 改进的进栈（满则容量翻倍）：包里三份 `arrStack.h` 的 `push` 都是栈满即报错返回 `false`，没有翻倍版本 |
| 代码3.4 | `ch03_StackQueue/alg3.4/lnkstack.h` | `class lnkStack` |
| 算法3.5 | `ch03_StackQueue/alg3.5/calc.h` | `class Calculator`、`GetTwoOperands`、`Compute`、`Run` |
| 算法3.6 | `ch03_StackQueue/alg3.6-8/fact.cpp` | `factorial`（第 1 个）；递归版在 `fact.cpp` 里整段注释掉了，与迭代版一起留作对照 |
| 算法3.7 | `ch03_StackQueue/alg3.6-8/fact.cpp` | `main` |
| 算法3.8 | `ch03_StackQueue/alg3.6-8/fact.cpp` | `factorial`（第 2 个）；迭代版在 `fact.cpp` 里整段注释掉了 |
| 算法3.9 | `ch03_StackQueue/alg3.6-8/fact.cpp` | `factorial`；`fact.cpp` 里唯一未注释的版本：用栈模拟递归 |
| 算法3.10 | `ch03_StackQueue/alg3.9-11/knap1.cpp` | `knap` |
| 算法3.11 | `ch03_StackQueue/alg3.9-11/knap1.cpp` | `nonRecKnap` |
| 算法3.12 | `ch03_StackQueue/alg3.9-11/knap1.cpp` | `nonRecKnapOpt` |
| 代码3.13 | `ch03_StackQueue/alg3.13-14/myQueue.h` | `class Queue` |
| 代码3.14 | `ch03_StackQueue/alg3.13-14/arrQueue.h` | `class arrQueue` |
| 代码3.15 | `ch03_StackQueue/alg3.13-14/lnkQueue.h` | `class lnkQueue` |
| 代码4.1 | 包里没有 | 字符串的抽象数据类型定义（成员用 … 占位），不是可编译程序 |
| 代码4.2 | 包里没有 | 印的是标准库 `std::basic_string` 的模板声明，不是作者的代码 |
| 算法4.3 | `ch04_String/alg3/strcmp.cpp` | `strcmp` |
| 算法4.4 | `ch04_String/alg4-5/myString.h` | `String`（第 2 个）；`myString.h` 里第 1 个 `String(...)` 是无参构造，原书这一条是第 2 个 `String(char *s)` |
| 算法4.5 | `ch04_String/alg4-5/myString.h` | `Substr` |
| 算法4.6 | `ch04_String/alg6/naive.h` | `NaiveStrMatching` |
| 算法4.7 | `ch04_String/alg7-8/kmp.h` | `findNext` |
| 算法4.8 | `ch04_String/alg7-8/kmp.h` | `KMPStrMatching` |
| 代码5.1 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTreeNode.h` | `class BinaryTreeNode` |
| 代码5.2 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `class BinaryTree` |
| 算法5.3 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `PreOrder`、`InOrder`、`PostOrder` |
| 算法5.4 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `PreOrderWithoutRecursion` |
| 算法5.5 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `InOrderWithoutRecursion` |
| 算法5.6 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `class StackElement`、`PostOrderWithoutRecursion` |
| 算法5.7 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `LevelOrder` |
| 代码5.8 | `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.h` | `isEmpty`、`Parent`、`LeftSibling`、`RightSibling`、`CreateTree`、`DeleteBinaryTree` |
| 算法5.9 | `ch05_BinaryTree/alg9-10binarysearchtree/BinarySearchTree.h` | `InsertNode` |
| 算法5.10 | `ch05_BinaryTree/alg9-10binarysearchtree/BinarySearchTree.h` | `DeleteNodeEx` |
| 代码5.11 | `ch05_BinaryTree/alg11MinHeap/MinHeap.h` | `class MinHeap`、`SiftDown`、`SiftUp` |
| 代码5.12 | `ch05_BinaryTree/alg12HuffmanTree/HuffmanTree.h` | `class HuffmanTree`、`HuffmanTree` |
| 代码6.1 | `ch06_Tree/tree(alg_1-7)/TreeNode.h` | `class TreeNode` |
| 代码6.2 | `ch06_Tree/tree(alg_1-7)/Tree.h` | `class Tree` |
| 算法6.3 | `ch06_Tree/tree(alg_1-7)/Tree.h` | `RootFirstTraverse` |
| 算法6.4 | `ch06_Tree/tree(alg_1-7)/Tree.h` | `RootLastTraverse` |
| 算法6.5 | `ch06_Tree/tree(alg_1-7)/Tree.h` | `WidthTraverse` |
| 代码6.6 | `ch06_Tree/tree(alg_1-7)/TreeNode.h` | `InsertFirst`、`InsertNext` |
| 代码6.7 | `ch06_Tree/tree(alg_1-7)/Tree.h` | `Parent`、`PrevSibling`、`DeleteSubTree` |
| 代码6.8 | `ch06_Tree/unionfind(alg_8-9)/ParTree.h` | `class ParTree`、`Find`、`Union` |
| 算法6.9 | `ch06_Tree/unionfind(alg_8-9)/ParTree.h` | `FindPC` |
| 算法6.10 | `ch06_Tree/DualTagFirstroot(alg6_10)/dualtagtree_firstroot.cpp` | `class DualTagTreeNode`、`Tree` |
| 代码7.1 | `ch07_Graph/graph_matrix/Graph.h` | `class Graph`；包里只有一个 `Graph` 类：原书把 ADT（代码7.1）与基类（代码7.2）分成两段印 |
| 代码7.2 | `ch07_Graph/graph_matrix/Graph.h` | `class Edge`、`class Graph` |
| 代码7.3 | `ch07_Graph/graph_matrix/Graphm.h` | `class Graphm` |
| 代码7.4 | `ch07_Graph/Graph_Link/Graphl.h` | `class Graphl` |
| 算法7.5 | `ch07_Graph/graph_matrix/Graphm.h` | `DFS` |
| 算法7.6 | `ch07_Graph/graph_matrix/Graphm.h` | `BFS` |
| 算法7.7 | `ch07_Graph/TopologicalSorting/topological_sorting.cpp` | `TopsortbyQueue` |
| 算法7.8 | `ch07_Graph/Graph_Dijkstra/Dijkstra.cpp` | `class Dist`、`Dijkstra` |
| 算法7.9 | `ch07_Graph/Graph_Floyd/Floyd.cpp` | `Floyd` |
| 算法7.10 | `ch07_Graph/Graph_Prim/Prim.cpp` | `minVertex`、`Prim` |
| 算法7.11 | `ch07_Graph/Graph_Kruskal/Kruskal.cpp` | `Kruskal` |
| 算法8.1 | `ch08_Sort/InsSort.cpp` | `InsertSort` |
| 算法8.2 | `ch08_Sort/ShellSort/ShSort2.cpp` | `ShellSort`、`ModInsSort`；`ShSort3.cpp` 是增量每次除以 3 的变体，原书没有印 |
| 算法8.3 | `ch08_Sort/SelSort.cpp` | `SelectSort` |
| 算法8.4 | `ch08_Sort/HeapSort/HeapSort.cpp` | `sort`；堆排序主体在 `sort()` 里，堆本身见 `HeapSort/MaxHeap.h` |
| 算法8.5 | `ch08_Sort/BubSort.cpp` | `BubbleSort` |
| 算法8.6 | `ch08_Sort/QuickSort/QuickSort.cpp` | `QuickSort`、`SelectPivot`、`Partition` |
| 算法8.7 | `ch08_Sort/QuickSort/ModQuickSort.cpp` | `ModQuickSort` |
| 算法8.8 | `ch08_Sort/MergeSort/MergeSort.cpp` | `MergeSort`、`Merge` |
| 算法8.9 | `ch08_Sort/MergeSort/ModMergeSort.cpp` | `ModMergeSort`、`ModMerge` |
| 算法8.10 | `ch08_Sort/BucketSort.cpp` | `BucketSort` |
| 算法8.11 | `ch08_Sort/RadixSort/RadixSort_Bin.cpp` | `RadixSort` |
| 代码8.12 | `ch08_Sort/RadixSort/LinkRadixSort.h` | `class StaticQueue` |
| 算法8.13 | `ch08_Sort/RadixSort/LinkRadixSort.h` | `RadixSort`、`Distribute`、`Collect` |
| 算法8.14 | `ch08_Sort/AddSort_Insert.cpp` | `IndexSort`；文件开头还有一个整段注释掉的 `IndexSort(Array, n)`（内部 `new` 索引数组、结尾不 `delete`）；原书印的是带 `IndexArray` 参数的这一版 |
| 算法8.15 | `ch08_Sort/AddSort_Insert.cpp` | `AdjustRecord` |
| 代码8.16 | `ch08_Sort/sort.h` | `Randomize`、`Random` |
| 代码8.17 | `ch08_Sort/sort.h` | `Settime`、`Gettime` |
| 算法9.1 | `ch09_ExternalSort/alg9_1 replacementSelection/ReplacementSelection.cpp` | `replacementSelection` |
| 代码9.2 | `ch09_ExternalSort/alg9_2WinnerTree/WinnerTree.h` | `class WinnerTree` |
| 代码9.3 | `ch09_ExternalSort/alg9_3LoserTree/LoserTree.h` | `class LoserTree` |
| 代码10.1 | `ch10_Search/algor1-3/algor1&2.cpp` | `class Item` |
| 算法10.2 | `ch10_Search/algor1-3/algor1&2.cpp` | `SeqSearch` |
| 算法10.3 | `ch10_Search/algor1-3/algor1&2.cpp` | `BinSearch` |
| 代码10.4 | `ch10_Search/BitSet/setm.h` | `class mySet` |
| 算法10.5 | `ch10_Search/BitSet/setm.h` | `set` |
| 算法10.6 | `ch10_Search/BitSet/setm.h` | `operator&` |
| 算法10.7 | `ch10_Search/BitSet/setm.h` | `operator>` |
| 算法10.8 | `ch10_Search/hash_dict/HASHDICT.H` | `ELFhash` |
| 算法10.9 | `ch10_Search/hash_dict/HASHDICT.H` | `class hashdict` |
| 算法10.10 | `ch10_Search/hash_dict/HASHDICT.CPP` | `hashInsert` |
| 算法10.11 | `ch10_Search/hash_dict/HASHDICT.CPP` | `hashSearch` |
| 算法10.12 | `ch10_Search/hash_tomb2/HASHDICT.CPP` | `hashDelete` |
| 算法10.13 | `ch10_Search/hash_tomb2/HASHDICT.CPP` | `hashInsert`；`hash_tomb` 与 `hash_tomb2` 两版：`tomb2` 记下第一个墓碑位置（`insplace`）再插入，与原书一致 |
| 算法12.1 | 包里没有 | 可利用空间表（重载 LinkNode 的 new/delete）：包里 ch12 只有广义表、稀疏矩阵、AVL、伸展树与最佳 BST |
| 算法12.2 | `ch12_AdvDS/AdvTree/BestBST/OptimalBST.cpp` | `OptimalBST` |

## 今天的 g++ 编译得过吗

包里能单独编译成程序的 `.cpp` 共 54 个（自身或它包含的头文件里有 `main`；第 8 章的排序都把 `main` 放在 `SortMain.h` 里）。先把源码转成 UTF-8，再按 `-std=c++17` 只做语法检查：g++（gcc/libstdc++）38 个编译得过；clang（clang/libstdc++）38 个编译得过；clang + libc++（clang/libc++）37 个编译得过。**编译结果随工具链而变**：OpenJudge 的 C++ 用 g++；macOS 上的 `g++` 其实是 Apple clang + libc++，最接近「clang + libc++」那一列。

编译不过的几乎全是 2008 年 VC6 能容忍、今天的编译器不再接受的写法，不是算法错。下表列出在任一工具链下编译不过的程序（✓ 编译得过，✗ 编译不过），报错取第一个失败工具链的第一条。

| 程序 | g++ | clang | clang + libc++ | 第一条报错 | 改法 |
| --- | --- | --- | --- | --- | --- |
| `ch04_String/alg4-5/main.cpp` | ✗ | ✗ | ✗ | `ISO C++ forbids comparison between pointer and integer [-fpermissive]` | `assert(str != '\0')` 改为 `assert(str != NULL)`（勘误 E11） |
| `ch05_BinaryTree/alg1-8BinaryTree/BinaryTree.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch05_BinaryTree/alg9-10binarysearchtree/binarysearchtree.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch06_Tree/DualTagFirstroot(alg6_10)/dualtagtree_firstroot.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch06_Tree/DualTagWidth(alg6_11)/dualtagtree_Width.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch06_Tree/unionfind(alg_8-9)/unionfind.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch07_Graph/Graph_Floyd/Floyd.cpp` | ✗ | ✗ | ✗ | `extra qualification ‘Graphm::’ on member ‘IniGraphm’ [-fpermissive]` | 类内声明去掉 `Graphm::` 前缀 |
| `ch07_Graph/Graph_Link/link.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch07_Graph/Graph_Prim/Prim.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch07_Graph/graph_matrix/matrix.cpp` | ✗ | ✗ | ✗ | `‘::main’ must return ‘int’` | `void main()` 改成 `int main()` |
| `ch08_Sort/RadixSort/LinkRadixSort.cpp` | ✗ | ✗ | ✗ | `‘i’ was not declared in this scope` | `LinkSort.h` 的 `PrintAddr` 里循环变量 `i` 没声明：写成 `for (int i=0; ...)` |
| `ch09_ExternalSort/alg9_1 replacementSelection/ReplacementSelection.cpp` | ✗ | ✗ | ✗ | `reference to ‘less’ is ambiguous` | 自定义的 `less` 与 `std::less` 撞名：改名，或去掉 `using namespace std;` |
| `ch09_ExternalSort/alg9_2WinnerTree/WinnerTree.cpp` | ✗ | ✗ | ✗ | `‘cout’ was not declared in this scope; did you mean ‘std::cout’?` | 补 `using namespace std;`，或写 `std::cout` |
| `ch09_ExternalSort/alg9_3LoserTree/LoserTree.cpp` | ✗ | ✗ | ✗ | `‘cout’ was not declared in this scope; did you mean ‘std::cout’?` | 补 `using namespace std;`，或写 `std::cout` |
| `ch10_Search/BitSet/bag.cpp` | ✓ | ✓ | ✗ | `reference to 'count' is ambiguous` | 全局变量 `count` 与 `std::count` 撞名（只在 libc++ 下）：改名，或去掉 `using namespace std;` |
| `ch12_AdvDS/AdvList/SparseMatrix/Example.cpp` | ✗ | ✗ | ✗ | `afxtempl.h: No such file or directory` | VC6 的 MFC 头文件，g++ 没有：换成标准库容器 |
| `ch12_AdvDS/AdvTree/AVLTree/Example.cpp` | ✗ | ✗ | ✗ | `redeclaration of ‘avlNode<T>::avlNode(T, avlNode<T>*, avlNode<T>*, int)’ may not have default arguments [-fpermissive]` | 默认参数只留在类内声明，类外定义处删掉 |

还有一件事语法检查看不出来：第 8 章这些排序程序在 clang 下「编译得过」，只因为计时驱动里的排序调用被注释掉了（见「包里的坑」P06），模板从没被实例化。真去调用 `ShellSort`、`QuickSort`、`MergeSort` 时，clang 会按标准报「函数在模板定义处不可见」——作者都是先调用、后定义（`ModInsSort`、`Partition`、`Merge`……），只有 g++ 放行。在 clang 下用它们，把被调函数的定义挪到前面，或先写一行声明。

所有工具链下都编译得过的：`ch02_LinearList/alg2.1-5/main.cpp`、`ch02_LinearList/alg2.6-11/main.cpp`、`ch03_StackQueue/alg3.12/main.cpp`、`ch03_StackQueue/alg3.13-14/main.cpp`、`ch03_StackQueue/alg3.4/main.cpp`、`ch03_StackQueue/alg3.5/main.cpp`、`ch03_StackQueue/alg3.6-8/fact.cpp`、`ch03_StackQueue/alg3.9-11/knap1.cpp`、`ch04_String/alg3/strcmp.cpp`、`ch04_String/alg6/main.cpp`、`ch04_String/alg7-8/main-kmp.cpp`、`ch05_BinaryTree/alg11MinHeap/MinHeap.cpp`、`ch05_BinaryTree/alg12HuffmanTree/HuffmanTree.cpp`、`ch06_Tree/tree(alg_1-7)/tree.cpp`、`ch07_Graph/Graph_Dijkstra/Dijkstra.cpp`、`ch07_Graph/Graph_Kruskal/Kruskal.cpp`、`ch07_Graph/TopologicalSorting/topological_sorting.cpp`、`ch08_Sort/AddSort_Insert.cpp`、`ch08_Sort/BubSort.cpp`、`ch08_Sort/BucketSort.cpp`、`ch08_Sort/HeapSort/HeapSort.cpp`、`ch08_Sort/InsSort.cpp`、`ch08_Sort/MergeSort/MergeSort.cpp`、`ch08_Sort/MergeSort/ModMergeSort.cpp`、`ch08_Sort/QuickSort/ModQuickSort.cpp`、`ch08_Sort/QuickSort/QuickSort.cpp`、`ch08_Sort/RadixSort/LinkSortMain.cpp`、`ch08_Sort/RadixSort/RadixSort_Bin.cpp`、`ch08_Sort/SelSort.cpp`、`ch08_Sort/ShellSort/ShSort2.cpp`、`ch08_Sort/ShellSort/ShSort3.cpp`、`ch10_Search/BitSet/test.cpp`、`ch10_Search/algor1-3/algor1&2.cpp`、`ch12_AdvDS/AdvList/GenList/Example.cpp`、`ch12_AdvDS/AdvList/GenList_ReEntrant/Example.cpp`、`ch12_AdvDS/AdvTree/BestBST/OptimalBST.cpp`、`ch12_AdvDS/AdvTree/SplayTree/splay.cpp`。

## 包里的坑

下面几处缺陷**原书没有**——要么原书没印那段代码，要么印对了——只在包里。考场上直接拿包里的代码交题，会在这些地方读写到数组外面或得到错的结果。

| 编号 | 位置 | 缺陷 |
| --- | --- | --- |
| P01 | 算法8.4（包里的 MaxHeap），`ch08_Sort/HeapSort/MaxHeap.h` | `MaxHeap` 的 `SiftDown` 判右孩子写 `j<CurrentSize`，应为 `j<CurrentSize-1`：n 为偶数时建堆读 `heapArray[n]`，越界一格，那格若更大还会被换进堆、再把 `temp` 写出界。原书没印 `MaxHeap`；第 5 章 `MinHeap` 与代码5.11 写对了。ASan 在对拍里当场报 heap-buffer-overflow |
| P02 | 算法8.5，`ch08_Sort/BubSort.cpp` | `BubbleSort` 内层循环写 `j>=i`：i=0 时比较 `Array[0]` 与 `Array[-1]`，越界一格；原书印的 `j > i` 是对的 |
| P03 | 算法2.11，`ch02_LinearList/alg2.6-11/lnkList.h` | `lnkList` 的 `del` 删尾结点时 `tail = p; delete q;` 之后又落进 `if (q != NULL) { p->next = q->next; delete q; }`——释放后使用加二次释放；原书用 `else if` 写对了，还多了 `p == tail` 的判断 |
| P04 | 算法3.10，`ch03_StackQueue/alg3.9-11/knap1.cpp` | `knap` 最后一行是 `else knap(s, n-1);`——少了 `return`，结果被丢弃、函数从末尾流出（未定义行为）；原书印的 `else return knap(s, n-1)` 是对的 |
| P05 | 算法8.10（包里的驱动），`ch08_Sort/BucketSort.cpp` | `BucketSort.cpp` 的 `sort` 包装写死 `max = 80`，而计时驱动 `SortMain.h` 生成 `Random(32003)`——值 ≥ 80 时 `count[TempArray[i]]` 越界写。只因驱动里的排序调用被注释掉（P06）才没炸 |
| P06 | 第 8 章计时驱动，`ch08_Sort/SortMain.h` | `SortMain.h` 的计时循环里 `//sort<int>(&array[i], listsize);` 被注释掉：包里 12 个排序程序跑起来只生成 1000 万个随机数、给空循环计时，随后逐对检查有序、打出成串 `ERROR!!!`——作者的排序从没在这个驱动里真正跑过 |

另有与原书同病、但不在勘误表里的：
- **P07**（代码3.2）：无参构造 `arrStack()` 只设 `top = -1`，`mSize` 与 `st` 都未初始化，析构时 `delete [] st` 释放野指针——原书代码3.2 的无参构造同病

与原书同病的那些（印出来就错、包里也错）见书末「原书勘误」的「作者代码包里有没有」一节。