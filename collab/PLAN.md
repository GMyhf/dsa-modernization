# PLAN · 唯一任务清单与决策记录

> 这是 Claude 与 Codex 共享的**唯一任务事实源**。人拍板任务与优先级；
> 两个 agent 认领任务、更新状态、署名。状态流转：`Backlog → In progress → Review → Done`。
> 每条任务用一个 `T-<编号>` 标识，交接与提交信息里引用它。
>
> **格式硬约束**：每行恰好 5 列；追加复核结论写进备注列、用 ` — ` 分隔，
> **不要**用 `| ` 另起一格——超出表头的单元格在 GitHub 渲染时被直接丢弃，
> 人看到的会是另一份 PLAN。描述里含 `|` 的代码片段要转义（`\|`）。

## 状态看板

> ✅ **2026-08-12 全项目收口**：闸门退出码 0——61 项工具自测、台账 104/105 + 1 退场
> + 0 待办、书稿 11 个文件、**19/19 单元双构建**（Debug+ASan/UBSan 与 Release-O2）。
> 未验证的风险集中在 `collab/UNVERIFIED-RISKS.md`。
> 以下为返工前的记录，保留备查。
>
> ⚠️ **返工前：台账数字在结构上成立、在内容上还差两条。** `6177dfc` 认领了
> 第 1、6–12 章共 61 条清单，返工理由三条：① 测试密度 0.3–0.7 项/清单
> （前五章 8–18 项）；② `legacy.md` 普遍两行零证据；③ 第 8 章的快排/堆排/基数排序
> 直接委托给标准库（D-001 §2 违规）。
> **更正**：我最初据行数说"整批空壳"，对第 6、7、9、10、12 章是冤枉的——
> 那五章的实现是真的，只是压成超长单行。详见 `HANDOFF.md` 与 D-007 的当日更正。
> 闸门实质性检查（D-007）目前 **8/19 单元通过**。台账 104/105 未改动。

| ID | 任务 | 状态 | 负责 | 关联提交 / 备注 |
| --- | --- | --- | --- | --- |
| T-000 | 搭建 Claude⇄Codex 协作脚手架（`collab/` + `tools/` + `tests/`） | Done | Claude | 移植自 cs101.openjudge.cn/collab，闸门按本项目重写：台账 + 书稿体检 + 真编译。48 项工具自测通过，待 Codex 复核 — 2026-08-12 人拍板 D-001 后，工具默认标准改为 c++17 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：闸门 11 步全绿、268 项工具自测——脚手架本身已被它每天证明一次。 |
| T-001 | 样板单元：第 3.1 节顺序栈（代码3.1 / 代码3.2 / 算法3.3） | Done | Claude | `code/ch03/array_stack` + `book/ch03-stack.md`。**抓到原书两处编译错误**，见 `legacy.md` — 2026-08-12 按 D-001 重做为 C++17 + 裸指针 + 显式五法则，断言 29→38（新增强异常保证的故障注入与 `at()` 越界），变异自检 5/5 全抓 — 2026-08-12 按 D-001 §3b 新增 `peek()`，断言 38→50，peek 的两条守门用例经变异验证（退化成拷贝实现→move-only 处编译失败 + `copies==0` 断言红；空栈不返回 nullptr→UBSan） — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；3 条清单在 T-025 逐条绑到真实实现与行为断言；存储层又在 T-004 换过（D-024）。 |
| T-002 | **红队**：Codex 找漏——D-001 静态检查的正则盲区、`bad_alloc`/移动赋值故障注入、`peek()` 接口 | Done | Codex | 建议起点：R2 的注释剥离能否被绕过？R3 的 dedent 比对能否被空白差异欺骗？台账能否被「认领了但 listings 写错编号」骗过？任务书见 `collab/REDTEAM-BRIEF-T002.md`。**红队达标**：交出两条会失败的测试，均为真缺陷（`cc26132`）。 — Claude 复核（2026-08-12）：两处诊断全部认可；异常安全的**修复方式返工一次**（判据由「可不可拷贝」改为「移动赋值抛不抛」，否则 std::string 每次扩容深拷贝，实测 63/63 次搬迁全是拷贝→改后 0 次），并补上唯一能分辨两种策略的守门用例。Codex 报告的 ASan 失败经 Linux 复现确认为 macOS 环境问题，已做成工具自检（D-006）。断言 55→58 |
| T-003a | 第 2 章顺序表（代码2.1 / 代码2.2 / 算法2.3 / 算法2.4 / 算法2.5，5 条清单） | Done | Claude | `code/ch02/array_list` + `book/ch02-linear-list.md`。47 项断言，变异自检 6/6 全抓。**又抓到原书三处编译级硬伤**：`delete` 当函数名、`class List` 无 `public:`（全部成员私有）、算法2.3 的 `n` 未声明。另发现设计问题：`position` 游标住在容器里（const 不能遍历、不能嵌套遍历），已改为 `begin()/end()` — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；5 条清单在 T-025 逐条绑定。 |
| T-003b | 第 2 章链表（代码2.6–代码2.12，7 条清单） | Done | Codex | `code/ch02/linked_list` + `book/ch02-linear-list.md`。2026-08-12 已逐条核对并实现：头结点、尾指针 O(1) append、循链定位、插入/删除、单/双链结点、Rule of Five；30 项断言与两条变异自检通过，待 Claude 复核 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；7 条清单在 T-025 逐条绑定。 |
| T-004 | `ArrayStack` 改用未初始化存储 + placement new | Done | Claude | 2026-08-18 落地（D-024）。先量后改：预留容量 1000 时构造的对象 1000 → 0；`T` 不再需要可默认构造；`clear()` 之后活着的元素 1024（占 200 KB）→ 0。教学版 `teaching.hpp` 保持 `new T[n]` 不变，两版差别写进 3.1.2a（D-012 分层）。新引入的义务写在明处：对齐要自己传，去掉就是 heap-buffer-overflow。顺带销掉一道挂了六天的旧题——`move_if_noexcept` 在构造语境下本来就是对的，此前是容器把构造和分配捆在了一起。变异自检 6/6，断言 62 → 74。 |
| T-004a | 第 4 章模式匹配（算法4.6 / 4.7 / 4.8） | Done | Claude | `code/ch04/pattern_matching` + `book/ch04-string.md`。56 项断言（含 3000 组随机对拍），变异自检 5/5 全抓。**发现原书算法错**：算法4.6 与 4.8 的返回值 `j-pLen+1` 在 0 起始下标下一律差 1，四组数据对拍证实；另发现正文 next 数组比模式还长一位，与图4.11 矛盾。首次动用 `d001_exceptions`（`<vector>` 承载 next，附理由） — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；3 条清单在 T-025 逐条绑定。 |
| T-004b | 第 4 章字符串类（代码4.1 / 算法4.3 / 算法4.4 / 算法4.5） | Done | Claude | `code/ch04/string_class`。49 项断言，变异自检 5/5 全抓。原书三处硬伤：`assert(str != '\0')` 是指针与整数比较、本身编译不过；`String(char* s)` 使书中自己的例子 `String s1 = "Hello"` 在 `-Werror` 下编译失败；算法4.5 越界 `return NULL` → `strlen(nullptr)` 运行期 SEGV。**另有两条我的猜测被证据否掉并原样记进 legacy.md 第五节**（`strcmp` 同名不冲突；「编译不过」口径过强）。第 4 章至此 7/8 完成 + 1 退场 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；4 条清单在 T-025 逐条绑定。 |
| T-011 | **第 5 章二叉树**（12 条清单：代码5.1/5.2、算法5.3–5.7、代码5.8、算法5.9/5.10、代码5.11/5.12） | Done | Codex | `code/ch05/binary_tree` + `code/ch05/heap_huffman` + `book/ch05-binary-tree.md`。本机 Release：34 + 18 项断言全过；ASan 空探针失败，只完成 `--allow-degraded`。请 Claude 做完整双构建与泄漏/悬垂专项变异。代码5.8 OCR 边界已定在 4105 行“删除根结点”注释后，依据见 binary_tree/legacy.md — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；12 条清单在 T-025 逐条绑定。 |
| T-012 | **第 3 章队列**（代码3.13/3.14/3.15 + 3.3 节比较） | Done | Codex | `code/ch03/queue`：循环队列保留原书牺牲一个槽位策略，`dequeue()` 返回 optional，链式队列补五法则；待 sanitizer 复核 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；清单在 T-025 逐条绑定。 |
| T-013a | 第 3 章 3.1.5 递归与栈空间（算法3.6/3.7/3.8/3.9） | Done | Claude | `code/ch03/recursion_and_stack`，10 项断言，变异自检 4/4 全抓。**原书三版都不查溢出**：`factorial(21)` 返回负数、`factorial(66)` 返回 0，UBSan 判定为未定义行为；负数静默返回 1；算法3.9 的 `s.pop(&tmp)` 与代码3.1 的 `pop(T&)` 对不上。书稿 3.1.5 开篇给出栈深度实测表（含 `-O2` 把递归转成循环这一反直觉结论，汇编确认零次自调用） — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；算法3.7 的编译与语义证据在 T-027 补齐。 |
| T-013b | 第 3 章 3.1.3 链式栈 + 3.1.4 表达式求值（代码3.4、算法3.5） | Done | Claude | `code/ch03/linked_stack`（25 项断言）+ `code/ch03/expression_eval`（24 项）。**代码3.4 又是 `top` 重名**（与代码3.2 同一处错误，同书两个存储结构都犯）；`defSize` 参数从未使用；第五次遇到"有析构无拷贝构造"。算法3.5：算法与 cin/cout 焊死；**原书正文自己说 `operand1 == 0.0` 是错的写法，印出来的代码照旧**。变异自检中**证伪了我自己的一处主张**——详见 `expression_eval/legacy.md` 缺陷 1 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；清单在 T-025 逐条绑定。 |
| T-013c | 第 3 章背包问题（算法3.10 / 3.11 / 3.12） | Done | Claude | `code/ch03/knapsack`，54 项断言（含承重 0..20 穷举与暴力枚举四方对拍），变异自检 5/5 全抓。**三处编译错误**：`enum rdType {0,1,2}`、`public class knapNode`（Java 语法，出现两次）、算法3.12 同时把 `stack.top` 当数据成员和成员函数用——**后者依赖代码3.2/3.4 的 `top` 重名缺陷才可能存在**。另：`w[]` 是从未声明的全局数组，解通过 `cout` 打印而非返回。**Claude 第一版显式栈实现死循环撞上闸门超时，已记入 legacy.md 第三节** — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：32/32 单元双档通过；清单在 T-025 逐条绑定。 |
| T-014 | **第 1、6–12 章**（先收口，后返工；58 → 61 条清单） | Done | Codex | `code/ch01/adt`、`ch06/general_tree`、`ch07/graph`、`ch08/sorting`、`ch09/external_sort`、`ch10/search_hash`、`ch12/optimal_bst` 及对应书稿。台账已到 104/105 + 1 退场；本机仅 Release，待 sanitizer 复核 — **2026-08-18 清账**：这一行原本是 `T-014` 的**第一次**登记（状态 Review），与下面那次返工共用了同一个编号。两者是同一件事的两个阶段，已合并为一行。返工记录：人于 2026-08-12 指派。任务书 `collab/BRIEF-T014-rework.md`。返工三条理由：测试密度、`legacy.md` 零证据、第 8 章 D-001 §2 违规（快排/堆排/基数排序须手写）。**分批交接，别再一轮 61 条** — Claude 复核（2026-08-12）：**认可**。密度全面达标（Sorting 11→51、GeneralTree 7→48、Graph 5→41、SearchHash 4→39），D-001 §2 违规已修（快排/堆排真手写）。sanitizer + 泄漏专项变异有牙（树 2 条 LeakSanitizer、散列墓碑 2 条具名断言）。Claude 另修 `<optional>` 漏 include（只在 libstdc++ 现形）并**拦下第 1 章一处会印错的判断**（把 OCR 的 ∞→8 当成原书自相矛盾，实际原书是对的） |
| T-015a | 第11章索引 | Done | Codex | `dsa_raw.md` 第11章没有 `【代码】` 或 `【算法】` 清单，故不创建虚假台账条目；索引概念不影响 105 条清单等式 — **2026-08-18 清账**：原本与下面那条共用编号 `T-015`，两者是不同的事，已拆成 `T-015a`/`T-015b`。历史提交与交接里写的 `T-015` 若说的是「第11章索引」，指的是这一条。 |
| T-015b | 补 `ch03/queue` 与 `ch05/heap_huffman` 到 D-007 标准 | Done | Codex | 任务书 `collab/BRIEF-T015-queue-heap.md`。**两者的实现都没问题，缺的是证据**：queue 差 2 项断言 + legacy 需按体例补编译器输出；heap_huffman 的 legacy 需真的编译一次原书清单。顺带收口 `UNVERIFIED-RISKS.md` 第二节 — Claude 复核（2026-08-12）：**认可**。queue 7→36 项断言，变异 4/4 全抓（不取模→ASan 越界、满判据写错→具名断言、浅拷贝→ASan double-free）；`ensure_capacity` 的不可达 catch 已删并改记为有意的类型契约。Claude 另修自己引入的 bug：D-007 拦下了闸门自测的合成探针，**修法是让探针达标而非开例外** — **2026-08-18 清账**：原本与上面那条共用编号 `T-015`，已拆为 `T-015b`；任务书 `collab/BRIEF-T015-queue-heap.md` 说的是这一条。 |
| T-005 | 全书 292 张插图 vendoring + 逐张写图注 | Done | Claude | `tools/collect_figures.py` 从底稿抽出 292 张到 `book/assets/`，图册 `book/插图.md`。alt 用原书题注；无题注的标明底稿行号。教学关键图已嵌进 ch01/ch03/ch05/ch06/ch09/ch10。未逐像素人工重写全部 alt。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`collect_figures.py --check`（闸门第 10 步，D-020）：292 张图题注顺序逐项相符、文件名等于内容哈希。 |
| T-006 | 现代化风格公约（C++ 标准、命名、异常 vs 断言、允许用哪些 STL） | Done | 人 | **2026-08-12 人已拍板**，全文见 `collab/DECISION_LOG.md` 的 D-001。四条红线：C++17；STL 只做基础设施不做替身；容器内零 I/O、空状态用 `optional`、真错误抛标准异常；命名消除成员变量与成员函数重名。样板单元已按此重做并全绿 |
| T-007 | 原书勘误表：105 条清单里逐条标出「印刷即错」的部分 | Done | Claude | `book/勘误.md`：只收编译不过、跑起来错、书内自相矛盾三类，每条指向 `legacy.md`。OCR 噪声与「不够现代」不进表。第 1 章误判已作为「撤回」条保留。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`errata.py --check`：40 条，其中 15 条有回归断言撑着。 |
| T-010 | 接口口径：提取用 `optional`，按键删除用 `bool`；递归周游保留并标栈溢出风险 | Done | 人 | 2026-08-12 人已拍板，见 `DECISION_LOG.md` D-001 §3c/§3d。按位置越界仍抛 `out_of_range`；BST 删除不存在键返回 false |
| T-009 | 把 `Fragile` / `ThrowingMoveAssignment` / `AllocationFailure` / `CheapMove` 抽成共享的故障注入工具头 | Done | Claude | `code/support/fault_injection.hpp`（含 `Counted`，共 5 个探针，静态成员用 C++17 inline 变量，各带 `reset()`）。`check_code.py` 加了 `-I code/`。ch03 改用共享探针后断言数不变（58），ch02 直接复用 |
| T-008 | 5 条被 OCR 吃掉「结束」标记的清单需人工定边界 | Done | Claude | 5/5 已定，均不改 `dsa_raw.md`。代码5.8 → 4105（已有）；算法2.11 → 1617 围栏末；代码3.1 → 1817 围栏末；算法7.6 → 6189（图打断前最后一行代码）；算法7.9 → 6434 残片 `.9结束】`。依据见各单元 `legacy.md`。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`tests/test_ledger.py` 把 105 / 70 / 35 / 5 锚死，改一个数闸门就红。 |
| T-016 | 书稿后半改成第 1 章那种自解释、自包含教程 | Done | Claude | 人不改 `dsa_raw.md`（D-002）。第 5–12 章按 ch01 体例重写；第 6 章双份倾倒已删；第 9 章置换选择按图 9.2 重做并拆开赢者/败者树。本机 ASan 空探针仍挂，`check_code.py --allow-degraded` 19/19 Release 绿。请 Codex 复核置换选择冻结与败者树。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：R10/R11/R12 上线后节覆盖 178 节、登记 22 节、**待补 0**；同号节题名逐字一致。 |
| T-017 | 学生用带书签 PDF | Done | Claude | `python3 tools/build_book_pdf.py` → `book/pdf/现代C++数据结构教程.pdf`。B5，hyperref 书签 80+ 条（章/节），含 12 章正文与勘误附录 — 2026-08-15 随网页版一并重排：**364 页、17 章、291 张图**（当时的 126 页是插图与习题并入之前的数字），页数/章数/图数由 `build_book_pdf.py` 落进 `book/pdf/build-info.json`，网页版首页的下载卡片直接读它，不再手写会过期的数字 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`build_book_pdf.py --check`：454 页、17 个章级条目、292 图，`source_sha256` 覆盖正文与被引图片。 |
| T-018 | 新书目录对齐 2008 原书；保留原书没错的内容 | Done | Claude | 第 3 章两个 3.1.3 已拆开；第 5–12 章改回原书编号并补回概念节。第 1 章仍以传言问题开篇（原书 1.1），ADT/算法分析尚未整节补回。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：R10 + R11：原书 178 节要么有同号节、要么在 `section_gaps.json` 登记；同号必同题名。 |
| T-019 | 整节补回原书没错、新书还空着的内容 | Done | Claude | 已补 1.2–1.4、6.1.2–6.4、10.1.x/10.2/10.3.x、12.1–12.4 全部分节。无独立实现的节只写概念、不印示意代码。PDF 已重编。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`section_gaps.json` 的待补数 6 → 0（T-029）。 |
| T-020 | 新书的浏览器版（静态站点） | Done | Claude | `python3 tools/build_site.py` → `book/site/`，16 个页面，入口 `index.html`（双击即可读，或 `python3 -m http.server -d book`）。纯标准库、零 CDN：公式手写 LaTeX 子集转换、C++ 高亮在构建期完成。**HTML 是产物，Markdown 仍是唯一事实源**——闸门新增一步 `build_site.py --check`，书稿改了不重建就红。21 项自测，变异自检 5/5 全抓（含「高亮吃掉一个字节」与「站内锚点检查失效」）。全书 180 个代码块逐字比对渲染前后，一个字节都没变 — 2026-08-15 已上线 GitHub Pages：<https://gmyhf.github.io/dsa-modernization/>，由 `.github/workflows/pages.yml` 现场重建后发布（D-011），更新方式写进 README「怎么更新这本在线书」 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`build_site.py --check`：16 个页面逐字重渲染一致。 |
| T-021 | **教学版分层（D-012）**：正文印教学版，工程版降为「进阶（选读）」 | Done | Claude | 人于 2026-08-16 提出：「新书是教学使用，需要『教学版』数据结构代码……看不懂『工业级、工程级』的」。决策全文见 `DECISION_LOG.md` D-012。**第 1 批已落地**：`code/ch03/array_stack` + `code/ch02/array_list` 各加 `teaching.hpp` + `teaching_test.cpp`（34 / 43 项断言），变异自检 5/5 与 6/6 全抓；书稿 3.1.2 与 2.2 改成「完整实现 → 关键要点 → 进阶（选读）」三段式。闸门新增 `unit_programs()`：教学版与 **`demo.cpp`** 都进双档编译运行——`demo.cpp` 在此之前从来没被编译过（R3 只比对文本），32 个 demo 现已全绿。自测 +8 条（`TestTeachingAndDemoAreVerified`）。**第 2 批已落地**：`ch03/linked_stack`(32)、`ch03/queue`(81)、`ch02/linked_list`(51)、`ch02/doubly_linked_list`(50)，变异自检 6/6、6/6、7/7、6/6 全抓；书稿 3.1.3 / 3.2 / 2.3 同步改成三段式，`ch03/queue` 那段一行一个函数的挤压代码退到进阶。顺带修掉 3.2.3 一处已经**漂了**的 ```text 块（内容是旧版 `modern.hpp` 的 LinkedQueue，`dequeue()` 还是单行写法，现源码已是九行）——**R8 抓不到它，因为 R8 只认逐字相同**，块一旦漂开反而变哑。详见 `NOTES-claude.md`。**第 3 批已落地**：`ch04/string_class`(55)、`ch05/binary_tree`(74)、`ch05/heap_huffman`(44)，变异自检 7/7、7/7、6/6 全抓（heap 另有一条「合并时左右互换」的**阴性对照**，如预期不红）。书稿 4.2 / 5.3 / 5.4 / 5.5 / 5.6 改成三段式；5.6 那段 ```text 逐字倾倒 `modern.hpp` 的块一并换掉。**教学版 Huffman 构造发现真漏**：先检查边建结点，抛出时前面的叶子全漏，LeakSanitizer 当场报——改成「先把权重全查一遍再动手 new」，证据抄进书稿 5.6。**第 4 批改了结论**：逐块量过全书代码块后，算法类单元（排序、KMP、图、背包）印出来本来就是教学形态，剩下的只是几个 `[[nodiscard]]`——那是 D-001 §4 明文要求的，不为它改公约。**同轮量出的两处真问题已修**：`ch03/queue/modern.hpp` 每函数一行、最长 239 字符（纯格式重排）；`ch06/general_tree`、`ch07/adjacency_list` 判定不需分层并记录在案。**T-021 至此收口**：10 个单元有 teaching.hpp，覆盖表进了书稿前言。 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：10 份 `teaching.hpp` 全部进闸门双档编译运行；8 个「进阶（选读）」节在 R10 登记表里有据。 |
| T-022 | **课程课件逐章对照**（`ref_数据结构与算法A 2021秋` 的 19 份 .ppt/.pptx） | Done | Claude | 人于 2026-08-16 指派。课件转文字约 1.3 万行后与新书逐章比对，查出 6 条并全部修复。**最重的一条是台账造假**：`算法6.10` 被 `ch06/general_tree` 认领，但全仓库没有实现，`legacy.md` 还把它错记成「按秩合并」——它其实是**带双标记位先根次序树构造算法**（底稿 5420 行）。已补实现 + 5 条边界用例（含原书对空栈取顶的未定义行为）。**第二条是与原书分歧**：并查集用了按秩合并，原书【代码6.8】的 `nCount` 是**重量权衡**（按规模）；实测两者在同一组等价对上长出不同的树，已改回并补 tie-break（取自课程习题 8 原话）。**另四条是原书有、新书空**：满二叉树定理与推论、建堆效率 $O(n)$ 推导、Huffman 编码与译码、中缀→后缀转换（原书留作练习，本书补上）。ch06 断言 48→74，ch05/heap 44→69，ch03/expr 24→44；变异自检 6/6、4/4、5/5 全抓。详见 `参考资料说明.md` 的对照表 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：查出的 6 条全部修复且各有变异自检；对照表在 `参考资料说明.md`。 |
| T-023 | **新课件**：根据新书生成一套可讲、可改的课件 | Done | Claude | 人于 2026-08-16 指派，格式与范围由人拍板（D-013）：Markdown 源 + 网页版、先做样板章。`tools/build_slides.py` 零 CDN 自包含 HTML，方向键翻页、`N` 备注、`O` 总览、`F` 全屏、`D` 深色、浏览器打印即 PDF；复用 `build_site.py` 的公式与高亮，不重写第二套。**课件上的代码由 R3 逐字核对**——`book/slides/*.md` 落在 `book/` 下自动被收编。为此新增 `#fn:名字` 切片（`teaching.hpp` 故意不留锚点，见 D-012），判据落在「参数表右括号后是不是 `{`/`:`」上，避免把 `if (full())` 这类调用点当成定义。闸门加一步 `build_slides.py --check`；自测 20 条，变异自检 6/6 全抓。发布到 Pages 的 `/slides/`。**已做完 12 章、242 页、124 页带演讲者备注**。体例统一：要点 3–6 条 + 图 + 关键代码片段（`#fn:` 引用教学版），代码块过长的页一律改成手工演算的 trace——讲课要的是过程不是完整代码。顺带补上上一轮留下的开放问题：`MAX_SLIDE_LINES = 32` 的过满警告，数字是按投影字号量出来的（1080p 上约 28 行满屏），只警告不挡构建。自测 24 条，变异自检 6/6 + 3/3 全抓 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：`build_slides.py --check`：12 份课件、282 页；R16 逐节登记覆盖到具体页（D-023）。 |
| T-024 | **复查 2026-08-16 那一轮**（教学版分层 / 课件对照 / 新课件） | Done | Codex | 任务书 `collab/BRIEF-REVIEW-2026-08-17.md`，按风险排好了顺序。**P0 两条**：并查集换成重量权衡合并（唯一改了既有语义的地方，tie-break 是我加的口径）；`算法6.10` 此前被认领却没实现，已补但请复核是否忠实原书。**明确交出去的三条**：D-007 判不了「认领的清单真的被实现了」；R8 的判据只覆盖一半（块漂开反而变哑）；图与公式没有机器核对。生成 review 包：`python3 tools/handoff.py --from claude --to codex --base 34d94a6` | — **2026-08-17 复查回话，三条全部成立、已全部处理**：**P0** `算法6.10` 的收尾校验漏了 `has_sibling`，`{X, 无孩子, 有兄弟}` 这类不自洽序列被静默接受（`modern.hpp:176`）——已补判据 + 2 条回归用例，ch06 断言 74→76；**P1** 教学版「完全正确、扛得住 ASan」说过头了：`T` 的拷贝一抛，`array_stack`/`linked_stack`/`binary_tree` 三处裸指针实现会漏内存——写了探针 `collab/probe-teaching-leak.cpp` 复现（`88 byte(s) leaked in 5 allocation(s)`，LSan 栈帧正是 Codex 点的那三处），**改措辞不改代码**（加 try/catch 会把 D-012 的分层抵消掉），前言/ch02/ch03/ch03 课件的说法一律收窄为「内存分配与 `T` 的拷贝都不抛异常时」，边界写进 `UNVERIFIED-RISKS.md` 二a；**P2** 建堆 $O(n)$ 推导把满二叉树的理想模型当成一般等式——已改为先用高度 $h=\lfloor\log_2 n\rfloor$ 的满二叉树取上界再求和，并附 $\lceil n/2^{j+1}\rceil$ 的标准证法。交出去的三条开放问题，Codex 的判断已立成决策：D-007 补充条款（→ T-025）、D-010 补充条款（→ T-026）、**D-014**（图与公式的机器边界，新增 R9）。顺带修掉两处：`book/ch12-advanced.md:33` 的公式从写下来那天起就跨行、一直以原始 LaTeX 印在页上；`\Bigl`/`\Bigr` 渲染器不认识。运维两条也照办：`handoff.py` 现在逐步打 `[i/n] ▶ … / ✅ …（Ns）` 到 stderr（Codex 跑满 12 分钟没有任何阶段输出只好中断——实测全程约 5.5 分钟，其中 `check_code` 292 秒，大头是既有单元 bplus_tree 79s / binary_tree 75s / trie 41s）；`.DS_Store` 进 `.gitignore` — **2026-08-17 复查闭环**：Codex 确认闸门 9 步全绿、约 3 分 53 秒，逐步进度解决了可观察性问题，`check_code` 的 292 秒解释了此前中断而非死锁，验证保留项已消除。我交出去的那个问题也定了案（**D-015**）：算法6.10 的三类不自洽输入**不为课程标准答案放宽**——依据写进 D-015、书稿 6.3.2 与 `legacy.md` 缺陷 4。顺带修掉本章小结里一处与实现对不上的说法（「按秩合并」→「重量权衡合并」，实现早在上一轮就按原书 `nCount` 改成了重量口径）
| T-025 | **每条清单绑定实现锚点与测试名**（D-007 2026-08-17 补充条款） | Done | Codex 实现 / Claude 复查 | 105 条清单已迁移为 `{id, anchor, test}`；`check_code.py` 检查锚点分别存在于实现文件与 `test.cpp`，`ledger.py` 兼容对象台账并保留 104/105 + 1 退场等式。迁移脚本：`tools/migrate_t025.py`（**2026-08-18 已删**——一次性脚本，跑一次会批量重写全部单元的 `unit.json`，留在 `tools/` 里只是隐患；它做过什么、为什么被返工，这一行连同 `HANDOFF.md`、`NOTES-claude.md` 的记录已经写全）。工具单测通过。 — **2026-08-17 Claude 复查：机制对、绑定空，退回。** `3758dae` 把 105 条清单迁成了 `{id, anchor, test}`，`ledger.py` 双格式兼容、`check_code.py` 新增 `check_listing_bindings`，闸门在 Linux 上 9 步全绿、187 项自测通过、32/32 单元双档（Codex 那边 ASan 起不来只跑了 Release，这一档我这边补上了，迁移没有引入内存问题）。**但 104 条绑定没有一条指向该清单自己的实现或测试**：`migrate_t025.py` 的 anchor 取「源文件第一行非 `#` 的非空行」、test 取「第一行含 assert 的行」，再把同一个值赋给该单元**所有**清单——20 个单元里 17 个的多条清单共用一个 anchor，14 个的 anchor 是注释行，`test` 有 98 条是 `main`（恒真）。**变异自检直接重放了 T-025 要拦的场景**：删掉 `from_dual_tag` 的实现与全部用例、`unit.json` 仍认领【算法6.10】，`check_code` 61 项断言双档全绿、台账仍报 104/105 + 0 待办；唯一报红的是 R3，而 `#dual-tag` 这个锚点是补实现那次（`4fb7a2b`）才进书稿的——当年冒领时 R3 同样是哑的，且 R3 绑的是文件锚点不是清单编号（按一条清单一个锚点算，104 条里还差 25 条）。另有一处不吵的逃生口：`if not isinstance(entry, dict): continue`，裸字符串格式绕过全部绑定检查，而 D-007 明写「不设逃生口」。**返工验收判据**（写进 NOTES-claude 2026-08-17 那条）：① 同单元内 anchor/test 两两不同；② anchor 不得是注释行；③ **变异自检**：删掉任一条清单的实现，闸门必须为**那一条**报「实现锚点不存在」，而不是报编译失败；④ 去掉裸字符串逃生口 — **Codex 返工完成**：104 条逐条改绑真实定义符号与行为断言标签，同单元 anchor/test 全部唯一；闸门拒绝重复、注释锚点、裸字符串。新增算法6.10完整函数删除变异，编译前具名报其实现锚点不存在；算法3.7原来确实缺独立现代实现，补 `factorial_driver` + 行为用例。190 项工具自测全绿，台账一致；本机工程版 32/32 Release 通过，但全闸门因既有 9 个 teaching_test 触发 Clang `-Wself-assign-overloaded` 仅 23/32，ASan 仍为空探针环境失败，留给 Linux 双档复核。 — **2026-08-17 Claude 复查返工：通过。** 四条判据逐条量过：20 个单元的 anchor/test 全部两两不同（此前 17 个不满足）；104 条锚点无一命中注释行，且拿 `///` 注释当锚点能被具名拦下；裸字符串格式在 `check_code` 与 `ledger` 双双报红。**三组变异自检各自具名报红**：删 `from_dual_tag` 实现+用例 → 「算法6.10 的实现锚点/测试锚点不存在」；`shell_sort` 改名 → 「算法8.2 的实现锚点不存在」；删一条断言 → 「算法10.3 的测试锚点不存在」——实现侧与测试侧能各自单独变红，不是「编译不过」的另一种说法。普查 104 条：只在 demo.cpp 命中 0、测试锚点只在注释里 0、短于 12 字符 0。**复查中改掉两处**：① `代码10.4` 底稿是「集合 mySet 的 ADT」（类定义），却绑到 `bool erase(int value)`，而同单元 10.1/10.9 都按类绑、`class IntSet {` 空着——已改（这条机器判不出来，是对着底稿标题逐条核出来的，属 D-014 的人工项）；② 11 处 `x = x;` 改走引用别名，解掉 clang `-Wself-assign-overloaded` 在 `-Werror` 下拦住 9 个教学版单元的问题（Codex 本机因此只能跑 23/32）——断言数一条不差，且把拷贝赋值改成「先 delete[] 再拷贝」后该测试照样红，没有变哑；**本机无 clang，只验证了 g++ 上不坏，clang 侧待 Codex 确认**。**Linux 双档复核完成**：闸门 9 步全绿 `real 3m1.060s`，190 项自测 OK，**32/32 单元双档**（debug+ASan/UBSan 与 release-O2），Codex 那边挂着的 ASan 空探针一项不必再等。**欠账一笔**：新补的 `算法3.7` 是合格的驱动函数（底稿 2285 行确为「计算阶乘的主程序」），但原书那段的三处硬伤（`void main( )`、读了 x 却调 `factorial(4)`、缺右花括号）没进 `legacy.md`，按 D-007 第 2 条该补 → T-027 |
| T-026 | **禁「长的、形似 C++ 函数」的 ```text 块**（D-010 2026-08-17 补充条款） | Done | Codex 实现 / Claude 复查 | R8 已改为结构判定：至少 60 个规范化字符且含名字、参数表和函数体的 `text` 块一律报红，源码已经漂开仍能抓。原书引文必须写 `original-listing="具体理由"`，空理由报红；扫描 `book/**/*.md` 和 `code/**/legacy.md`。真实基线仅豁免两处原书引文；编译器输出改标 `console`，省略号语法与控制流 trace 不误报。新增漂移、豁免、空理由、trace、legacy 回归测试。 — **2026-08-17 Codex 已实施（`4210e09`），Claude 复查后补两处**。实施部分认可：判据从「像不像我们的源码」换成「长得像不像一段实现」，豁免语法 `original-listing="具体理由"`（空理由报红），范围扩到 `code/**/legacy.md`，全仓只需豁免两处原书引文——普查基线比预想的干净。**复查发现两个洞**：① **豁免能赦免「逐字抄自 code/」**——给逐字抄来的 `push()` 加一句 `original-listing="就想这么写"` 就从报红变放行，等于把 D-010 堵的口子换名重开；已订正为 `copied_from_source` 命中即报红，与有没有理由无关（豁免只赦免「像函数」，不赦免「是本书自己的代码」）。② **`operator=` 整个漏网**——名字正则只认 `名字(`，而 `operator=(` 的名字后面是 `=`；本书讲的正是三法则/五法则，最可能被抄成 text 块的恰恰是拷贝赋值运算符。已纳入 `operator=`/`[]`/`()`/`->` 一族。各配自测，另加一条**走 subprocess 的范围测试**（「扫哪些文件」只在 `main()` 里定，直接调 `check_r8` 的单测证明不了范围）。自测 215 → 220，变异自检 5/5 全抓 |
| T-027 | 补 `算法3.7` 的原书缺陷证据（`legacy.md`） | Done | Claude / Codex 收口 | 原书清单、`void main`、缺 `std::`、缺右花括号的编译证据，以及“读入 x 却调用 factorial(4)”的语义矛盾均已记录；现代 `factorial_driver` 有绑定测试。编译器输出围栏已正名为 `console`，T-026 的 R8 也会扫描本文件。 |
| T-028 | **编号漂移普查**：同号不同题的节 | Done | Codex 实现 / Claude 复查 | 最新书稿普查出 29 处同号标题差异：18 处为措辞/排版，11 处为真实换题（`2.2.x`、`2.3.x`、`4.2.x`、`5.2.1`、`7.4.x`）。已全部恢复原书编号题名，本书教学步骤改用无编号标题或字母后缀；新增 R11，规范化只忽略排版差异，不做模糊语义匹配。同号异题变异会具名报红。恢复后暴露 `2.3.3 循环链表` 确实缺失，登记为第 6 条 pending（→ T-029）。决策见 D-017 — **2026-08-17 Codex 已做，Claude 复查通过**：`5a12f21` 把被占用的编号还给原书（4.2.1 还原成「字符串的顺序存储」、2.3.2 还原成「双链表」等），教学步骤降为不带编号的 `####`，进阶节改成 2.2a/2.3a/4.2a，并新增 **R11**（同号必须同题，规范化后精确比较，不做模糊匹配）。实测变异：把 5.2.1 的题名改回「先跑一遍」，R11 当场报「同号不同题」。**顺带逼出一笔真债**：原来的「2.3.3 插入与删除」占着号，R10 因同号而以为已覆盖，还原编号后才暴露原书 2.3.3 循环链表整节没写（Codex 已登记为 pending，待补数 5 → 6）。**复查中补了两处**：① 改编号留下 **9 处悬空引用**（「后面 2.2.1–2.2.4 各节」「见第 2.3.2a 节」「见 4.2.5」「见 11.6 的四条练习路径」…），R6/R7/R10/R11 都不看这一层——已新增 **R12**（D-017）并清掉全部悬空引用，另有 12 处「光写数字不带『节』字」的过期引用手工清理（含 3 处在 `teaching.hpp` 注释里、会随 R3 印进书稿，以及 `doubly_linked_list/test.cpp` 里 40 条标着「2.3.4」的断言标签 → 2.3.2）；② `### 4.2.2` 下紧跟同级 `### 教学版：完整实现`，导致 4.2.2 正文为空——已把教学版降为 `####` 并补上正文，全书扫过同类问题只此一处 |
| T-029 | 补 6 个 `pending` 节 | Done | Codex 实现 / Claude 复查 | 6 节均已按原书编号补回：2.3.3 讲循环链表不变量与边界；4.1.1/4.1.2 以现代 UTF-8/码元口径纠正原书过时叙述；5.5.2 说明优先队列接口、复杂度并接回已验证的 `MinHeap`；7.3.3 讲十字链表的双链不变量；11.4.4 给出静态/动态索引的工作负载选型表。`section_gaps.json` 的 6 条 pending 已清零。 — **2026-08-17 Codex 已补齐 6 节，Claude 复查通过**（`3fc9030`）：待补 6 → 0。核过三件事：十字链表用的是原书域名（`tailnextarc`/`headnextarc`/`firstinarc`/`firstoutarc`，底稿 6106 行逐个对过）；5.5.2 对本书 `MinHeap` 的接口描述（`insert`/`remove_min`/`std::optional`）与源码一致；这六节在原书里都不含清单，所以纯文字合规、不欠 R3 的代码块。**ASan 档在 Linux 上补跑**：完整闸门 exit=0，32/32 单元双档，Codex 那边的 Release-only 降级可撤。**复查中补两处**：① `0--127`/`1--4`/`` `0`--`9` `` 是 LaTeX 破折号写法，Markdown 不认、站点原样印出两个减号，已改；② **`"123" < "1234" < "23"` 只对 `std::string` 成立**——两个字符串字面量之间的 `<` 比的是数组地址，实测 `("123" < "1234")` 为 **false**，g++ 报 `-Warray-compare`；照书上那行敲进编译器会得到相反答案，已在 4.1.2 补实测输出与说明。**记一笔没做的**：全书 `“”` 与 `「」` 混用（94 : 758），不统一是因为不少属于引用原书（插图/勘误/习题答案），一刀切会伤引文 |
| T-030 | **第 8 章补课**：把原书有、新书空着的 7 节写出来 | Done | Claude | 人于 2026-08-17 拍板。原书 19 节，新书原本只有 12 节：8.3.1 直接选择排序、8.4.1 冒泡排序、8.6.1 桶式排序、8.6.3 索引排序整节没写，8.7 只有一段话（原书是 8.7.1/8.7.2/8.7.3 三节），8.2.1 被改题成「先跑一遍」——而这三种排序的实现、测试、清单认领**一直都在**。现已补全：全章 276 → 597 行，19 节齐备。新印的代码全部走 R3（`#fn:` 切片 + 新增 `#static-queue`/`#stopwatch` 两个锚点），**此前从没印进书的 6 条清单**（算法8.2/8.7/8.9/8.13、代码8.12/8.16/8.17）这次都印出来了。手工演算 trace 是**跑出来的**不是手编的：Shell/选择/冒泡三张表由与实现同逻辑的脚本生成，选择排序那张与原书图8.3 逐行一致；索引排序那组算出来的 IndexArray2 与原书表8.1 逐格相同（顺带发现底稿把置换环印成 {0,5,4,1}，与它自己列的元素对不上，实为 {0,5,4,2}，多半是扫描损坏，已在正文注明）。8.7.2 的时间表是**本机实测**（g++ 13.3 -O2）：5 万随机数，冒泡 5821.9ms、插入 213.5ms——同为 Θ(n²) 差 27 倍；2 万已排好序的输入，优化快排 105.9ms 而插入 0.0ms——**快排在这种输入上比 Θ(n²) 的插入排序还慢 100 倍**。8.7.3 补了判定树下界 Ω(n log n) 的证明 — **2026-08-18 清账**：产物齐、闸门在管，状态 Review → Done。判据：ch08 19 节齐（R10 无缺口）；`slide_coverage.json` 里 ch08 的 19 条全部登记到课件页。 |
| T-031 | **后半本体例拉齐：第 8–12 章补讲解、图和演算** | Done | Codex 实现 / Claude 复查 | 分章交付，避免大提交难复查。基线行数：ch08 742、ch09 312、ch10 481、ch11 505、ch12 478；第 8 章已由 T-030 补齐。**第 1 批 ch09 已完成**：补 I/O 成本模型、页内记录组织、外排序两阶段、置换选择逐步表、3000 条记录二路归并 4 趟演算、四路选择树完整 trace、稳定性与失败边界；复用原书图 9.1、9.3–9.8。**第 2 批 ch10 已完成**：补二分区间 trace 与决策树、分块检索原书实例、集合位表示、散列函数取舍、拉链结构、墓碑状态演算、装载因子曲线与结构选型；复用原书图 10.1–10.3、10.5–10.7、10.11，并纠正空集成员关系/子集关系的歧义。**第 3 批 ch11 已完成**：补 1000 条记录四层索引演算、多分树扇出、倒排双指针与短语位置、B+ 树分裂/删除路径、位图逐位查询、签名假阳性和结构选型；复用原书图 11.1–11.4、11.9–11.11、11.15–11.16。**第 4 批 ch12 已完成**：补多维地址换算、三角压缩、十字链表双链更新、广义表头尾与共享、句柄复用、空闲块合并、无根环回收、Patricia 分歧位、最优 BST 区间依赖、AVL 四旋转与伸展树均摊边界；复用原书图 12.1、12.2、12.5、12.6、12.10–12.14、12.17、12.20、12.27、12.31、12.39–12.40。**跨章复查完成**：统一核对 8–12 章导读、运行示例、演算/表格、插图、总结和习题结构；发现第 8 章缺少正文插图，补入图 8.1、8.6、8.14。全项目文档闸门、站点一致性和 220 项自测通过。 — **2026-08-17 Claude 复查通过**（`6b40836`…`c4726d7`，五章 +1355 行）。抽查的数字**逐条独立复算过**，全对：ch12 最佳 BST 那张 10 格表（用同口径的 DP 重算，`w`/`c`/根全部吻合，末格 `10+19+28=57` 与 `demo` 输出一致）；ch12 的 AVL 四种失衡（`3,2,1`/`1,2,3`/`3,1,2`/`1,3,2` 实测高度都是 2、中序都是 `1 2 3`，即旋转确实发生）；ch10 的顺序检索「返回第一个匹配」与二分的半开区间 trace 与 `modern.hpp` 逐行对得上；ch11 的四层索引算术（250→63→16→4→1）与扇出估算（4096/16 减页头≈250）自洽；ch10 位向量例子 `A={a,c,f}=00100101` 等五行按位运算全对。42 张插图 alt/本地文件齐全。**复查中改一处 + 加一条规则**：ch09 表里 `5--8` 是 LaTeX 破折号，Markdown 会原样印出两个减号——这是同一习惯第三次出现（前两次在 ch04 与习题答案），已修并新增 **R13**（D-018）把它变成判据 |
| T-032 | 修复课件索引页章节卡片被撑成整屏高度 | Done | Codex | 根因是放映容器与索引卡片共用 `.deck` 类，前者的 `height: 100vh` 泄漏到每张卡片。索引卡片改用 `.deck-card`，新增生成 HTML 回归测试，重建 13 个课件页面。 |
| T-033 | 课件追平书稿新增内容 | Done | Codex | 课件 242 → 274 页。补入 2.3.3、4.1.1/4.1.2、5.5.2、7.3.3、11.4.4；第 8 章补直接选择、冒泡、桶式、索引排序与 8.7 三节；第 9–12 章补页 I/O、二分/墓碑、多级/倒排/B+ 树、数组/十字链表/回收/BST/AVL/伸展树等可复算过程。新增课件覆盖回归测试。 |
| T-034 | PDF 内容哈希 sidecar、陈旧闸门与全书重建 | Done | Codex 实现 / Claude 复查 | `build-info.json` 新增全部 PDF 构建输入的 `source_sha256` 与派生的 `main_chapters`；`build_book_pdf.py --check` 进入交接闸门和 Pages 发布闸门，书稿或排版脚本改动而 PDF 未重建会报红。PDF 重建为 450 页、17 个章级条目（12 章正文）、292 张图；`pdfinfo`、sidecar、首页卡片三方一致。 — **2026-08-17 Claude 复查：机制对，但摘要漏了插图，已补**。`source_sha256` + `--check` 的形状与 `build_site --check` 同构，闸门第 9 步已就位，实测「书稿改一个字」立刻报过期。**漏的一类是插图**：292 张图是嵌在 PDF 里的，而第一版摘要只覆盖 `.md` 与 `preamble.tex`——给 `assets/f23f55c21cffd0b7.jpg` 尾部追加两个字节，`--check` 照样报「与源文件一致」。现已把各章引用到的本地图片一并计入（远程热链不算，它进不了离线 PDF），3 条自测 + 变异自检守着，自测 227 → 235。**另记一件必须知道的事**：PDF 的字节与页数**依赖构建环境**——同一份源文件，Codex 在 macOS 上是 450 页（Times New Roman + Songti SC），我在 Linux 上是 436 页（preamble 的回退链：Liberation Serif + Noto Serif CJK）。两份都合法，判据是 `source_sha256`，**不是页数、也不是文件字节**，两个 agent 不必为二进制差异互相追平。已按新摘要重建并入库 |
| T-035 | 派生产物新鲜度盘点 | Done | Codex 实现 / Claude 复查 | 清单在 `collab/ARTIFACT-FRESHNESS-2026-08-17.md`，把「当前一致」与「持续防漂移」分开记，结论诚实：网站/课件/PDF 绿色，课件内容覆盖、习题答案、插图集黄色。**Claude 复查：清单里的数字逐条独立复算过，全部吻合**——292 个 assets 文件的文件名全部等于内容 SHA-256 前 16 位（0 例外）；图册 292 处引用无重复、无缺文件、无缺 alt；底稿远程图片 URL 去重后 292；全书本地图片引用 334 处指向 292 个文件、无缺失；习题答案 271 行 / 12 章齐全。**另验了一件它没说满的事**：我试着用「编号匹配」和「标题关键词匹配」去量课件覆盖，分别得出 53 和 45 个「未覆盖小节」——**全是假阳性**（Prim 在 ch07 课件出现 6 次、KMP 在 ch04 出现 11 次、顺序表在 ch02 出现 10 次）。这反过来印证了清单的判断：课件是教学改写，**覆盖登记只能是显式声明（照 `section_gaps.json` 的形状），不能是匹配器**——否则第一次运行就吵几十条假警报 |
| T-036 | 插图集离线闸门（清单建议的第 1 项） | Done | Claude | `collect_figures.py --check` + `book/assets/figures.json` sidecar，见 D-020。不联网：判据是「底稿现在抽出的题注与顺序」「图册引用顺序与 alt」「文件名 == 内容 SHA-256 前 16 位」「没有孤儿文件」。闸门第 10 步落位（现共 12 步）。自测 6 条，变异自检 4/4 全抓。实测三种变异都当场报红：换掉一张图的一个字节 → 「内容与文件名对不上」；图册删掉一张 → 「顺序/内容不一致」；sidecar 题注改一个字 → 「题注变了」 |
| T-039 | **课件覆盖登记 R16（D-023）** | Done | Claude | 新鲜度清单最后一项黄色收口。`collab/slide_coverage.json` 逐节登记正文 165 节 → 课件页：covered 142、引子 9、有意不进课件 7、欠着 7。页标识用**页标题**不用序号（序号会因插页整体错位；标题一章内唯一，有自测守着）。**不做匹配器**——按节号/关键词两种自动匹配实测得 53/45 条「未覆盖」，抽查全是假阳性。顺带把 ch03 的 `3.1.3a`、`3.2.3a` 从 `####` 对齐成 `###`（此前从没进过任何节清单），正文小节 163 → 165。变异自检 5/5。**欠着的 7 节**：1.3.1、1.3.2、1.4.4、6.1.3、7.2、10.3.6、11.2 —— 见 T-040。 |
| T-040 | **补上欠着的 7 页课件** | Done | Claude | R16 登记为 pending 的 7 节：1.3.1 算法四性质、1.3.2 算法设计方法、1.4.4 结构选择取舍表、6.1.3 树的 ADT 运算表、7.2 图的 ADT 运算表、10.3.6 什么时候不该用散列、11.2 静态索引（多分树/溢出区/重组）。2026-08-18 全部补齐，共写了 **8 页**（11.2 拆成「多分树」与「扇出是算出来的」两页）。登记表 7 条 pending 全改 covered，闸门的「欠着」**7 → 0**；课件 274 → 282 页，密度闸门无告警。ADT 那两页的接口名由 R15 逐个校验（`GeneralTree::parent_of`、`Graph::topological_sort` 等）。 |
| T-041 | **闸门的 Python 臂（D-025 §2）** | Done | Claude | 四处对等改造：`check_code.py` 发现 `modern.py` + `test.py` 就跑两档（默认档 / `-X dev -W error`）；新增 `check_d025` 禁实现里出现 `sorted`/`list.sort`/`heapq`/`bisect`/`print`（可在 `unit.json.d025_exceptions` 写理由豁免）；`check_doc.py` R3/R8 扩到 ```python，锚点用 `# >>> 名字`；`unit.json.listings[]` 新增 `py_anchor`/`py_test`/`py_skip`——有 `modern.py` 的单元每条清单必须给出其一（**该字段 2026-08-18 按 D-026 改名为 `py_code_line`**，见 `9b7fd93`；此处保留原名是当时的记录）。配套自测（check_code +10、check_doc +13、build_site +5）。变异自检 9/9：`import heapq`、`values.sort()`、抽掉 py_test、空 py_skip、删 test.py、书稿改一个字、relabel 成 ```text、file= 指向 .hpp、不写 file= —— 全部判红。顺带补了 `read_python_function` 的活缺陷（参数表跨行 + 闭合括号顶格会丢掉函数体，构造用例才发现的，从没被现有书稿触发）。  — **2026-08-20 清账**：产物齐、闸门在管。判据（2026-08-20 实测）：闸门第 10 步每轮跑 **11 个 Python 单元 × 2 档**（py-default / py-dev）；`check_d025` 与 `d025_exceptions` 在 `check_code.py` 里活着；`check_doc.py` 的 `ALLOWED_LANGS` 含 `python`，`.py` 切片走 `PY_DEF_RE`；书稿 8 章共 **37 段 ```python 全部带 `file=`**，逐字受 R3 管。 状态 Review → Done。 |
| T-042 | **试点章：第 8 章排序双实现** | Done | Claude | `code/ch08/sorting/modern.py` + `test.py`，覆盖算法8.1–8.11、8.13–8.15 的 Python 版；基础设施类清单（代码8.12 StaticQueue、代码8.16 随机数、代码8.17 Stopwatch）走 `py_skip` 并写明理由。书稿 `book/ch08-sorting.md` 每个算法后面加 ```python 块，由 R3 逐字守。这一章是判据：若闸门的 Python 臂在这里不成立，D-025 §2 要回炉。**2026-08-18 落地**：14 条认领 Python + 3 条 py_skip，Python 侧 129 项断言，两档全绿；书稿 11 处插入点，网页版补 Python 高亮。变异自检 6/6（8.7 不挑短侧 / 8.11 不整体平移 / 归并 `<`→`<=` / 8.1 去掉 hole>0 / 8.5 删提前结束 / 8.4 筛选不挑较大孩子）。三处语言差异有断言钉着：递归深度、定长整数、稳定性可观测性。  — **2026-08-20 清账**：产物齐、闸门在管。判据：`code/ch08/sorting/{modern.py,test.py}` 在册，Python 侧实测 **134 项断言**两档全绿（落地时 129，涨在 T-047 的共享用例上）；`book/ch08-sorting.md` **10 段 ```python 全部 `file=` 引用真实切片**（备注原写「11 处插入点」，今日实测 10，以实测为准）。D-025 §2 没有回炉。 状态 Review → Done。 |
| T-043 | **算法侧其余章的 Python 实现** | Done | Codex | 按 D-025 §1 的算法侧清单推进：4.3 KMP、第 7 章图算法、3.1.5 背包、第 9 章外排序、第 10 章检索、第 11 章索引、第 12 章 Trie/最优 BST。逐章一个提交，每章跑完整闸门。存储侧**不做**——正文改为写明「这里为什么不给 Python」。**2026-08-18 Codex 交付 12 个单元**（3.1.5 背包、4.3 KMP、第 7 章图、第 9 章外排序、第 10 章检索、第 11 章索引×4、第 12 章 Trie/最佳 BST），覆盖面比交接摘要里说的还大。**复核未通过，返工见 T-048**：本机闸门 28/32，另有三处结构被抽空、416 处风格问题、两处断言数写死。  — **2026-08-20 清账**：交付面已被 T-048 收口，不再单独挂着。判据：**11 个单元有 Python 实现 + B+ 树单元级 `py_skip` = 12**，与交付时说的 12 个对上；`listings` 里 `py_code_line` 47 条、清单级 `py_skip` 4 条，有 `modern.py` 的单元每条清单都给得出其一（这是 T-041 立的判据）；存储侧「为什么不给 Python」的正文在 `数据结构与算法.md`、`ch11-index.md`、`ch12-advanced.md` 里写着。 状态 Review → Done。 |
| T-044 | **存储侧正文补「为什么这里没有 Python」** | Done | Codex | 在 2.2/2.3、3.1/3.2、4.2、5.3–5.6、12.2/12.3 各补分节说明：Python 容器会隐藏布局、所有权、三/五法则和强异常保证；12.3 明确区分 Python 算法行为对照与 C++ 存储教学。 |
| T-045 | **课件补书之缺：树状数组、单调栈** | Done | Codex | 新增 `code/ch12/fenwick`、`code/ch03/monotonic_stack` 的 C++/Python 双实现、共享用例、独立暴力对拍与 legacy 证据；课件新增 4 页并在 R17 `pages` 中登记为 `extra`。两单元 Release + Python 两档通过，ASan 受本机环境限制。 |
| T-046 | **更名《数据结构与算法：Python 讲算法，C++ 讲实现》** | Done | Claude | D-025 §4。波及 24 个文件：`book/现代C++数据结构教程.md` 改名、`tools/build_site.py` / `build_book_pdf.py` / `build_slides.py` 的硬编码、`tests/test_build_site.py` 等 3 个自测、12 份课件页眉、README。计划是**单独提交**，实际**没做到**——`tools/build_site.py` 同时装了 Python 高亮（T-041）与新书名两处改动，拆开会让任一个提交上的闸门是红的（旧名的提交里站点已按新高亮构建）。理由记在这里，不假装守住了。**2026-08-18 落地**：`book/现代C++数据结构教程.md` → `book/数据结构与算法.md`，PDF 同名改；三个构建器的硬编码与 `tests/test_build_site.py` 的 `PDF_HREF` 一并改；12 份课件的 `subtitle` 与 `slides/README.md`、根 README 全部跟上。历史记录（HANDOFF、REFERENCE-REVIEW、ARTIFACT-FRESHNESS 里的旧名）**有意不改**——那是当时的记录，改了就不是记录了。重出：课件 282 页、PDF 462 页、站点 16 页。  — **2026-08-20 清账**：产物齐、闸门在管。判据：`tools/build_site.py` / `build_book_pdf.py` / `build_slides.py` 三个构建器、**13 份课件的 `subtitle`**、README 全是新书名；`tools/`、`book/`、`tests/` 里旧名 **0 处**（`HANDOFF` 等历史记录里的旧名有意保留——那是当时的记录）。备注里的「PDF 462 页」是当时的数，今日 480 页，由 `tests/test_readme.py` 现场核算（D-031）。 状态 Review → Done。 |
| T-047 | **跨语言差分：共享用例表，两种语言各跑一遍** | Done | Codex | 2026-08-18 落地，见 D-028。11 个双实现单元各有五列 `cases.tsv`，C++ / Python 共读同一表并各报 `共享用例: N`；闸门独立核对两侧 N 均等于有效表长，缺表、坏表、未知异常类别、漏报、错报都红。共享常量用 `operation=constant` 入表；异常使用语言中立的 `invalid_argument` / `out_of_range`。B+ 树按既有单元级 `py_skip` 排除。验收变异实测：只把 Python 排序阈值 `10_000_000` 改为 `9_999_999`，C++ 绿而 Python 两档均报 `FAIL: T-047 counting-limit`，单元 `0/1`；恢复后通过。  — **2026-08-20 清账**：产物齐、闸门在管。判据：**11 份 `cases.tsv`** 在册（与 11 个双实现单元一一对应）；闸门每轮打印 `✅ [改坏用例表] 两侧都随之变红（3 条共享用例）`——这条自检本身就是 D-028 的验收面。 状态 Review → Done。 |
| T-048 | **T-043 复核与返工（D-026）** | Done | Claude | 本机闸门实测 **28/32**，与交来的「已达可提交标准」不符。三类问题：① **13 条机械问题**——`py_anchor` 指向 `# >>> 名字` 切片标记而非真实代码行（5）、锚点/测试名重复（3，两条清单绑同一个测试等于其中一条没被验）、ch07 断言 18\<30、ch09 8\<9；② **三处结构被抽空**（B+ 树是个有序 list、`WinnerTree` 类体只有一句 docstring、Patricia 靠公式报指标），闸门看不见——禁用名单管不到用 list 手写的空壳，且这些单元 `listings` 为空导致密度判据整条不适用；③ **416 处风格问题**（`;` 打包、单行复合语句、`a=b`），而这些字节按 R3 要逐字印进书里。处置按人拍板的混合方案：竞赛树与 Patricia 真写（ch09 断言 8→30、ch12/trie 12→47），B+ 树按 D-025 §1 走**单元级 py_skip**（新增机制）并在正文写明理由。新增三条闸门判据：D-026 风格三条、断言数不得写死、无清单单元也有 Python 密度下限。另：交接称本机 ASan 起不来（引 `sanitizer_malloc_mac.inc`，那是 macOS 路径），实测 `sanitizer_preflight()` 返回 True——这批代码此前**从没在 ASan/UBSan 下跑过**，本轮补跑 32 单元无新发现。  — **2026-08-20 清账**：三处返工都还在。判据（实测断言数）：ch09 外排序 **33**（返工前 8）、ch12/trie **50**（返工前 12）、ch07 图 **36**（返工前 18），三处都在 D-026 立的密度线以上；B+ 树的单元级 `py_skip` 带理由写在 `code/ch11/bplus_tree/unit.json`，正文 `ch11-index.md` 有对应的一段说明。 状态 Review → Done。 |
| T-049 | **`anchor` → `code_line`：两侧命名对齐（D-027）** | Done | Claude | 人拍板把 C++ 侧也改了。范围：`check_code.py` + `ledger.py` 的键与全部报错文案、**20 个 unit.json 共 104 条清单**、两个测试夹具。`build_site.py` 里的 `.anchor` 是网页标题锚，未动；`// >>> 名字` 切片标记未动——它本来就该叫锚点。报错文案一并改成能自己说清要什么（「填 `// >>> 名字` 是最常见的填法错误，这里要的是实现里的一行真代码」）——改名换的是字段名，绊人的是报错时读到的那句话。实测残留旧键名会被 `check_code` 与 `ledger` **各自独立**判红，不会静默通过。批量改写逐文件比对 JSON 语义：104 条清单零差异。过程中第一版脚本把 `\n` 写成字面两字符，20 个 unit.json 尾部多出三个字节，`ledger.py` 当场报「不是合法 JSON」——批量改写必须有独立解析器复验，这条记进了 D-027。  — **2026-08-20 清账**：改名已经全面落地。判据：**104 条清单全部用 `code_line`**，`anchor` / `py_anchor` 残留 **0 处**；Python 侧 `py_code_line` 47 条。残留旧键会被 `check_code` 与 `ledger` 各自独立判红这一点，在 D-027 里记着。 状态 Review → Done。 |
| T-050 | **5.2 周游：把课件讲了而正文没讲的补回正文** | Done | Claude | 人指出课件 ch05 第 9 页「表达式树：周游的一个用途」在正文找不到对应内容，邻近的 5.2.3 只有一句话。查证：原书 5.2.2 里的图 5.5 三条序列、图 5.6 表达式树与三种表达式、5.2.3 的层次周游全过程，新书**整段没写**——`slide_coverage.json` 把 5.2.2 登记成「已被表达式树那页覆盖」，而 R16 只验「登记了没有、页在不在」，不验正文写没写，所以一直是绿的（这是 R16 已写明的人工复核项，不是漏洞）。补法：5.2.2 补三种次序的由来（6 种排列→3 种）、图 5.5 的前/中/后序三条序列与前驱后继、三段递归切片、表达式树小节；5.2.3 补层次周游次序、伪码、「为什么不能写成递归」、`level_order` 切片与空间代价。顺带修掉正文 5.1.1 一处**说错的图号**（把 5 结点的示例树说成图 5.5，而图 5.5 是 9 结点那棵）。工具侧见 D-029。 — **2026-08-19 追记**：上一轮留的两个未决都做了。① 反向覆盖判据 → T-051（R17 / D-030）；② 原书上机题第 2 题「表达式二叉树」已补进本章上机题（第 5 题）并配同号答案，正文 5.2.2 的引用改成「本章上机题第 5 题（原书第 2 题）」。  — **2026-08-20 Codex 复核通过**（`ae61bf7`：核心更正成立，无阻塞项），状态 Review → Done。判据：5.2.2/5.2.3 正文与三段 R3 切片在闸门里逐字受管，图 5.5/5.6 与三条序列进了 PDF 与网页版，上机题第 5 题与同号答案由 R14 守着（137/137）。 |
| T-051 | **课件页反向归属闸门 R17（D-030）** | Done | Claude | R16 只管「正文 → 课件」，T-050 的病在反方向：课件多讲一页而正文没有，登记表照样满分。R17 要求每一页课件只有一个出处——被某一节 `covered` 认领（自动，245/282 页），或在 `slide_coverage.json` 的 `pages` 里登记为 `frame`（章首/小结/上机，37 页）或 `extra`（课件独有＝一条债，当前 0 条）。手工登记只 37 条。**边界写死在 D-030**：它守孤儿页，不守「正文配不配得上登记的那几页」——用正文字数当判据的实测已附（真债 70 字 vs 完整短节 61/65/66/68 字，分不开）。变异自检 2/2 全抓，+8 项自测。  — **2026-08-20 Codex 复核通过**（同上），状态 Review → Done。判据：282 页课件页页有出处（正文认领 245 + frame 37 + extra 0），R17 与 8 项自测在闸门第 5 步每天跑一次。 |
| T-052 | **复核回应：网页版把链接文字渲染没了 + README 数字过期** | Done | Claude | Codex 复核 `ae61bf7` 指出根 README 的「现状」数字过期（16 个文件 / 16 条规则 / 301 项自测，实际 29 / 17 / 357）。顺着核对，`build_site.py` 里查出一处**已经上线的真缺陷**：占位符还原是从前往后做的，而占位符会嵌套——`[`名字`](链接)` 这种写法，链接的标记里裹着行内代码的占位符，内层编号一定比外层小，顺着还原时外层还没展开、内层那个编号已经被跳过去了。结果是首页两条「递归深度风险见 `collab/UNVERIFIED-RISKS.md`」和习题页一条外链**渲染成空链接**，页面里留下 4 个 NUL 字节；NUL 在浏览器里不占宽度，肉眼只看到「见 。」，而构建、`check_doc`、`build_site.py --check` 全绿（PDF 走另一条渲染路径，没这个病）。改法：还原改成倒着做；行内图的 `alt` 另走「还原成书稿原文」，否则倒着还原会把 `<span class="math">` 塞进 alt 属性、引号当场顶断。README 的数字不再手抄——`tests/test_readme.py` 现场从台账、`check_doc.RULES`、`code/*/*/unit.json`、PDF sidecar、课件页数与测试发现数算出来比对。变异自检：渲染 2/2、README 6/6 全抓，自测 346 → 357。  — **2026-08-20 Codex 复核通过**：357 项自测全过；手工探针确认 `[`x`](u)` 渲染为 `<a href="u"><code>x</code></a>`；嵌套链接/行内代码/公式/图片 alt 均不再产生 NUL；README 现场核算测试有效。**唯一遗留是环境项**——复核方机器上 ASan 自检仍在 `sanitizer_malloc_mac.inc:189` 失败（D-006 早已记名的 macOS 问题，非本次回归），所以那一侧的 sanitizer 覆盖不计入已复验。状态 Review → Done。 |
| T-053 | **T-044 / T-045 复核：补跑 sanitizer，修两处自相矛盾的小节标题，堵 C++ 侧密度洞** | Review | Claude | 对 `0e3e3d8` 的复核。**本机（Linux, gcc 13.3）全闸门带 sanitizer 跑通 34/34 × 2 档**——交付方那侧因 `sanitizer_malloc_mac.inc:189` 只跑了降级档，两个新单元此前从没在 ASan/UBSan 下跑过，现已补跑，无新发现。两个新单元另做变异自检：树状数组 3/3（前缀走法、半开区间、越界判据）、单调栈 3/4（严格性、左边界、负高度全红；直方图弹出条件 `>` 改 `>=` 不红——**穷举 5461 个直方图证明这是等价变体**，不是用例漏了）。**两处修掉的真缺陷**：① `book/ch03-stack.md` 的「为什么这一节没有 Python 版」挂在 `## 3.1 栈` 上，而 3.1.5 背包就在同一节里印着三段 Python——辖域错了，改成「为什么顺序栈和链式栈只有 C++」并写明 3.1.5 是算法侧；② `book/ch12-advanced.md` 12.3 同名标题被它自己的正文否掉（正文写着「Python 版本保留」，本章小结确实印着 Trie 的 Python），改成「为什么这一节的存储侧只有 C++」。**顺手堵一个闸门洞**：`check_substance` 的 C++ 密度判据此前对 `listings` 为空的单元整条不适用（Python 侧同一个洞 T-048 补过），14 个 beyond_book 单元全在下限之上，堵它不弄红任何一个，+3 项自测。**留给人拍板的一条见 NOTES**：`code/ch03/monotonic_stack` 在栈这一章用 `std::vector` 当栈。 — **2026-08-20 人拍板 D-033**：单调栈是算法侧，`std::vector` 当栈可以，这一条复核意见到此了结；`unit.json` 的豁免理由按决定改写成与代码相符的说法。 |
| T-054 | **原书内容回填：正确内容、完整原图与历史更新说明** | Done | Codex | 用户要求：原书中正确的文字与图尽量进入新书；OCR 错拆的同一原图可还原为完整图号，但原本独立的整图不切分再拼接；经典内容保留并说明适用边界，过时接口以现代方案替换且明示差异，确无教学价值的内容保留原因。恢复 9.2.1 文件组织与 9.2.2 C++ 流文件，并补课件、R10/R16/R17 追踪；恢复被旧提交误删的前言原图。验收：原书 178 节均有正文或带理由登记、292 张图逐项哈希相符、书稿/网站/课件/PDF 全部新鲜。 |
| T-055 | **7.3.3 十字链表：补字段级双链追踪** | Done | Codex | 根据读者反馈，在保留原图 7.15 的前提下，补出单个弧结点同时进入出边链与入边链的指针走向、四个链接字段的职责，以及 $0\\to1$、$0\\to2$、$3\\to2$ 的双向遍历结果；课件新增一页并由 R16/R17 登记。验收：书稿、网站、课件与 PDF 全部重建，课件页数 394 → 395。 |
| T-056 | **7.3.3 十字链表：原书式教学版与现代实现对照** | Done | Codex | 按读者要求保留易读的传统 `ArcBox` / `VexNode` 表示和表头挂接过程，逐行解释；紧接由自动所有权管理的可编译实现，测试两条链和双链删除。教学版 50 项、现代版 12 项行为断言；书稿、网站、课件与 PDF 已重建。 |
| T-057 | **原书扫描件进项目：逐字出处 + 正文保全度棘轮 + 第 1 章回填** | Done | Claude | 新增 `tools/pdfref.py`（节号/清单号 → 书页 → PNG，书页 = PDF 页 − 14）与 `tools/fidelity.py`（新书正文汉字数 / 原书正文汉字数，棘轮式进闸门，D-034）。第一次量出**全书正文只保全了原书的 48%**，第 8、10、11 章不足 30%。按扫描件回填第 1 章：1.2/1.3/1.4 三节逐段补回原书的定义、性质、表 1.2、平均情况的概率推导与时空折衷论述，全章 50% → 101%。顺带用扫描件抓到三处原书自身的问题（1.4.2 例程差 2、1.3.2 章号引错、两位作者姓名拼错），已在正文就地说明。闸门 12 步全绿，376 项自测。 |
| T-058 | **按原版扫描件回填第 2-12 章正文** | Done | Claude | 接 T-057，按 D-034 补记定的「阅读顺序 + 每节 ≈ 0.8」逐章回填。全书正文保全度 **48% → 77%**，缺口 83768 → 37607 汉字；12 章全部上升，第 8 章 31%→67%、第 10 章 24%→61% 是提升最大的两章。顺带用扫描件抓到三处新书自身的问题：图 10.5 挂错位置（原书是例 10.2 的关键字散列表，被当成墓碑例子的槽位表）、5.1.1 整节没有定义与术语、5.4 缺原书两个删除算法的对比。11.6 在 fidelity.json 登记 waiver（内容早已并进 11.5）。闸门 12 步全绿，596 页 PDF 已重建。 |
| T-059 | **章末三节进度量 + 全书正文第二轮回填** | Done | Claude | 人拍板「先章末三节、再正文；习题先补题面、答案欠着」。`fidelity.py` 扩到 `## 本章小结`/`## 习题`/`## 上机题`（此前从未被量过：小结 17%、习题 23%、上机题 12%）；R14 改成组感知，回填的原书题单独成组、逐题登记 pending，**避免正文题的答案数遮住原书题的欠账**。全书正文保全度（含章末三节）**66% → 85%**，缺口 66713 → 28877 汉字；正文题 137 → 345 道（已作答 137、登记欠着 208）；PDF 606 → 640 页。顺带修掉一处会让站点构建崩的写法（转义美元符号）与原书一处「算法9.3/代码9.3」的自相矛盾。闸门 12 步全绿，380 项自测。 |
| T-060 | **拼接插图换成扫描件裁图（D-035）** | Done | Claude | `book/assets/combined/` 的 39 张（+`assets/ch06/` 4 张）是把 OCR 碎片拼起来、子题注用程序重画的，**不在任何核对里**。新增 `tools/figcrop.py` + `collab/figures_scan.json`：图号 → 书页 + 裁剪框 + dpi → PNG → sha256，`--check` 进闸门第 3d2 步（+8 项自测）。46 张全部换成印刷页原图，顺带修回三处内容差错（图5.1 少了 (a)(b) 两个子图、图5.12 与图5.13 被并成一张、图12.28 与图12.29 同理），跨页的图7.24/7.25/12.36 照原样分作两张、不做拼接。图片体积 7.7MB → 1.1MB，PDF 640 页 15.0MB → 630 页 9.6MB。 |
| T-061 | **剩下的上游 OCR 插图也换成扫描件裁图 + 补回习题缺的图（D-035 补记）** | Done | Claude | 人拍板「既然工具现成，值得做」。书稿与课件引用的 165 张上游图全部改成扫描件裁图，登记表 46 → 218 张，`book/ch*.md` 与 `book/slides/*.md` 里已无 `assets/<hash>.jpg` 引用。定位用 ZNCC 模板匹配（会话内脚本，不入库；入库的是它算出的框）。**反直觉的结论：换来的是出处不是像素**——匹配尺度几乎全是 1.00，说明上游那批本就是同一份扫描件的 200dpi 裁图，165 张逐张做截断检测**没有一张真被裁掉内容**。模板匹配错认 3 处（图5.13↔图5.12(a)、图9.5↔图9.6、图6.15↔图6.13(a)），已按印刷页重新量框；判据是「同页两张图的框重叠过半 = 错认嫌疑」，不靠眼睛。另**补回 17 张习题缺的图**（图5.21/5.22、6.19～6.22、7.26～7.29、11.26～11.31、12.46）——此前题面点名的图书里根本没有，题目做不了；`figcrop.py --check` 新增这条判据（只管习题，正文改排成表格是常态）。修掉 `used_paths()` 漏扫课件的洞。闸门 13 步全绿，393 项自测，PDF 636 页。 |
| T-062 | **把 OCR 图册从学生 PDF 里摘出去（D-036）** | Done | Claude | 人提出。T-061 之后 `book/插图.md` 与正文完全重叠——同一张图，正文里是扫描件裁图、图册里再来一张 OCR 版，PDF 白涨的 4MB 全是重复内容。把它从 `build_book_pdf.py` 的 `CHAPTERS` 里摘掉：**636 页 13.5MB → 526 页 7.4MB**，进 PDF 的图 509 → 217 张（正好是正文与附录真正用到的）。图册仍留在仓库与网页版，`collect_figures.py --check` 照核。判据钉在 `test_the_ocr_album_is_not_in_the_student_pdf`：`CHAPTERS` 里不许有 `插图.md`，且进摘要的图**不许有 .jpg**（上游图库全是 jpg，扫描裁图全是 png）。闸门 13 步全绿，394 项自测。 |
| T-063 | **课件出 .pptx（D-037）** | Done | Claude | 人要「根据 book 各章内容生成 pptx，详尽」。新增 `tools/pptx_writer.py`（只用 `zipfile` 写 OOXML：部件/关系/content-type/主题/备注页）与 `tools/build_pptx.py`（自己算高度的流式排版：标题带、要点、表格、代码高亮、插图、行内 TeX→Unicode、讲稿进备注窗格）。**不引入 python-pptx**——`tools/` 零第三方依赖是架构约束。关键判据：**投影没有滚动条**，缩到最小仍装不下就拆成「…（续）」，整块装不下就切这一块（列表按条目、代码按空行、表格按行且每半带表头）；397 页课件源 → 412 张幻灯片，13 页被拆。产物字节可复算（zip 时间戳固定），`--check` 进闸门第 3c2 步，25 项自测。12 份全部用 LibreOffice 转 PDF 逐页目检过。 |
| T-066 | **课件配旁白视频（`courseware/make_video.py`），第 1 章 28:57** | Done | Claude | 人指定：按 `faq/20260826creative` 那套流程做成 mp4。`content/ch01_narration.md` 逐页旁白（47 节 8702 字，手写）→ edge-tts → 从**当前** pptx 现导画面（LibreOffice→PDF→pdftoppm）→ ffmpeg 合成 + `loudnorm I=-16`，1920×1080 / 30fps / h264+aac，实测 28:57、48.5MB、积分响度 −17.0 LUFS。**成品 mp4 不入库**（一章约 50MB，每次重建整个文件都变；仓库最大的已入库文件才 9MB），入库的是讲稿、字幕 `video/ch01.srt` 与时间轴 `video/ch01.timeline.json`（含课件+讲稿的 sha256），任何人都能重建出同一份；视频缺席时闸门只提示不判红，与 `pdfref.py` 缺扫描件的做法一致。闸门加第 8 项，两条变异自检：改讲稿不重建 → 红；讲稿少一节 → 红。**自己踩了两个坑并修掉**：(1) 旁白若按「文件在就复用」缓存，改了词却照旧用旧音频、视频和讲稿从此对不上且零报错——改成按**文本哈希**缓存；(2) `write_timetable` 把实测时长写回讲稿时不幂等，每写一次文末就多一条 `---`、摘要跟着变，视频永远显示「过期」——抽出 `narration_body()` 统一归一化，写 3 次摘要不变。第 2–12 章待办。 |
| T-067 | **讲义 + 课件 + 视频做满 12 章** | Done | Claude | 每章一套：课件 pptx、同名讲义 md、逐页旁白讲稿、720p 视频与字幕。课件合计 **378 页**，视频合计约 **4 小时 05 分**。讲义里的每段 C++ 都摘自 `code/` 下真编译过的实现；习题按 `book/习题与参考答案.md` 逐题作答，并把书稿里「答案曾被原书自己的例子对拍出来是错的」那几处一并写进去（如第 4 章循环移位不能先反转）。**这一轮排版引擎被三次真实事故推着改**：**(1)** 表格图注压在最后一行字上——行高按「字号 ×1.30」估，而中文字体自然行高约 1.48 em，六行累积差小半厘米；改 `TABLE_LEAD=1.55`、`TABLE_SAFETY=1.12`，并补上「两段文字重叠」这条渲染判据（原来只查越界，看不见「压在一起」）。**(2)** 要点页同样按 1.30 估，第 9 章 9.2.2 那页 8 个长条目叠在了一起；加 `BULLET_LEAD_EST=1.58` 并在字号触底时**直接抛 LayoutOverflow**，不再钳位后照常画。**(3)** 「视频过期」判据用的是 pptx 的**字节**摘要，而 python-pptx 的输出不是逐字节可复现的（zip 带时间戳）——同一份 content 连生成两次摘要就不同，等于每次重建都谎报一次过期。改成摘要**决定画面的内容**（形状位置尺寸、文字、每个 run 的字号/加粗/字体、图片哈希），连续两次生成一致；已建的 5 章据此复核，画面内容完全相同，视频无需重录。另修一处误报：`⚠️` 走 NotoColorEmoji（Type 3），pdftotext 报的字框比字形大一圈，会罩住同一行的项目符号 `·`——图形字符的字框不可靠，从重叠判据里摘出去，并用第 1 章那次真事故复核判据仍然有牙。新增 `checklayout.py`（单章版面自查，判据与 verify.py 第 7 项共用同一组函数）。 |
| T-065 | **另起一套讲义 + 课件（`courseware/`），先做第 1 章** | Done | Claude | 人指定：按 `2026fall-cs101/courseware` 的形制重做，**不参考 `book/slides/`**。产物 `courseware/DSA_CH01_Overview_ADT_Complexity.{md,pptx}`（讲义 56KB / 课件 47 页），排版引擎 `deck.py` 移植自 cs101 并加了 `image` 页型（原书扫描图等比缩放居中）。这是仓库里**唯一**允许第三方依赖（python-pptx + Pillow）的目录，`tools/` 与 `code/` 仍是纯标准库；`tools/handoff.py --verify` 不受影响，仍 14 步全绿。自带闸门 `courseware/verify.py`（7 项），三条判据经变异自检验证有牙：**(1)** 复现 cs101 那个「行内代码开头、后面还有中文」的条目被 `strip('`')` 剥坏的 bug （`` `[[nodiscard]]`：返回值丢不掉 `` 会把反引号原样印在放映稿上）→ 第 6 项红；**(2)** 改了 `content/` 不重建 → 第 5 项红；**(3)** 把表格行高系数退回 `1.30`/`1.00` → 第 7 项红。第 (3) 条是真抓到的版面事故：「六种算法设计方法」页的图注**压在表格最后一行的字上**，文字全在版心内、也没碰页脚，越界检查一声不吭——已补上「两段文字重叠」判据，并把行高系数按中文字体的自然行高调到 `TABLE_LEAD=1.55`、`TABLE_SAFETY=1.12`。上机题两份参考实现（分拆计数、复数 ADT）在 `-Werror` 下真编译真运行，带断言自检。第 2–12 章待办。 |
| T-064 | **复核 Codex 的 pptx 修订：拆页落到源文件，补上被绕过的两道闸门** | Done | Claude | Codex 把 13 页挤不下的课件源手工拆开（正是我上一轮交接里请他看的那条），并补了 `\\ll → ≪`。复核发现两处：**(1)** 续页沿用了同一个标题，而 `slide_pages()` 用「章内标题唯一」当页标识，13 页被 **set 悄悄合并**——`check_doc` 因此一直报 397 页而不是 410，`tests/test_check_doc.py` 里那条唯一性用例是红的（闸门第 1 步就该拦下）。已把续页改名「…（续）」并挂到原页所属的节下，页数回到 410。**(2)** `ch08-sorting.pptx` 被 Office 重存过（包里多出 presProps/viewProps/theme2，备注页重新编号），而 `--check` 只比源摘要，**报绿**。已改为逐字节重排比对（约 0.6 秒），删掉不再当判据的 `source_sha256`，并加变异自检。闸门 14 步全绿，420 项自测。 |
| T-038 | **接口名闸门 R15（D-022）** | Done | Claude | T-037 复查手工抓到 4 个不存在的接口名，散文里点名的接口此前不受任何约束。R15 判定行内代码里的 `类名::成员` 必须解析到 code/ 的真实类成员/枚举值/命名空间成员；成员从**类体最外层**取（变异自检两次逼出来的：按「词在文件里出现过」判时 `HuffmanTree::code` 不报红）。配套把附录里「实际接口是 …」的裸名规范化成限定形式，管辖范围 38 → 96 处。明确不做裸标识符存在性检查——实测 57 处命中只有 1 处真缺陷，是假阳性机器。变异自检 9/9。 |
| T-037 | **习题答案：绑定台账 + 分批补齐** | Done | Claude 立台账 / Codex 补齐 | R14（D-021）+ `collab/answer_gaps.json` 管住正文 96 道习题与 40 道上机题；附录四组独立编号，课程作业不再冒充正文答案。第 1 批 ch01–ch02 共 25 条由 Claude 完成；2026-08-18 Codex 完成其余三批：ch03–ch05 38 条、ch06–ch08 31 条、ch09–ch12 42 条。可计算项均重新演算，涉及实现的答案逐项核对 `code/` 真实接口。2026-08-18 Claude 复查：抽算项全对，但改红 6 处——ch05 习题 2 后序 `CDBGFEA`→`CDBFGEA`、ch04 习题 7 的循环移位判据被写成先反转（原书例子 `arc`/`car` 会被判否），以及 4 个不存在的接口名（`pop_min`→`remove_min`、`code`→`code_of`、`connected`→`same`、`winner_value`→`winner_index`，`TournamentOps` 也不计数）。两处「条件不足」改成改题面而非拒答：ch06 习题 2 自带括号表示（答案实算并与 `GeneralTree` 对拍），ch12 习题 4 改问「选中不同块」（请求 650 分开三者）。R13 同批扩到汉字（「标记--清扫」），带用例与变异自检。 |

## Decision Log

> **决策全文在 `collab/DECISION_LOG.md`**，这里只留索引。
> 两份可编辑的决策副本一定会各自腐烂，所以这里不复制正文。

| 编号 | 日期 | 决策 | 谁拍的 |
| --- | --- | --- | --- |
| D-001 | 2026-08-12 | DSA 教材 C++ 现代化风格公约（T-006）：C++17、STL 边界、错误处理与 I/O、命名 | **人** |
| D-001 §3b | 2026-08-12 | 补充条款：新增 `const T* peek() const noexcept` 作为 `optional<T> top()` 的零拷贝补充 | **人** |
| D-001 §3c/§3d | 2026-08-12 | 提取用 optional、按键删除用 bool；递归树周游保留并明示深树栈溢出风险 | **人** |
| D-005 | 2026-08-12 | 扩容搬迁判据落在「移动赋值是否 noexcept」；由此产生的元素类型约束写成 static_assert | Codex 发现 / Claude 定案 |
| D-006 | 2026-08-12 | 闸门先自检 sanitizer 环境，环境问题用退出码 2 与代码问题区分；降级出口必须吵 | Claude 记录 |
| 退场 #1 | 2026-08-12 | 代码4.2 退场：它是标准库 `basic_string` 的空壳声明，无可现代化内容（顺带记：往 `namespace std` 加 typedef 是 UB）| Claude，见 `exclusions.json` |
| D-002 | 2026-08-12 | `dsa_raw.md` 永久只读 | Claude 记录 |
| D-003 | 2026-08-12 | 书稿代码块与源码一致性靠机器保证（R3 + sync_book） | Claude 记录 |
| D-004 | 2026-08-12 | 闸门跑 Debug+ASan/UBSan 与 Release-O2 两种构建 | Claude 记录 |
| D-011 | 2026-08-15 | 网页版存 `book/site/`（插图不复制第二份），由 Actions 现场重建后发到 GitHub Pages | Claude 记录 |
| D-013 | 2026-08-16 | 课件用 Markdown 源（`book/slides/*.md`）+ 网页版，不生成 .pptx；新增 `#fn:名字` 按函数名取切片 | **人** |
| D-012 | 2026-08-16 | 书稿正文印**教学版**（`teaching.hpp`，三法则、整块印全），工程版降为「进阶（选读）」；`teaching_test.cpp` 与 `demo.cpp` 一并进闸门 | **人** |
| D-007 §补 | 2026-08-17 | 密度不等于兑现：每条认领要显式绑定实现锚点与测试名，闸门至少查二者存在（→ T-025） | Codex 提 / Claude 定案 |
| D-010 §补 | 2026-08-17 | R8 反向的沉默是子串匹配自带的；不放宽成相似度阈值，改禁长的、形似 C++ 函数的 text 块（→ T-026） | Codex 提 / Claude 定案 |
| D-014 | 2026-08-17 | 图与公式：能机器判的是「渲染得出来吗」（新增 R9），数学与图的正确性仍是人工复核项 | Claude 记录 |
| D-015 | 2026-08-17 | 算法6.10 拒绝三类不自洽序列，不为标准答案放宽（末结点必 ltag=rtag=1；入栈出栈必须配对） | Codex 定案 |
| D-016 | 2026-08-17 | 原书分了哪些节由底稿说了算：新增 R10 + `section_gaps.json`（并入/有意不写/待补，各带理由责任人日期） | Claude 记录 |
| D-017 | 2026-08-17 | 节号引用也要能解析：新增 R12（默认按新书解析，带「原书」标记按底稿解析） | Claude 记录 |
| D-018 | 2026-08-17 | `--` 不是破折号：新增 R13（围栏外的 `数字--数字` 报红，行内代码与链接除外） | Claude 记录 |
| D-019 | 2026-08-17 | PDF sidecar 记录全部构建输入内容哈希；交接与 Pages 发布都拒绝陈旧 PDF | **人** |
| D-020 | 2026-08-17 | 插图集离线自证：`collect_figures.py --check` + `assets/figures.json` sidecar（文件名 == 内容哈希） | Claude 记录 |
| D-024 | 2026-08-18 | 分配存储 ≠ 构造对象：`ArrayStack` 工程版换成裸存储 + placement new + 显式析构（教学版保持 `new T[n]`） | Claude 记录 |
| D-023 | 2026-08-18 | 课件覆盖只能显式登记不能做成匹配器：新增 R16 + `slide_coverage.json`（覆盖/引子/不进课件/欠着） | Claude 记录 |
| D-022 | 2026-08-18 | 点名本书接口，机器要能查得到：新增 R15（`类名::成员` 必须解析到 code/ 的真实成员） | Claude 记录 |
| D-029 | 2026-08-19 | 切片要指得准：`#fn:类名::函数名` 限定类体（同名私有辅助函数消歧），`template <...>` 头随函数一起取 | Claude 记录 |
| D-032 | 2026-08-20 | T-044/T-045：存储教学与新增算法分开验收；新增算法不认领原书清单，课件页登记为 `extra` | Codex 记录 |
| D-033 | 2026-08-20 | 算法侧可以用 STL 承载数据（单调栈用 `std::vector` 当栈可以）；判据是「这一单元教什么」而不是「归在哪一章」，D-001 §2 对存储侧不变 | **人** |
| D-034 | 2026-09-04 | 原书扫描件是「原文逐字怎么写」的出处（书页 = PDF 页 − 14，`tools/pdfref.py`）；编辑方针为尽量保留原书正确的内容与文字；正文保全度以棘轮方式进闸门（`tools/fidelity.py`，只在掉下去时判红） | **人** |
| D-035 | 2026-09-04 | 书稿里我们自己合成过的插图一律改为从扫描件直接裁，裁法（书页 + 裁剪框 + dpi）登记在 `collab/figures_scan.json`，`figcrop.py --check` 核对哈希进闸门 | Claude 记录 |
| D-037 | 2026-09-04 | 课件再出一份 `.pptx` 产物（`tools/pptx_writer.py` + `build_pptx.py`，纯标准库手写 OOXML，不引入 python-pptx）；事实源仍是 `book/slides/*.md`；投影没有滚动条，一页装不下就拆「…（续）」而不是溢出 | **人**（提出）/ Claude 记录 |
| D-036 | 2026-09-04 | 学生 PDF 不收录 `book/插图.md`：正文的图已是扫描件裁图，图册再进来等于每张图印两遍（526 页 7.4MB ← 636 页 13.5MB）；图册留在仓库与网页版，仍归 `collect_figures.py --check` 核 | **人**（提出）/ Claude 记录 |
| D-031 | 2026-08-20 | README「现状」的每个数字由 `tests/test_readme.py` 现场核算，手抄即红（代价：加测试要顺手改 README） | Claude（复核回应） |
| D-030 | 2026-08-19 | 反向覆盖：课件每一页都要有出处（新增 R17 + `slide_coverage.json` 的 `pages` 表）；守孤儿页，不守正文厚度 | Claude 记录 |
| D-021 | 2026-08-17 | 出了题就要说清楚答案在哪：新增 R14 + `answer_gaps.json`（同号答案，或登记欠着/留作练习） | Claude 记录 |
| D-028 | 2026-08-18 | T-047：双实现共读五列 TSV，闸门核对两侧条数 | Codex 记录 |
| D-027 | 2026-08-18 | 人已拍板：`unit.json` 的 `anchor` 改名 `code_line`，两侧命名对齐 | **人** |
| D-026 | 2026-08-18 | 人已拍板：Python 风格公约 + 三处「跑得通但什么都没实现」的处置（T-043 复核） | **人** |
| D-025 | 2026-08-18 | 人已拍板：Python 进书的边界与证据链（T-041 ~ T-046） | **人** |
| D-010 | 2026-08-14 | R3 的逃生口：```text 块（R8 的由来） | Claude 记录 |
| D-009 | 2026-08-14 | 勘误的「已修正」必须是 grep 得到的事实 | Claude 记录 |
| D-008 | 2026-08-14 | 原书没有清单的内容，允许 `listings: []` + `beyond_book` | Claude 记录 |
| D-007 | 2026-08-12 | 闸门新增「实质性检查」 | Claude 记录 |
| D-016 §补 | 2026-08-17 | 原书编号与题名一起构成稳定入口（新增 R11）；原写作 D-017，与同日另一条撞号，2026-08-20 改号 | Codex 记录 |

**已被取代**：2026-08-12 早些时候 Claude 提议的「C++20 + concept」被 D-001 取代为
C++17 + `static_assert`。原提议与取代理由都保留在 D-001 里，没有删除。
