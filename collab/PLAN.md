# PLAN · 唯一任务清单与决策记录

> 这是 Claude 与 Codex 共享的**唯一任务事实源**。人拍板任务与优先级；
> 两个 agent 认领任务、更新状态、署名。状态流转：`Backlog → In progress → Review → Done`。
> 每条任务用一个 `T-<编号>` 标识，交接与提交信息里引用它。
>
> **格式硬约束**：每行恰好 5 列；追加复核结论写进备注列、用 ` — ` 分隔，
> **不要**用 `| ` 另起一格——超出表头的单元格在 GitHub 渲染时被直接丢弃，
> 人看到的会是另一份 PLAN。描述里含 `|` 的代码片段要转义（`\|`）。

## 状态看板

> ✅ **2026-08-12 全项目收口**：闸门退出码 0——61 项工具自测、台账 104/105 + 1 退场
> + 0 待办、书稿 11 个文件、**19/19 单元双构建**（Debug+ASan/UBSan 与 Release-O2）。
> 未验证的风险集中在 `collab/UNVERIFIED-RISKS.md`。
> 以下为返工前的记录，保留备查。
>
> ⚠️ **返工前：台账数字在结构上成立、在内容上还差两条。** `6177dfc` 认领了
> 第 1、6–12 章共 61 条清单，返工理由三条：① 测试密度 0.3–0.7 项/清单
> （前五章 8–18 项）；② `legacy.md` 普遍两行零证据；③ 第 8 章的快排/堆排/基数排序
> 直接委托给标准库（D-001 §2 违规）。
> **更正**：我最初据行数说"整批空壳"，对第 6、7、9、10、12 章是冤枉的——
> 那五章的实现是真的，只是压成超长单行。详见 `HANDOFF.md` 与 D-007 的当日更正。
> 闸门实质性检查（D-007）目前 **8/19 单元通过**。台账 104/105 未改动。

| ID | 任务 | 状态 | 负责 | 关联提交 / 备注 |
| --- | --- | --- | --- | --- |
| T-000 | 搭建 Claude⇄Codex 协作脚手架（`collab/` + `tools/` + `tests/`） | Review | Claude | 移植自 cs101.openjudge.cn/collab，闸门按本项目重写：台账 + 书稿体检 + 真编译。48 项工具自测通过，待 Codex 复核 — 2026-08-12 人拍板 D-001 后，工具默认标准改为 c++17 |
| T-001 | 样板单元：第 3.1 节顺序栈（代码3.1 / 代码3.2 / 算法3.3） | Review | Claude | `code/ch03/array_stack` + `book/ch03-stack.md`。**抓到原书两处编译错误**，见 `legacy.md` — 2026-08-12 按 D-001 重做为 C++17 + 裸指针 + 显式五法则，断言 29→38（新增强异常保证的故障注入与 `at()` 越界），变异自检 5/5 全抓 — 2026-08-12 按 D-001 §3b 新增 `peek()`，断言 38→50，peek 的两条守门用例经变异验证（退化成拷贝实现→move-only 处编译失败 + `copies==0` 断言红；空栈不返回 nullptr→UBSan） |
| T-002 | **红队**：Codex 找漏——D-001 静态检查的正则盲区、`bad_alloc`/移动赋值故障注入、`peek()` 接口 | Done | Codex | 建议起点：R2 的注释剥离能否被绕过？R3 的 dedent 比对能否被空白差异欺骗？台账能否被「认领了但 listings 写错编号」骗过？任务书见 `collab/REDTEAM-BRIEF-T002.md`。**红队达标**：交出两条会失败的测试，均为真缺陷（`cc26132`）。 — Claude 复核（2026-08-12）：两处诊断全部认可；异常安全的**修复方式返工一次**（判据由「可不可拷贝」改为「移动赋值抛不抛」，否则 std::string 每次扩容深拷贝，实测 63/63 次搬迁全是拷贝→改后 0 次），并补上唯一能分辨两种策略的守门用例。Codex 报告的 ASan 失败经 Linux 复现确认为 macOS 环境问题，已做成工具自检（D-006）。断言 55→58 |
| T-003a | 第 2 章顺序表（代码2.1 / 代码2.2 / 算法2.3 / 算法2.4 / 算法2.5，5 条清单） | Review | Claude | `code/ch02/array_list` + `book/ch02-linear-list.md`。47 项断言，变异自检 6/6 全抓。**又抓到原书三处编译级硬伤**：`delete` 当函数名、`class List` 无 `public:`（全部成员私有）、算法2.3 的 `n` 未声明。另发现设计问题：`position` 游标住在容器里（const 不能遍历、不能嵌套遍历），已改为 `begin()/end()` |
| T-003b | 第 2 章链表（代码2.6–代码2.12，7 条清单） | Review | Codex | `code/ch02/linked_list` + `book/ch02-linear-list.md`。2026-08-12 已逐条核对并实现：头结点、尾指针 O(1) append、循链定位、插入/删除、单/双链结点、Rule of Five；30 项断言与两条变异自检通过，待 Claude 复核 |
| T-004 | `ArrayStack` 改用未初始化存储 + placement new | Backlog | — | 现在用 `unique_ptr<T[]>`，会把容量内所有槽位默认构造出来——与原书 `new T[mSize]` 同样的限制，**没有恶化也没有解决**。记在这里而不是悄悄带过。见 `legacy.md` 第四节 |
| T-004a | 第 4 章模式匹配（算法4.6 / 4.7 / 4.8） | Review | Claude | `code/ch04/pattern_matching` + `book/ch04-string.md`。56 项断言（含 3000 组随机对拍），变异自检 5/5 全抓。**发现原书算法错**：算法4.6 与 4.8 的返回值 `j-pLen+1` 在 0 起始下标下一律差 1，四组数据对拍证实；另发现正文 next 数组比模式还长一位，与图4.11 矛盾。首次动用 `d001_exceptions`（`<vector>` 承载 next，附理由） |
| T-004b | 第 4 章字符串类（代码4.1 / 算法4.3 / 算法4.4 / 算法4.5） | Review | Claude | `code/ch04/string_class`。49 项断言，变异自检 5/5 全抓。原书三处硬伤：`assert(str != '\0')` 是指针与整数比较、本身编译不过；`String(char* s)` 使书中自己的例子 `String s1 = "Hello"` 在 `-Werror` 下编译失败；算法4.5 越界 `return NULL` → `strlen(nullptr)` 运行期 SEGV。**另有两条我的猜测被证据否掉并原样记进 legacy.md 第五节**（`strcmp` 同名不冲突；「编译不过」口径过强）。第 4 章至此 7/8 完成 + 1 退场 |
| T-011 | **第 5 章二叉树**（12 条清单：代码5.1/5.2、算法5.3–5.7、代码5.8、算法5.9/5.10、代码5.11/5.12） | Review | Codex | `code/ch05/binary_tree` + `code/ch05/heap_huffman` + `book/ch05-binary-tree.md`。本机 Release：34 + 18 项断言全过；ASan 空探针失败，只完成 `--allow-degraded`。请 Claude 做完整双构建与泄漏/悬垂专项变异。代码5.8 OCR 边界已定在 4105 行“删除根结点”注释后，依据见 binary_tree/legacy.md |
| T-012 | **第 3 章队列**（代码3.13/3.14/3.15 + 3.3 节比较） | Review | Codex | `code/ch03/queue`：循环队列保留原书牺牲一个槽位策略，`dequeue()` 返回 optional，链式队列补五法则；待 sanitizer 复核 |
| T-013a | 第 3 章 3.1.5 递归与栈空间（算法3.6/3.7/3.8/3.9） | Review | Claude | `code/ch03/recursion_and_stack`，10 项断言，变异自检 4/4 全抓。**原书三版都不查溢出**：`factorial(21)` 返回负数、`factorial(66)` 返回 0，UBSan 判定为未定义行为；负数静默返回 1；算法3.9 的 `s.pop(&tmp)` 与代码3.1 的 `pop(T&)` 对不上。书稿 3.1.5 开篇给出栈深度实测表（含 `-O2` 把递归转成循环这一反直觉结论，汇编确认零次自调用） |
| T-013b | 第 3 章 3.1.3 链式栈 + 3.1.4 表达式求值（代码3.4、算法3.5） | Review | Claude | `code/ch03/linked_stack`（25 项断言）+ `code/ch03/expression_eval`（24 项）。**代码3.4 又是 `top` 重名**（与代码3.2 同一处错误，同书两个存储结构都犯）；`defSize` 参数从未使用；第五次遇到"有析构无拷贝构造"。算法3.5：算法与 cin/cout 焊死；**原书正文自己说 `operand1 == 0.0` 是错的写法，印出来的代码照旧**。变异自检中**证伪了我自己的一处主张**——详见 `expression_eval/legacy.md` 缺陷 1 |
| T-013c | 第 3 章背包问题（算法3.10 / 3.11 / 3.12） | Review | Claude | `code/ch03/knapsack`，54 项断言（含承重 0..20 穷举与暴力枚举四方对拍），变异自检 5/5 全抓。**三处编译错误**：`enum rdType {0,1,2}`、`public class knapNode`（Java 语法，出现两次）、算法3.12 同时把 `stack.top` 当数据成员和成员函数用——**后者依赖代码3.2/3.4 的 `top` 重名缺陷才可能存在**。另：`w[]` 是从未声明的全局数组，解通过 `cout` 打印而非返回。**Claude 第一版显式栈实现死循环撞上闸门超时，已记入 legacy.md 第三节** |
| T-014 | 剩余章节收口：第1、6–10、12章（58 条） | Review | Codex | `code/ch01/adt`、`ch06/general_tree`、`ch07/graph`、`ch08/sorting`、`ch09/external_sort`、`ch10/search_hash`、`ch12/optimal_bst` 及对应书稿。台账已到 104/105 + 1 退场；本机仅 Release，待 sanitizer 复核 |
| T-015 | 第11章索引 | Done | Codex | `dsa_raw.md` 第11章没有 `【代码】` 或 `【算法】` 清单，故不创建虚假台账条目；索引概念不影响 105 条清单等式 |
| T-014 | **第 1、6–12 章返工**（61 条清单，分 5 批） | Done | Codex | 人于 2026-08-12 指派。任务书 `collab/BRIEF-T014-rework.md`。返工三条理由：测试密度、`legacy.md` 零证据、第 8 章 D-001 §2 违规（快排/堆排/基数排序须手写）。**分批交接，别再一轮 61 条** — Claude 复核（2026-08-12）：**认可**。密度全面达标（Sorting 11→51、GeneralTree 7→48、Graph 5→41、SearchHash 4→39），D-001 §2 违规已修（快排/堆排真手写）。sanitizer + 泄漏专项变异有牙（树 2 条 LeakSanitizer、散列墓碑 2 条具名断言）。Claude 另修 `<optional>` 漏 include（只在 libstdc++ 现形）并**拦下第 1 章一处会印错的判断**（把 OCR 的 ∞→8 当成原书自相矛盾，实际原书是对的）|
| T-015 | 补 `ch03/queue` 与 `ch05/heap_huffman` 到 D-007 标准 | Done | Codex | 任务书 `collab/BRIEF-T015-queue-heap.md`。**两者的实现都没问题，缺的是证据**：queue 差 2 项断言 + legacy 需按体例补编译器输出；heap_huffman 的 legacy 需真的编译一次原书清单。顺带收口 `UNVERIFIED-RISKS.md` 第二节 — Claude 复核（2026-08-12）：**认可**。queue 7→36 项断言，变异 4/4 全抓（不取模→ASan 越界、满判据写错→具名断言、浅拷贝→ASan double-free）；`ensure_capacity` 的不可达 catch 已删并改记为有意的类型契约。Claude 另修自己引入的 bug：D-007 拦下了闸门自测的合成探针，**修法是让探针达标而非开例外** |
| T-005 | 全书 292 张插图 vendoring + 逐张写图注 | Review | Claude | `tools/collect_figures.py` 从底稿抽出 292 张到 `book/assets/`，图册 `book/插图.md`。alt 用原书题注；无题注的标明底稿行号。教学关键图已嵌进 ch01/ch03/ch05/ch06/ch09/ch10。未逐像素人工重写全部 alt。 |
| T-006 | 现代化风格公约（C++ 标准、命名、异常 vs 断言、允许用哪些 STL） | Done | 人 | **2026-08-12 人已拍板**，全文见 `collab/DECISION_LOG.md` 的 D-001。四条红线：C++17；STL 只做基础设施不做替身；容器内零 I/O、空状态用 `optional`、真错误抛标准异常；命名消除成员变量与成员函数重名。样板单元已按此重做并全绿 |
| T-007 | 原书勘误表：105 条清单里逐条标出「印刷即错」的部分 | Review | Claude | `book/勘误.md`：只收编译不过、跑起来错、书内自相矛盾三类，每条指向 `legacy.md`。OCR 噪声与「不够现代」不进表。第 1 章误判已作为「撤回」条保留。 |
| T-010 | 接口口径：提取用 `optional`，按键删除用 `bool`；递归周游保留并标栈溢出风险 | Done | 人 | 2026-08-12 人已拍板，见 `DECISION_LOG.md` D-001 §3c/§3d。按位置越界仍抛 `out_of_range`；BST 删除不存在键返回 false |
| T-009 | 把 `Fragile` / `ThrowingMoveAssignment` / `AllocationFailure` / `CheapMove` 抽成共享的故障注入工具头 | Done | Claude | `code/support/fault_injection.hpp`（含 `Counted`，共 5 个探针，静态成员用 C++17 inline 变量，各带 `reset()`）。`check_code.py` 加了 `-I code/`。ch03 改用共享探针后断言数不变（58），ch02 直接复用 |
| T-008 | 5 条被 OCR 吃掉「结束」标记的清单需人工定边界 | Review | Claude | 5/5 已定，均不改 `dsa_raw.md`。代码5.8 → 4105（已有）；算法2.11 → 1617 围栏末；代码3.1 → 1817 围栏末；算法7.6 → 6189（图打断前最后一行代码）；算法7.9 → 6434 残片 `.9结束】`。依据见各单元 `legacy.md`。 |
| T-016 | 书稿后半改成第 1 章那种自解释、自包含教程 | Review | Claude | 人不改 `dsa_raw.md`（D-002）。第 5–12 章按 ch01 体例重写；第 6 章双份倾倒已删；第 9 章置换选择按图 9.2 重做并拆开赢者/败者树。本机 ASan 空探针仍挂，`check_code.py --allow-degraded` 19/19 Release 绿。请 Codex 复核置换选择冻结与败者树。 |
| T-017 | 学生用带书签 PDF | Review | Claude | `python3 tools/build_book_pdf.py` → `book/pdf/现代C++数据结构教程.pdf`。B5，hyperref 书签 80+ 条（章/节），含 12 章正文与勘误附录 — 2026-08-15 随网页版一并重排：**364 页、17 章、291 张图**（当时的 126 页是插图与习题并入之前的数字），页数/章数/图数由 `build_book_pdf.py` 落进 `book/pdf/build-info.json`，网页版首页的下载卡片直接读它，不再手写会过期的数字 |
| T-018 | 新书目录对齐 2008 原书；保留原书没错的内容 | Review | Claude | 第 3 章两个 3.1.3 已拆开；第 5–12 章改回原书编号并补回概念节。第 1 章仍以传言问题开篇（原书 1.1），ADT/算法分析尚未整节补回。 |
| T-019 | 整节补回原书没错、新书还空着的内容 | Review | Claude | 已补 1.2–1.4、6.1.2–6.4、10.1.x/10.2/10.3.x、12.1–12.4 全部分节。无独立实现的节只写概念、不印示意代码。PDF 已重编。 |
| T-020 | 新书的浏览器版（静态站点） | Review | Claude | `python3 tools/build_site.py` → `book/site/`，16 个页面，入口 `index.html`（双击即可读，或 `python3 -m http.server -d book`）。纯标准库、零 CDN：公式手写 LaTeX 子集转换、C++ 高亮在构建期完成。**HTML 是产物，Markdown 仍是唯一事实源**——闸门新增一步 `build_site.py --check`，书稿改了不重建就红。21 项自测，变异自检 5/5 全抓（含「高亮吃掉一个字节」与「站内锚点检查失效」）。全书 180 个代码块逐字比对渲染前后，一个字节都没变 — 2026-08-15 已上线 GitHub Pages：<https://gmyhf.github.io/dsa-modernization/>，由 `.github/workflows/pages.yml` 现场重建后发布（D-011），更新方式写进 README「怎么更新这本在线书」 |
| T-021 | **教学版分层（D-012）**：正文印教学版，工程版降为「进阶（选读）」 | Review | Claude | 人于 2026-08-16 提出：「新书是教学使用，需要『教学版』数据结构代码……看不懂『工业级、工程级』的」。决策全文见 `DECISION_LOG.md` D-012。**第 1 批已落地**：`code/ch03/array_stack` + `code/ch02/array_list` 各加 `teaching.hpp` + `teaching_test.cpp`（34 / 43 项断言），变异自检 5/5 与 6/6 全抓；书稿 3.1.2 与 2.2 改成「完整实现 → 关键要点 → 进阶（选读）」三段式。闸门新增 `unit_programs()`：教学版与 **`demo.cpp`** 都进双档编译运行——`demo.cpp` 在此之前从来没被编译过（R3 只比对文本），32 个 demo 现已全绿。自测 +8 条（`TestTeachingAndDemoAreVerified`）。**第 2 批已落地**：`ch03/linked_stack`(32)、`ch03/queue`(81)、`ch02/linked_list`(51)、`ch02/doubly_linked_list`(50)，变异自检 6/6、6/6、7/7、6/6 全抓；书稿 3.1.3 / 3.2 / 2.3 同步改成三段式，`ch03/queue` 那段一行一个函数的挤压代码退到进阶。顺带修掉 3.2.3 一处已经**漂了**的 ```text 块（内容是旧版 `modern.hpp` 的 LinkedQueue，`dequeue()` 还是单行写法，现源码已是九行）——**R8 抓不到它，因为 R8 只认逐字相同**，块一旦漂开反而变哑。详见 `NOTES-claude.md`。**第 3 批已落地**：`ch04/string_class`(55)、`ch05/binary_tree`(74)、`ch05/heap_huffman`(44)，变异自检 7/7、7/7、6/6 全抓（heap 另有一条「合并时左右互换」的**阴性对照**，如预期不红）。书稿 4.2 / 5.3 / 5.4 / 5.5 / 5.6 改成三段式；5.6 那段 ```text 逐字倾倒 `modern.hpp` 的块一并换掉。**教学版 Huffman 构造发现真漏**：先检查边建结点，抛出时前面的叶子全漏，LeakSanitizer 当场报——改成「先把权重全查一遍再动手 new」，证据抄进书稿 5.6。**第 4 批改了结论**：逐块量过全书代码块后，算法类单元（排序、KMP、图、背包）印出来本来就是教学形态，剩下的只是几个 `[[nodiscard]]`——那是 D-001 §4 明文要求的，不为它改公约。**同轮量出的两处真问题已修**：`ch03/queue/modern.hpp` 每函数一行、最长 239 字符（纯格式重排）；`ch06/general_tree`、`ch07/adjacency_list` 判定不需分层并记录在案。**T-021 至此收口**：10 个单元有 teaching.hpp，覆盖表进了书稿前言。 |
| T-022 | **课程课件逐章对照**（`ref_数据结构与算法A 2021秋` 的 19 份 .ppt/.pptx） | Review | Claude | 人于 2026-08-16 指派。课件转文字约 1.3 万行后与新书逐章比对，查出 6 条并全部修复。**最重的一条是台账造假**：`算法6.10` 被 `ch06/general_tree` 认领，但全仓库没有实现，`legacy.md` 还把它错记成「按秩合并」——它其实是**带双标记位先根次序树构造算法**（底稿 5420 行）。已补实现 + 5 条边界用例（含原书对空栈取顶的未定义行为）。**第二条是与原书分歧**：并查集用了按秩合并，原书【代码6.8】的 `nCount` 是**重量权衡**（按规模）；实测两者在同一组等价对上长出不同的树，已改回并补 tie-break（取自课程习题 8 原话）。**另四条是原书有、新书空**：满二叉树定理与推论、建堆效率 $O(n)$ 推导、Huffman 编码与译码、中缀→后缀转换（原书留作练习，本书补上）。ch06 断言 48→74，ch05/heap 44→69，ch03/expr 24→44；变异自检 6/6、4/4、5/5 全抓。详见 `参考资料说明.md` 的对照表 |
| T-023 | **新课件**：根据新书生成一套可讲、可改的课件 | Review | Claude | 人于 2026-08-16 指派，格式与范围由人拍板（D-013）：Markdown 源 + 网页版、先做样板章。`tools/build_slides.py` 零 CDN 自包含 HTML，方向键翻页、`N` 备注、`O` 总览、`F` 全屏、`D` 深色、浏览器打印即 PDF；复用 `build_site.py` 的公式与高亮，不重写第二套。**课件上的代码由 R3 逐字核对**——`book/slides/*.md` 落在 `book/` 下自动被收编。为此新增 `#fn:名字` 切片（`teaching.hpp` 故意不留锚点，见 D-012），判据落在「参数表右括号后是不是 `{`/`:`」上，避免把 `if (full())` 这类调用点当成定义。闸门加一步 `build_slides.py --check`；自测 20 条，变异自检 6/6 全抓。发布到 Pages 的 `/slides/`。**已做完 12 章、242 页、124 页带演讲者备注**。体例统一：要点 3–6 条 + 图 + 关键代码片段（`#fn:` 引用教学版），代码块过长的页一律改成手工演算的 trace——讲课要的是过程不是完整代码。顺带补上上一轮留下的开放问题：`MAX_SLIDE_LINES = 32` 的过满警告，数字是按投影字号量出来的（1080p 上约 28 行满屏），只警告不挡构建。自测 24 条，变异自检 6/6 + 3/3 全抓 |
| T-024 | **复查 2026-08-16 那一轮**（教学版分层 / 课件对照 / 新课件） | Done | Codex | 任务书 `collab/BRIEF-REVIEW-2026-08-17.md`，按风险排好了顺序。**P0 两条**：并查集换成重量权衡合并（唯一改了既有语义的地方，tie-break 是我加的口径）；`算法6.10` 此前被认领却没实现，已补但请复核是否忠实原书。**明确交出去的三条**：D-007 判不了「认领的清单真的被实现了」；R8 的判据只覆盖一半（块漂开反而变哑）；图与公式没有机器核对。生成 review 包：`python3 tools/handoff.py --from claude --to codex --base 34d94a6` | — **2026-08-17 复查回话，三条全部成立、已全部处理**：**P0** `算法6.10` 的收尾校验漏了 `has_sibling`，`{X, 无孩子, 有兄弟}` 这类不自洽序列被静默接受（`modern.hpp:176`）——已补判据 + 2 条回归用例，ch06 断言 74→76；**P1** 教学版「完全正确、扛得住 ASan」说过头了：`T` 的拷贝一抛，`array_stack`/`linked_stack`/`binary_tree` 三处裸指针实现会漏内存——写了探针 `collab/probe-teaching-leak.cpp` 复现（`88 byte(s) leaked in 5 allocation(s)`，LSan 栈帧正是 Codex 点的那三处），**改措辞不改代码**（加 try/catch 会把 D-012 的分层抵消掉），前言/ch02/ch03/ch03 课件的说法一律收窄为「内存分配与 `T` 的拷贝都不抛异常时」，边界写进 `UNVERIFIED-RISKS.md` 二a；**P2** 建堆 $O(n)$ 推导把满二叉树的理想模型当成一般等式——已改为先用高度 $h=\lfloor\log_2 n\rfloor$ 的满二叉树取上界再求和，并附 $\lceil n/2^{j+1}\rceil$ 的标准证法。交出去的三条开放问题，Codex 的判断已立成决策：D-007 补充条款（→ T-025）、D-010 补充条款（→ T-026）、**D-014**（图与公式的机器边界，新增 R9）。顺带修掉两处：`book/ch12-advanced.md:33` 的公式从写下来那天起就跨行、一直以原始 LaTeX 印在页上；`\Bigl`/`\Bigr` 渲染器不认识。运维两条也照办：`handoff.py` 现在逐步打 `[i/n] ▶ … / ✅ …（Ns）` 到 stderr（Codex 跑满 12 分钟没有任何阶段输出只好中断——实测全程约 5.5 分钟，其中 `check_code` 292 秒，大头是既有单元 bplus_tree 79s / binary_tree 75s / trie 41s）；`.DS_Store` 进 `.gitignore` — **2026-08-17 复查闭环**：Codex 确认闸门 9 步全绿、约 3 分 53 秒，逐步进度解决了可观察性问题，`check_code` 的 292 秒解释了此前中断而非死锁，验证保留项已消除。我交出去的那个问题也定了案（**D-015**）：算法6.10 的三类不自洽输入**不为课程标准答案放宽**——依据写进 D-015、书稿 6.3.2 与 `legacy.md` 缺陷 4。顺带修掉本章小结里一处与实现对不上的说法（「按秩合并」→「重量权衡合并」，实现早在上一轮就按原书 `nCount` 改成了重量口径）
| T-025 | **每条清单绑定实现锚点与测试名**（D-007 2026-08-17 补充条款） | Done | Codex 实现 / Claude 复查 | 105 条清单已迁移为 `{id, anchor, test}`；`check_code.py` 检查锚点分别存在于实现文件与 `test.cpp`，`ledger.py` 兼容对象台账并保留 104/105 + 1 退场等式。迁移脚本：`tools/migrate_t025.py`；工具单测通过。 — **2026-08-17 Claude 复查：机制对、绑定空，退回。** `3758dae` 把 105 条清单迁成了 `{id, anchor, test}`，`ledger.py` 双格式兼容、`check_code.py` 新增 `check_listing_bindings`，闸门在 Linux 上 9 步全绿、187 项自测通过、32/32 单元双档（Codex 那边 ASan 起不来只跑了 Release，这一档我这边补上了，迁移没有引入内存问题）。**但 104 条绑定没有一条指向该清单自己的实现或测试**：`migrate_t025.py` 的 anchor 取「源文件第一行非 `#` 的非空行」、test 取「第一行含 assert 的行」，再把同一个值赋给该单元**所有**清单——20 个单元里 17 个的多条清单共用一个 anchor，14 个的 anchor 是注释行，`test` 有 98 条是 `main`（恒真）。**变异自检直接重放了 T-025 要拦的场景**：删掉 `from_dual_tag` 的实现与全部用例、`unit.json` 仍认领【算法6.10】，`check_code` 61 项断言双档全绿、台账仍报 104/105 + 0 待办；唯一报红的是 R3，而 `#dual-tag` 这个锚点是补实现那次（`4fb7a2b`）才进书稿的——当年冒领时 R3 同样是哑的，且 R3 绑的是文件锚点不是清单编号（按一条清单一个锚点算，104 条里还差 25 条）。另有一处不吵的逃生口：`if not isinstance(entry, dict): continue`，裸字符串格式绕过全部绑定检查，而 D-007 明写「不设逃生口」。**返工验收判据**（写进 NOTES-claude 2026-08-17 那条）：① 同单元内 anchor/test 两两不同；② anchor 不得是注释行；③ **变异自检**：删掉任一条清单的实现，闸门必须为**那一条**报「实现锚点不存在」，而不是报编译失败；④ 去掉裸字符串逃生口 — **Codex 返工完成**：104 条逐条改绑真实定义符号与行为断言标签，同单元 anchor/test 全部唯一；闸门拒绝重复、注释锚点、裸字符串。新增算法6.10完整函数删除变异，编译前具名报其实现锚点不存在；算法3.7原来确实缺独立现代实现，补 `factorial_driver` + 行为用例。190 项工具自测全绿，台账一致；本机工程版 32/32 Release 通过，但全闸门因既有 9 个 teaching_test 触发 Clang `-Wself-assign-overloaded` 仅 23/32，ASan 仍为空探针环境失败，留给 Linux 双档复核。 — **2026-08-17 Claude 复查返工：通过。** 四条判据逐条量过：20 个单元的 anchor/test 全部两两不同（此前 17 个不满足）；104 条锚点无一命中注释行，且拿 `///` 注释当锚点能被具名拦下；裸字符串格式在 `check_code` 与 `ledger` 双双报红。**三组变异自检各自具名报红**：删 `from_dual_tag` 实现+用例 → 「算法6.10 的实现锚点/测试锚点不存在」；`shell_sort` 改名 → 「算法8.2 的实现锚点不存在」；删一条断言 → 「算法10.3 的测试锚点不存在」——实现侧与测试侧能各自单独变红，不是「编译不过」的另一种说法。普查 104 条：只在 demo.cpp 命中 0、测试锚点只在注释里 0、短于 12 字符 0。**复查中改掉两处**：① `代码10.4` 底稿是「集合 mySet 的 ADT」（类定义），却绑到 `bool erase(int value)`，而同单元 10.1/10.9 都按类绑、`class IntSet {` 空着——已改（这条机器判不出来，是对着底稿标题逐条核出来的，属 D-014 的人工项）；② 11 处 `x = x;` 改走引用别名，解掉 clang `-Wself-assign-overloaded` 在 `-Werror` 下拦住 9 个教学版单元的问题（Codex 本机因此只能跑 23/32）——断言数一条不差，且把拷贝赋值改成「先 delete[] 再拷贝」后该测试照样红，没有变哑；**本机无 clang，只验证了 g++ 上不坏，clang 侧待 Codex 确认**。**Linux 双档复核完成**：闸门 9 步全绿 `real 3m1.060s`，190 项自测 OK，**32/32 单元双档**（debug+ASan/UBSan 与 release-O2），Codex 那边挂着的 ASan 空探针一项不必再等。**欠账一笔**：新补的 `算法3.7` 是合格的驱动函数（底稿 2285 行确为「计算阶乘的主程序」），但原书那段的三处硬伤（`void main( )`、读了 x 却调 `factorial(4)`、缺右花括号）没进 `legacy.md`，按 D-007 第 2 条该补 → T-027 |
| T-030 | **第 8 章补课**：把原书有、新书空着的 7 节写出来 | Review | Claude | 人于 2026-08-17 拍板。原书 19 节，新书原本只有 12 节：8.3.1 直接选择排序、8.4.1 冒泡排序、8.6.1 桶式排序、8.6.3 索引排序整节没写，8.7 只有一段话（原书是 8.7.1/8.7.2/8.7.3 三节），8.2.1 被改题成「先跑一遍」——而这三种排序的实现、测试、清单认领**一直都在**。现已补全：全章 276 → 597 行，19 节齐备。新印的代码全部走 R3（`#fn:` 切片 + 新增 `#static-queue`/`#stopwatch` 两个锚点），**此前从没印进书的 6 条清单**（算法8.2/8.7/8.9/8.13、代码8.12/8.16/8.17）这次都印出来了。手工演算 trace 是**跑出来的**不是手编的：Shell/选择/冒泡三张表由与实现同逻辑的脚本生成，选择排序那张与原书图8.3 逐行一致；索引排序那组算出来的 IndexArray2 与原书表8.1 逐格相同（顺带发现底稿把置换环印成 {0,5,4,1}，与它自己列的元素对不上，实为 {0,5,4,2}，多半是扫描损坏，已在正文注明）。8.7.2 的时间表是**本机实测**（g++ 13.3 -O2）：5 万随机数，冒泡 5821.9ms、插入 213.5ms——同为 Θ(n²) 差 27 倍；2 万已排好序的输入，优化快排 105.9ms 而插入 0.0ms——**快排在这种输入上比 Θ(n²) 的插入排序还慢 100 倍**。8.7.3 补了判定树下界 Ω(n log n) 的证明 |
| T-027 | 补 `算法3.7` 的原书缺陷证据（`legacy.md`） | Done | Claude / Codex 收口 | 原书清单、`void main`、缺 `std::`、缺右花括号的编译证据，以及“读入 x 却调用 factorial(4)”的语义矛盾均已记录；现代 `factorial_driver` 有绑定测试。编译器输出围栏已正名为 `console`，T-026 的 R8 也会扫描本文件。 |
| T-028 | **编号漂移普查**：同号不同题的节 | Done | Codex 实现 / Claude 复查 | 最新书稿普查出 29 处同号标题差异：18 处为措辞/排版，11 处为真实换题（`2.2.x`、`2.3.x`、`4.2.x`、`5.2.1`、`7.4.x`）。已全部恢复原书编号题名，本书教学步骤改用无编号标题或字母后缀；新增 R11，规范化只忽略排版差异，不做模糊语义匹配。同号异题变异会具名报红。恢复后暴露 `2.3.3 循环链表` 确实缺失，登记为第 6 条 pending（→ T-029）。决策见 D-017 — **2026-08-17 Codex 已做，Claude 复查通过**：`5a12f21` 把被占用的编号还给原书（4.2.1 还原成「字符串的顺序存储」、2.3.2 还原成「双链表」等），教学步骤降为不带编号的 `####`，进阶节改成 2.2a/2.3a/4.2a，并新增 **R11**（同号必须同题，规范化后精确比较，不做模糊匹配）。实测变异：把 5.2.1 的题名改回「先跑一遍」，R11 当场报「同号不同题」。**顺带逼出一笔真债**：原来的「2.3.3 插入与删除」占着号，R10 因同号而以为已覆盖，还原编号后才暴露原书 2.3.3 循环链表整节没写（Codex 已登记为 pending，待补数 5 → 6）。**复查中补了两处**：① 改编号留下 **9 处悬空引用**（「后面 2.2.1–2.2.4 各节」「见第 2.3.2a 节」「见 4.2.5」「见 11.6 的四条练习路径」…），R6/R7/R10/R11 都不看这一层——已新增 **R12**（D-017）并清掉全部悬空引用，另有 12 处「光写数字不带『节』字」的过期引用手工清理（含 3 处在 `teaching.hpp` 注释里、会随 R3 印进书稿，以及 `doubly_linked_list/test.cpp` 里 40 条标着「2.3.4」的断言标签 → 2.3.2）；② `### 4.2.2` 下紧跟同级 `### 教学版：完整实现`，导致 4.2.2 正文为空——已把教学版降为 `####` 并补上正文，全书扫过同类问题只此一处 |
| T-029 | 补 6 个 `pending` 节 | Done | Codex 实现 / Claude 复查 | 6 节均已按原书编号补回：2.3.3 讲循环链表不变量与边界；4.1.1/4.1.2 以现代 UTF-8/码元口径纠正原书过时叙述；5.5.2 说明优先队列接口、复杂度并接回已验证的 `MinHeap`；7.3.3 讲十字链表的双链不变量；11.4.4 给出静态/动态索引的工作负载选型表。`section_gaps.json` 的 6 条 pending 已清零。 — **2026-08-17 Codex 已补齐 6 节，Claude 复查通过**（`3fc9030`）：待补 6 → 0。核过三件事：十字链表用的是原书域名（`tailnextarc`/`headnextarc`/`firstinarc`/`firstoutarc`，底稿 6106 行逐个对过）；5.5.2 对本书 `MinHeap` 的接口描述（`insert`/`remove_min`/`std::optional`）与源码一致；这六节在原书里都不含清单，所以纯文字合规、不欠 R3 的代码块。**ASan 档在 Linux 上补跑**：完整闸门 exit=0，32/32 单元双档，Codex 那边的 Release-only 降级可撤。**复查中补两处**：① `0--127`/`1--4`/`` `0`--`9` `` 是 LaTeX 破折号写法，Markdown 不认、站点原样印出两个减号，已改；② **`"123" < "1234" < "23"` 只对 `std::string` 成立**——两个字符串字面量之间的 `<` 比的是数组地址，实测 `("123" < "1234")` 为 **false**，g++ 报 `-Warray-compare`；照书上那行敲进编译器会得到相反答案，已在 4.1.2 补实测输出与说明。**记一笔没做的**：全书 `“”` 与 `「」` 混用（94 : 758），不统一是因为不少属于引用原书（插图/勘误/习题答案），一刀切会伤引文 |
| T-026 | **禁「长的、形似 C++ 函数」的 ```text 块**（D-010 2026-08-17 补充条款） | Done | Codex 实现 / Claude 复查 | R8 已改为结构判定：至少 60 个规范化字符且含名字、参数表和函数体的 `text` 块一律报红，源码已经漂开仍能抓。原书引文必须写 `original-listing="具体理由"`，空理由报红；扫描 `book/**/*.md` 和 `code/**/legacy.md`。真实基线仅豁免两处原书引文；编译器输出改标 `console`，省略号语法与控制流 trace 不误报。新增漂移、豁免、空理由、trace、legacy 回归测试。 — **2026-08-17 Codex 已实施（`4210e09`），Claude 复查后补两处**。实施部分认可：判据从「像不像我们的源码」换成「长得像不像一段实现」，豁免语法 `original-listing="具体理由"`（空理由报红），范围扩到 `code/**/legacy.md`，全仓只需豁免两处原书引文——普查基线比预想的干净。**复查发现两个洞**：① **豁免能赦免「逐字抄自 code/」**——给逐字抄来的 `push()` 加一句 `original-listing="就想这么写"` 就从报红变放行，等于把 D-010 堵的口子换名重开；已订正为 `copied_from_source` 命中即报红，与有没有理由无关（豁免只赦免「像函数」，不赦免「是本书自己的代码」）。② **`operator=` 整个漏网**——名字正则只认 `名字(`，而 `operator=(` 的名字后面是 `=`；本书讲的正是三法则/五法则，最可能被抄成 text 块的恰恰是拷贝赋值运算符。已纳入 `operator=`/`[]`/`()`/`->` 一族。各配自测，另加一条**走 subprocess 的范围测试**（「扫哪些文件」只在 `main()` 里定，直接调 `check_r8` 的单测证明不了范围）。自测 215 → 220，变异自检 5/5 全抓 |

| T-031 | **后半本体例拉齐：第 8–12 章补讲解、图和演算** | In Progress | Codex | 分章交付，避免大提交难复查。基线行数：ch08 742、ch09 312、ch10 481、ch11 505、ch12 478；第 8 章已由 T-030 补齐。**第 1 批 ch09 已完成**：补 I/O 成本模型、页内记录组织、外排序两阶段、置换选择逐步表、3000 条记录二路归并 4 趟演算、四路选择树完整 trace、稳定性与失败边界；复用原书图 9.1、9.3–9.8。**第 2 批 ch10 已完成**：补二分区间 trace 与决策树、分块检索原书实例、集合位表示、散列函数取舍、拉链结构、墓碑状态演算、装载因子曲线与结构选型；复用原书图 10.1–10.3、10.5–10.7、10.11，并纠正空集成员关系/子集关系的歧义。**第 3 批 ch11 已完成**：补 1000 条记录四层索引演算、多分树扇出、倒排双指针与短语位置、B+ 树分裂/删除路径、位图逐位查询、签名假阳性和结构选型；复用原书图 11.1–11.4、11.9–11.11、11.15–11.16。后续处理 ch12，最后做跨章体例复查。 |

## Decision Log

> **决策全文在 `collab/DECISION_LOG.md`**，这里只留索引。
> 两份可编辑的决策副本一定会各自腐烂，所以这里不复制正文。

| 编号 | 日期 | 决策 | 谁拍的 |
| --- | --- | --- | --- |
| D-001 | 2026-08-12 | DSA 教材 C++ 现代化风格公约（T-006）：C++17、STL 边界、错误处理与 I/O、命名 | **人** |
| D-001 §3b | 2026-08-12 | 补充条款：新增 `const T* peek() const noexcept` 作为 `optional<T> top()` 的零拷贝补充 | **人** |
| D-001 §3c/§3d | 2026-08-12 | 提取用 optional、按键删除用 bool；递归树周游保留并明示深树栈溢出风险 | **人** |
| D-005 | 2026-08-12 | 扩容搬迁判据落在「移动赋值是否 noexcept」；由此产生的元素类型约束写成 static_assert | Codex 发现 / Claude 定案 |
| D-006 | 2026-08-12 | 闸门先自检 sanitizer 环境，环境问题用退出码 2 与代码问题区分；降级出口必须吵 | Claude 记录 |
| 退场 #1 | 2026-08-12 | 代码4.2 退场：它是标准库 `basic_string` 的空壳声明，无可现代化内容（顺带记：往 `namespace std` 加 typedef 是 UB）| Claude，见 `exclusions.json` |
| D-002 | 2026-08-12 | `dsa_raw.md` 永久只读 | Claude 记录 |
| D-003 | 2026-08-12 | 书稿代码块与源码一致性靠机器保证（R3 + sync_book） | Claude 记录 |
| D-004 | 2026-08-12 | 闸门跑 Debug+ASan/UBSan 与 Release-O2 两种构建 | Claude 记录 |
| D-011 | 2026-08-15 | 网页版存 `book/site/`（插图不复制第二份），由 Actions 现场重建后发到 GitHub Pages | Claude 记录 |
| D-013 | 2026-08-16 | 课件用 Markdown 源（`book/slides/*.md`）+ 网页版，不生成 .pptx；新增 `#fn:名字` 按函数名取切片 | **人** |
| D-012 | 2026-08-16 | 书稿正文印**教学版**（`teaching.hpp`，三法则、整块印全），工程版降为「进阶（选读）」；`teaching_test.cpp` 与 `demo.cpp` 一并进闸门 | **人** |
| D-007 §补 | 2026-08-17 | 密度不等于兑现：每条认领要显式绑定实现锚点与测试名，闸门至少查二者存在（→ T-025） | Codex 提 / Claude 定案 |
| D-010 §补 | 2026-08-17 | R8 反向的沉默是子串匹配自带的；不放宽成相似度阈值，改禁长的、形似 C++ 函数的 text 块（→ T-026） | Codex 提 / Claude 定案 |
| D-014 | 2026-08-17 | 图与公式：能机器判的是「渲染得出来吗」（新增 R9），数学与图的正确性仍是人工复核项 | Claude 记录 |
| D-015 | 2026-08-17 | 算法6.10 拒绝三类不自洽序列，不为标准答案放宽（末结点必 ltag=rtag=1；入栈出栈必须配对） | Codex 定案 |
| D-016 | 2026-08-17 | 原书分了哪些节由底稿说了算：新增 R10 + `section_gaps.json`（并入/有意不写/待补，各带理由责任人日期） | Claude 记录 |
| D-017 | 2026-08-17 | 节号引用也要能解析：新增 R12（默认按新书解析，带「原书」标记按底稿解析） | Claude 记录 |

**已被取代**：2026-08-12 早些时候 Claude 提议的「C++20 + concept」被 D-001 取代为
C++17 + `static_assert`。原提议与取代理由都保留在 D-001 里，没有删除。
