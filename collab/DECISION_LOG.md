# DECISION_LOG · 已生效的项目决策

> **本文件是「已拍板决策」的权威出处。** `collab/PLAN.md` 的 Decision Log 只记一句摘要
> 加一个指向这里的锚，避免两份会各自腐烂的副本。
>
> 决策一旦写进这里就是项目标准，两个 agent 无需再问；要推翻必须由人重新拍板，
> 并在原条目下**追加**「被 XXXX-XX-XX 的决策取代」，**不要删除原条目**——
> 来回可查比看起来干净重要。

---

## D-001 · 2026-08-12 · 人已拍板：DSA 教材 C++ 现代化风格公约（T-006）

改造数据结构教材的核心矛盾：**全用 STL（如 `std::stack`）就失去了手写数据结构的
教学意义；全用旧 C++ 又会给学生灌输危险的内存习惯。** 以下四条为红线。

### 1. 语言标准：C++17

所有单元默认 `-std=c++17`。理由：GCC/Clang/MSVC 支持完善，且 `std::optional`、
`std::string_view` 足以支撑安全、无副作用的数据结构接口。

`unit.json` 的 `standard` 字段仍可按单元覆盖，但**偏离 C++17 要在 `legacy.md` 里
写明理由**，并在交接记录里点出来。

> **取代记录**：本条取代 2026-08-12 早些时候 Claude 提议的「C++20 + concept」。
> 原提议的动机是用 concept 替掉原书那个「非纯虚、非虚析构」的假抽象基类 `Stack<T>`；
> 在 C++17 下这一角色由 **`static_assert` + `<type_traits>`** 承担——同样是编译期检查、
> 同样不付虚表代价，报错信息也在实例化处，只是不能像 concept 那样参与重载决议。
> 落地见 `code/ch03/array_stack/modern.hpp` 顶部的三条 `static_assert`。

### 2. STL 使用边界（核心权衡）

- **禁用（教学核心）**：不得用 `std::vector` / `std::stack` / `std::list` / `std::map`
  等容器**直接替代**该章节要讲的手写实现。把 `ArrayStack` 做成 `std::stack` 的薄封装
  等于把这一节删掉。
- **允许（现代基础设施）**：
  - 类型与辅助：`std::size_t`（替换 `int` 表示长度/下标）、`std::swap`、
    `std::initializer_list`、`<type_traits>`、`<utility>`。
  - 接口返回值：推荐 `std::optional<T>`，替换原书「返回 `bool` + 出参」的双通道设计。
  - 内存管理：**演示手写指针/动态数组时，显式编写 Rule of Five**；
    不涉及指针教学的上下文，优先 `std::unique_ptr` 管理底层数组。

> **第 3 章顺序栈按前者办**：存储结构本身就是这一节的教学内容，所以用裸 `T* data_`
> 加显式五法则。这是有意的——用 `unique_ptr` 时五法则基本是仪式（编译器生成的就够用），
> 用裸指针它才是承重的，学生才看得见「为什么必须写这五个」。
> 代价一并明写：扩容要手写 `try/catch` 清理新缓冲区，而 RAII 版本不用。

### 3. 错误处理与 I/O（彻底清除 `cout`）

- **红线**：数据结构类内部**严禁**出现 `std::cout` / `std::cerr`。原书「溢出时
  `cout << "栈满溢出"`」这类写法必须彻底清除，由 `test.cpp` 重定向流并断言其为空来守住。
- **可预期的空状态**（`pop()` / `top()` 在空容器上）：返回 `std::optional<T>`，
  空时 `std::nullopt`。不是错误，不抛异常。
- **非法参数 / 越界 / 容量溢出**：抛标准异常——`std::out_of_range`（下标越界）、
  `std::overflow_error`（容量翻倍溢出）、`std::invalid_argument`（参数非法）。

### 3b. 只读访问：`optional` 与裸指针各司其职（2026-08-12 补充条款，人已拍板）

「读栈顶」这件事有两种正当需求，公约同时提供两个接口，**不要合并**：

| 接口 | 返回 | 用在什么时候 | 代价 |
| --- | --- | --- | --- |
| `std::optional<T> top() const` | **副本** | 要把值带走、存起来、跨越后续修改 | 拷贝一次；`T` 必须可拷贝 |
| `const T* peek() const noexcept` | **指针**，空栈为 `nullptr` | 只看一眼、零拷贝；move-only 元素唯一可用的观望方式 | **指针在下一次 push / pop / clear 后失效** |

写下这条的理由：`optional<T>` 的安全是靠拷贝换来的，对 `std::unique_ptr` 这类
move-only 元素直接不可用；而裸指针零拷贝，代价是生命周期由调用方负责。
两种代价都真实存在，**藏起任何一种都是把选择权从读者手里拿走**。
失效契约必须写进接口注释与书稿正文，不能靠使用者猜。

落地：`code/ch03/array_stack/modern.hpp`；守门用例
`test_peek_does_not_copy`（`Counted` 计拷贝次数，断言 peek 为 0、top ≥ 1）、
`test_peek_on_empty_is_nullptr`、`test_peek_after_growth_is_refetched`。

### 3c. 提取与按键删除的统一口径（2026-08-12 补充条款，人已拍板，T-010）

- `pop()`、`dequeue()` 等**提取**操作：返回 `std::optional<T>`。空容器是可预期状态，
  返回 `std::nullopt`，不抛异常。
- `remove(key)`、BST 删除等**按键删除**操作：返回 `bool`。删到键返回 `true`，键不存在
  返回 `false`；这是一项幂等、可预期的轻量级失败，不是逻辑错误。

这条取代此前「线性表按位置 remove 返回 T 并在越界时抛异常」被误解为通用删除口径的风险。
按**位置**访问/插入/删除的非法下标仍是 `std::out_of_range`；本条只统一提取和按键删除。
原因是 C++17 的 `optional` 直接表达预期缺失，避免教学代码充满与算法无关的 try/catch，
也不以 `assert` 或未定义行为处理空状态。

### 3d. 递归周游的教学边界（2026-08-12 补充条款，人已拍板）

二叉树的递归前序/中序/后序周游保留为主实现：递归结构正是该节要教的语义，不强制改为
显式栈。实现注释与 `legacy.md` 必须标明：极深或病态树的递归深度受进程调用栈限制，存在
栈溢出风险。漂亮的迭代版可作为补充，不能替换主教学实现。

**2026-08-12 人补充要求：把「没能跑 ASan」的风险点像写遗嘱一样交代清楚。**
落地为 `collab/UNVERIFIED-RISKS.md`，其中第一节给出实测数字（Linux/gcc 13.3/8MB 栈）：
递归析构与周游在 50 万深度通过、100 万 SIGSEGV；Debug+ASan 档周游 50 万即
stack-overflow。**关键在于出事时的可诊断性**——ASan 档会打印
`AddressSanitizer: stack-overflow` 加完整递归回溯并指到具体行号，
而 Release 档只有一个裸 SIGSEGV，零诊断。
凡新增递归结构（第 6 章树、第 7 章图）动手前先读那一节。

### 4. 命名与 API 风格

- 类名 `PascalCase`；函数与变量 `snake_case` 或 `camelCase`（单元内保持一致）；
  私有数据成员后缀下划线（`capacity_`、`data_`）。
- **彻底消除成员变量与成员函数重名**——原书 `int top` 与 `bool top(T&)` 的冲突
  正是全书第一个被编译器拒绝的硬伤。栈顶位置一律用 `top_index_` 之类的名字，
  `top()` 留给成员函数。
- 查询函数标 `[[nodiscard]]` 与 `noexcept`（当且仅当真的不抛）。

---

## D-005 · 2026-08-12 · 红队 T-002 结论：搬迁判据与容器对 `T` 的约束

**结论**：容器扩容搬迁元素时，判据必须落在**实际执行的操作**上，不是惯用法。
本项目的规则：移动赋值 `noexcept` → 移动；否则 → 拷贝。
由此产生一条对元素类型的真实约束，写成 `static_assert`：
不可拷贝赋值的 `T` 必须 `is_nothrow_move_assignable`。

**来由**：Codex 红队用「移动构造 noexcept、移动赋值抛」的类型打穿了第一版实现
（`std::move_if_noexcept` 看的是移动构造）。Claude 复核时发现第一版修复
「可拷贝就拷贝」过宽，`std::string` 这类元素被迫深拷贝，摊还 O(1) 打折。
两版的差别只有一条数拷贝次数的用例能分辨——**该用例已补，判据由它守着**。

**代价明写**：move-only 且移动赋值可抛的类型，本容器直接拒收（编译期）。
提供基本保证而非强保证是另一条路，本项目选了前者：教学容器的行为要好解释。

## D-006 · 2026-08-12 · Claude 记录：闸门要能区分「环境坏了」和「代码坏了」

`check_code.py` 在跑任何单元之前，先用一个空程序试 sanitizer 能否启动
（`sanitizer_preflight()`）。失败则以**退出码 2**（区别于代码问题的 1）报告
「是这台机器跑不起来」，并给出复现命令。

**来由**：红队那轮在 macOS 上撞到 `sanitizer_malloc_mac.inc:189
(!asan_init_is_running)`——连空探针都挂，闸门把每个单元都判红。
Codex 是自己手动做探针才定位到环境问题的；这个诊断应当由工具给出。
（同一提交在 Linux 上闸门全绿，58 项断言双构建通过，佐证了这是环境问题。）

**降级出口的形状**：`--allow-degraded` 只在自检失败时生效，跳过 sanitizer 档、
只跑 Release，并在输出开头与结论旁**各喊一次**降级。降级会随交接包被看到——
按红线第 7 条，宁可吵，也不要悄悄变绿。

## D-007 · 2026-08-12 · Claude 记录：闸门新增「实质性检查」

**背景**：2026-08-12 一次提交用 542 行覆盖 61 条清单、8 章书稿，台账随即报
「104/105 已现代化，0 待办」。逐一核对后发现是**结构合规、内容近乎为空**：

| 单元 | 清单数 | 断言数 |
| --- | --- | --- |
| 第 8 章 排序 | 17 | 11 |
| 第 10 章 检索与散列 | 13 | 4 |
| 第 7 章 图 | 11 | 5 |
| 第 6 章 树 | 10 | 7 |

对照：`ArrayStack` 3 条清单 58 项断言，`Knapsack` 3 条 54 项。
第 8 章的 `quick()` 是 `std::sort`、`heap()` 是 `std::make_heap`、
`radix()` 直接调 `counting()`、`cycle_index()` 调 `quick()`——
**该章的正题（快排、堆排、基数排序）一个都没实现**，直接违反 D-001 §2；
`legacy.md` 普遍只有两行、零证据。

**这个空档在 2026-08-12 第一轮的 `NOTES-claude.md` 里被预言过**：
「台账只校验『认领的编号在原书里存在』，不校验『这个单元真的实现了那条清单』。
一个 `unit.json` 完全可以认领算法7.6 却实现一个空类。」同日被兑现。

**决定**：`check_code.py` 新增 `check_substance()`，两条判据：

1. **断言密度**：每条清单至少 3 项断言（单元下限 5 项）。
   现有扎实单元是每条清单 8–18 项，下限取 3 已经很宽。
2. **`legacy.md` 必须带可复现证据**：至少 20 行实质内容，
   且含 `error:` / `runtime error` / `Sanitizer` / `$ g++` / `$ ./` 之一。
   红线本来就写着「每条缺陷都要有证据」，两行说明不构成证据。

**不设逃生口**。真有正当理由低于下限，那是该来这里立一条决策的事，
不是再开一个 `substance_waiver` 让它悄悄溜过去。

**这条规则同样拦下了 Claude 自己的两个单元**（`expression_eval` 的 legacy.md
没有编译器输出、`recursion_and_stack` 密度差 2），已按同一标准补齐——
判据若只对别人生效，它就不是判据。

> **2026-08-12 当日更正（Claude）**：上面那段表格与措辞把问题说过头了。
> 逐个读完实现之后：第 6、7、9、10、12 章的**实现是真的**——并查集带路径压缩、
> DFS/BFS/拓扑排序/Dijkstra/Floyd/Prim/Kruskal、竞赛树与置换选择、
> 散列表的墓碑探测、最优二叉搜索树的动态规划，都确实写了，
> 只是压成了超长单行，所以按行数看像空壳。**我从行数推断内容，这一步错了。**
>
> 站得住的结论收窄为三条，前两条是机器判定的、第三条是逐行读出来的：
> ① **测试密度普遍严重不足**（0.3–0.7 项/清单，前五章是 8–18 项）；
> ② **`legacy.md` 普遍两行、零证据**——这违反「每条缺陷都要有证据」的红线，
> 而逐条核对原书、抓出编译级硬伤正是本项目的全部意义；
> ③ **第 8 章确有 D-001 §2 违规**：`quick()` 是 `std::sort`、`heap()` 是
> `std::make_heap`、`radix()` 调 `counting()`、`cycle_index()` 调 `quick()`。
>
> D-007 这两条判据本身不受影响——它们量的正是①②，而且量得准。

## D-008 · 2026-08-14 · Claude 记录：原书没有清单的内容，允许 `listings: []` + `beyond_book`

**背景**：第 11 章（索引技术）在 `dsa_raw.md` 的 105 条清单里**一条都没有**，12.3 的
Trie/Patricia、12.2.1 的广义表同样只有文字和图。这些内容要做成「实现并测试」，就必须建
`code/` 单元；但 `ledger.py` 原来硬性要求每个 `unit.json` 有**非空**的 `listings`，
新单元根本入不了库。

三条路走过一遍：

| 做法 | 问题 |
| --- | --- |
| 让新单元认领邻近章节的清单 | 台账的「已现代化」会虚高，`covered` 不再等于「这些清单真被实现了」 |
| 把这些内容永远留在「概念导读」 | 第 11 章整章没有可运行代码，与「测试是正文的一部分」矛盾 |
| 允许空 `listings`，但必须自报家门 | 采用 |

**决定**：`listings` 可以为空，**当且仅当**同一个 `unit.json` 里有非空的 `beyond_book`
字符串，说明原书为什么没有对应清单。空字符串、只有空白都不算数——否则「忘了填 listings」
和「本来就没有清单可认领」在台账里长得一模一样，正是 D-007 想堵的那种「结构合规、内容为空」。

**为什么这不削弱 105 的等式**：空 `listings` 对 `claimed` 的贡献是零，
`covered + excluded + pending == 105` 逐字不变。台账报告新增一行
「其中 N 个单元不对应原书清单」，把这类单元显式数出来，而不是让它们隐身。

**证据**：`tests/test_ledger.py` 三个用例——有 `beyond_book` 的空 listings 通过、
空白字符串仍然报错、以及「新增单元不认领任何清单」的等式检查。

## D-002 · 2026-08-12 · Claude 记录：`dsa_raw.md` 永久只读

它是「原书到底怎么写的」唯一凭据，修订一律落在 `book/`。
`tests/test_ledger.py::test_real_book_inventory_is_stable` 用
105 条清单 / 70 算法 / 35 代码 / 5 条缺结束标记这四个数字锚住它，被动过立刻变红。

**收益已兑现**：T-001 抓到原书两处编译错误，正是靠这条才说得清那是原书的问题，
不是我们改出来的。**代价**：书稿与底稿之间没有自动 diff，靠人工对照。

## D-003 · 2026-08-12 · Claude 记录：书稿代码块与源码的一致性靠机器保证

书稿里的 ```cpp 块必须写 `file=code/.../modern.hpp#anchor`，`check_doc.py` R3 逐字核对，
`sync_book.py --write` 负责灌入。理由：原书的错误之所以能印进教材，
正是因为「书上的代码」和「跑过的代码」从来不是同一份东西。

## D-004 · 2026-08-12 · Claude 记录：闸门跑两种构建，缺一不可

`-Wall -Wextra -Wpedantic -Werror` 之上，Debug 挂 ASan+UBSan（`-fno-sanitize-recover=all`），
Release 跑 `-O2`。**实测依据**：同一段堆越界在 Debug 档被 UBSan 当场 abort，
在 `-O2` 档**静默通过**（`tests/test_check_code.py::test_sanitizer_catches_heap_overflow`
把这个现象本身写成了断言）。只跑一种构建会漏。
