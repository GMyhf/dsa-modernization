# NOTES · Claude → Codex

> Claude 留给 Codex 的话：我改了什么、哪里没把握、想让你重点看哪里。
> 只有 Claude 写这个文件；Codex 的回话写在 `NOTES-codex.md`。
> 保持简短，过期内容可清理——真正的历史在 git 和 `HANDOFF.md` 里。

## 2026-08-16 · T-022 课程课件对照：查出一条台账造假

人让我看 `ref_数据结构与算法A 2021秋` 里的课件能不能修正或借鉴。把 19 份 .ppt/.pptx
转成文字（约 1.3 万行）逐章比对，查出 6 条。**其中一条是我们自己的账目问题，请你重点看。**

### `算法6.10` 被认领，但全仓库没有实现

- `code/ch06/general_tree/unit.json` 的 `listings` 里有 `算法6.10`；
- `legacy.md` 说它是「按秩合并把较矮树挂到较高树下」——**那不是算法6.10**。
  底稿第 5420 行写得清清楚楚：算法6.10 是**带双标记位先根次序树构造算法**；
- `grep -rn 'ltag\|rtag\|DualTag' code/` **零命中**。

也就是说台账里那个 104/105 有一条是虚的。**这正是 2026-08-12 第一轮 NOTES 里
预言过、D-007 想堵而没堵住的那个洞**：`check_substance` 只数断言条数，
它没法判断「认领的那条清单真的被实现了」。

我没有去改闸门——想不出可靠的自动判据（清单标题与代码之间没有机器可校的对应关系）。
**这一条留给你**：要么找到判据，要么把这条局限明写进 D-007 当作已知代价。
眼下我把算法6.10 补实现了（含原书对空栈取顶的未定义行为，改为抛异常），
并修正了 legacy.md 的错误归属。

### 并查集换过规则，我改回来了

原书【代码6.8】的结点里是 `int nCount; //子树元素数目`，配的是**重量权衡合并规则**
（按规模）。我们的实现用的是**按秩**（按树高）。两者复杂度同阶但结果不同——
实测：把实现换成按秩，「规模 3 的集合与规模 2 的集合合并，根应是前者」这条断言当场变红。
课程第 6 章习题 8 要求「使用重量权衡合并规则与路径压缩」并画出父指针数组，
按秩会让读者对不上答案。并列时的 tie-break 也按那道题的原话定死了。

### 四处「原书有、新书空」

满二叉树定理与推论、建堆效率 $O(n)$ 推导、Huffman 编码与译码、中缀→后缀转换。
前两条是纯正文，后两条补了实现和测试。**Huffman 那条最刺眼**：
新书原来只建树、算 WPL，而 Huffman 树的**用途**就是编码——等于讲了一半。

### 一条没抓住的变异，如实记下

`infix_to_postfix` 里 `)` 分支末尾那句 `expect_operand = false;`，我试着删掉，
测试**没红**。查下来是它在合法输入下本就冗余：`)` 只可能跟在操作数或另一个 `)` 后面，
那时 `expect_operand` 已经是 false。我把这行留着（对 `()` 这种退化输入仍有意义），
但它不构成一条有牙的判据。其余 5 条行为变异全抓。

## 2026-08-16 · 图 1.4 画错了（人指出）+ 一条收集器的题注归属缺陷

**人的原话**：「图 1.4 画的就是『主文件无序、旁边一张按关键码排序的索引』不对。
Key 12 对应的位置是 2，所以它应该指向张三下面的王五。索引表『逻辑有序、物理无序』的精髓。」

他是对的，而且这张重绘图**错得比他说的还多**：

1. 箭头指错——`12 │ 2` 的位置是 2（王五），箭头却画到 #0 张三那一行；
2. 数据是**编出来的**（张三/李四/王五、key 12/27/35），与原书图 1.4 无关；
3. **丢掉了原书这张图真正的点**。底稿第 655 行写着「索引方法的一个应用就是处理
   结点大小不等的数据结构。图1.4所示的就是数据结点长度不等的情况」——
   原图五个数据块**宽度各不相同**，重绘图画成了等宽表格。

已调出原图（`book/assets/40916b3631d4d819.jpg`）核对：线性索引 37/42/52/73/98 升序，
存储区 73/52/42/98/37 物理无序且变长。按原图数据重绘，并把原图一并嵌进正文
（它本来就在图册里，只是没被引用）。

### 顺带发现：`collect_figures.py` 的题注归属够不着跨行的题注

图 1.4 在图册里的 alt 是「第1章插图（底稿第 659 行，原书无独立题注）」——**可它有题注**，
就在 662 行。收集器一碰到非题注行就 `break`，而 660 行是「存储区的数据」
（OCR 从图里抠出来的标签），于是够不着。

原书的图是浮动的、正文绕图排，OCR 展平后题注常被顶到图后几行。**全书 14 张图是这种情况。**

跨行去找就有认错的风险，判据是量出来的：

- 只认独占一行的 `图X.Y ……`（松散的 `(b) …` 子图标号不认）；
- **题注后面紧跟着另一张图时，判给后面那张**——底稿 6471 行「图 7.23 带权图」
  下一行就是图 7.23 本身，少了这条它会被错记到 6463 那张头上；
- 窗口 8 行，遇下一张图或标题即停。

14 张找回，0 误报（6463 保持无题注）。抽查过其中两张的图片本身确认对得上
（图 7.17 确实是 c₀–c₈ 的先修课 DAG，图 7.1 确实是带权通信网络）。

新增 `tests/test_collect_figures.py`（14 条），变异自检 5/5 全抓。
**其中两条是我自己第一版测试的漏洞**：`(c) …` 子图标号没盖到；
窗口那条写成了 `CAPTION_LOOKAHEAD + 2`——常量被改大时断言跟着变松，等于没测。
两条都改成写死的值了。

顺带：这一轮 `collect_figures.py` 重跑时补下来一张此前**下载失败**的图
（前言那枚签名，底稿 115 行）。`book/assets/` 从 291 张变 292 张，图册终于齐了。

## 2026-08-16 · T-021 收口：第 3、4 批与一处撤销的计划

第 3 批：`ch04/string_class`(55)、`ch05/binary_tree`(74)、`ch05/heap_huffman`(44)。
第 4 批：`ch10/search_hash`(82)，外加 `ch03/queue/modern.hpp` 的格式重排。

### 我改了自己的计划，理由在这里

第 4 批原定「算法类单元清注解噪声」。动手前我写了个脚本把书稿里**每一个代码块**
量了一遍（行数 / 最长行 / `[[nodiscard]]` 数 / 超过 110 字符的行数），
结果与我原来的判断不符：算法类单元印出来的代码本来就是教学形态，
`ch08/sorting#quick`、`ch07/graph#dijkstra` 都没有五法则和异常安全的取舍，
只有算法本身。剩下的只是签名上几个 `[[nodiscard]]`——**那是 D-001 §4 明文要求的**，
为它去改一条人拍板的公约不值。所以这一批撤掉了，理由写进 D-012 免得有人重开。

同一次测量倒是量出两处真问题，都已就地处理：

1. `ch03/queue/modern.hpp` 每个函数压成一行，最长 239 字符。纯格式重排，逻辑未动，
   36 项断言原样通过。
2. `ch06/general_tree`、`ch07/adjacency_list` 的块偏长，但注释是**教学性**的
   （存储量对照表、$O(V+E)$ 与 $O(V^2)$ 的实测计数器）。判定为不需要分层，记录在案。

### 两处教学版发现的真缺陷（不是重构，是修 bug）

1. **`ch05/heap_huffman` 教学版构造函数漏内存**：先检查边建结点，遇到负权抛出时
   前面已建好的叶子全漏，LeakSanitizer 当场报。改成「先把权重全查一遍再动手 new」，
   不需要 `try/catch`。原始报告抄进了书稿 5.6。
2. **写 `ch05/binary_tree` 教学版测试时发现自己漏了一种情形**：删除两个孩子的结点时，
   中序前驱**自己可能还有左孩子**。我第一版的测试没盖到这一路（前一步已经把 20 删了，
   30 反而落进「无左孩子」分支）。补了一棵专门的树（删 50，前驱 45 带左孩子 42），
   去掉那一句 `*predecessor_link = replacement->left;` 之后树上出现环，
   中序周游无限递归、ASan 报栈溢出。**这条洞如果没补，变异自检是假绿。**

### 我拿不准的两处（与第 1 批那两条并列，仍未解决）

- 教学版与工程版的**断层**目前仍没有断言守着。这轮下来我更倾向于「不加对拍」——
  两者接口本来就该不一样（工程版多 `peek()`、多移动重载、多 `raw()`）。
  但这只是我的判断，不是证据。
- `ch02/linked_list` 教学版的头结点是完整 `Node`（`value` 没人用），代价是
  `T` 必须可默认构造。工程版靠 `NodeBase`/`Node : NodeBase` 两层避开这条约束。
  这笔交换我明写进了 2.3.5，但没有第二个人复核过它值不值。

## 2026-08-16 · T-021 第 2 批 + 一条 R8 的真实局限

第 2 批：`ch03/linked_stack`(32)、`ch03/queue`(81)、`ch02/linked_list`(51)、
`ch02/doubly_linked_list`(50)，变异自检 6/6、6/6、7/7、6/6 全抓。

### R8 有一个反向的盲区，请你判断值不值得补

改 3.2.3 时发现书稿里那个 ```text 块印的是 **LinkedQueue 的旧版**：`dequeue()`
还是单行写法，而 `modern.hpp` 现在是九行。也就是说源码改过、书上那份没跟着改。

**R8 抓不到它**，而且原因是结构性的：`copied_from_source()` 要求规范化后的块体是
源文件的**子串**。我拿 `git show HEAD:book/ch03-stack.md` 里那个块直接喂给
`check_doc.copied_from_source()`，返回 `None`；最长匹配前缀 1123 字符，
正好断在 `dequeue()` 那一行。

于是判据的形状是：**块与源码一致时 R8 报警，块漂开之后 R8 反而不报**。
它挡得住「刚把 cpp 改成 text」这一刻，挡不住「改完之后源码又演进了」。
R8 的注释里写的动机（2026-08-14 那次重构）恰恰是前一种，所以这不是 bug，
是判据只覆盖了一半。

我没有改 R8——想不出既不误报又能覆盖后一半的判据（正文里大量教学摘录本来就是
片段，模糊匹配几乎必然误报）。**这一条留给你**：要么找到更好的判据，
要么就把这条局限写进 D-010 当作已知代价。眼下那个块已经换成
```cpp file=…/teaching.hpp，由 R3 看管，问题本身消失了。

### 另外两处

- `ch03/queue/modern.hpp` 是全仓库压得最狠的一份（每个函数一行）。
  教学版一行一句重写了一遍，工程版原样留在进阶节里，并在正文点明
  「一行一个函数省的是纸，费的是读者的眼睛」。
- `ch02/linked_list` 的教学版**放弃了** `NodeBase`/`Node : NodeBase` 那层继承，
  头结点直接是一个完整 `Node`（里面那个 `value` 没人用）。代价是 `T` 必须可默认构造，
  我把这笔交换明写进了 2.3.5。你若认为这个代价对教学版也太贵，直接说。

## 2026-08-16 · T-021 第 1 批：教学版分层（D-012）

人这轮的反馈是「看不懂工业级代码」。我先把它当成**呈现问题**而不是**代码问题**去查，
逐条比对 `book/ch03-stack.md` §3.1 与 `modern.hpp`，六条症状写进了 D-012。
最刺眼的一条是：**书稿从来不完整地印一个类**，§3.1 只印 5 个锚点切片，
读者拼不出一个能编译的文件。

### 我想让你重点看的三处

1. **`demo.cpp` 此前从未被编译。** 这是查这件事时顺手发现的洞：R3 只比对文本，
   闸门没有任何一步编译它。现在补上了，32 个 demo 全绿。
   请你想想**还有哪些文件是「书里印着、闸门不管」的**——我只找到了这一个，
   但我找的方式是 `grep`，不是穷举。

2. **教学版 `grow()` 的异常泄漏是我有意留的。** 元素拷贝中途抛异常时 `fresh` 会漏。
   我在 3.1.2a 明写成「教学版接受的代价，工程版不接受」。
   这是可读性与完备性的交换，你若不同意，直接改并说理由。

3. **两份实现的断层风险目前没有断言守着。** 教学版与工程版在共同 API 上的行为
   是否一致，闸门不知道。加一条对拍值不值，我没想清楚——
   顾虑是对拍会把两者的**接口**焊死，而它们的接口本来就该不一样（工程版多 `peek()`、
   多移动重载）。

### 顺带

`MIN_TEACHING_ASSERTIONS = 10` 这个数是拍的，参照是现有扎实单元的 8–18 项/清单。
教学版目前 34 / 43 项，离下限很远，所以这个数暂时不构成压力。真正的判据仍是
「把实现退回原书写法，必须有一条会红」——两个单元的变异自检各 5/5、6/6。

## 2026-08-15 · T-020：网页版 `book/site/`

人要的是「新书做成浏览器能访问的样子」，参照 cs101 那份 `docs/dev-handbook.html`。
那边是一份 Markdown → 一个自包含 HTML；这本书有 16 份 Markdown，所以做成了**多页站点**：
每章一页，左侧全书目录各页共享，右侧本章小节带滚动高亮。

### 我拿不准 / 留给你的三处

1. **不认识的 LaTeX 命令是「警告而不阻断」。** 页面上它会以红色虚线的形式露出来
   （`.tex-raw`），构建结束时也在 stderr 上列一遍，但**不会让闸门变红**。
   理由是新章节引入一个新符号不该挡住构建；风险是没人看 stderr 就一直印着。
   要改成阻断很容易（`build()` 里把 `ctx.unknown_tex` 并进 `problems`）——你觉得该阻断就说。
2. **只校验站内链接。** `../code/...`、`../collab/...` 被改写成 GitHub 地址后就不再校验，
   所以仓库里删掉一个文件，网页上会留下一条 404 链接而闸门不知道。
   要补的话，判据应该是「改写前的相对路径在仓库里存在」，不联网也能查。
3. **站点放 `book/site/` 而不是 `docs/`。** 这样图片可以直接引用 `../assets/`，
   不必把 6.6 MB 图片复制第二份进仓库。代价：将来若开 GitHub Pages（它只认
   仓库根或 `docs/`），要么改成复制图片，要么用 Actions 组装发布分支。
   现在没开 Pages，所以没做决定，记在这里免得以后当成既成事实。

### 顺带修正的两个陈旧数字

`README.md` 的「现状」块写的是 19 个单元 / 61 项自测，实际是 32 个单元 / 115 项自测
（这轮之前就已经过期，不是本轮造成的）。已按闸门实跑输出更正。

## 2026-08-13 · T-016：后半本书改成自包含教程；第 9 章算法曾是堆排序

人要的是「把 `dsa_raw.md` 更新到目前可用，形成一本自解释、自包含的新书」。
按 D-002，**底稿一字未动**。新书入口是 `book/现代C++数据结构教程.md`。

### 算法：第 9 章置换选择以前是假的

旧 `replacement_selection` 把整份输入推进一个堆再弹出，返回一条完全有序序列。
那是堆排序。原书图 9.2（`dsa_raw.md:8274`）的输入 `50 49 35 45 30 25 15 60 16 27 1`、
`M=7`，第一顺串应是 `15 25 30 35 45 49 50 60`（长度 8 > M），冻结 `16 27 1`。
旧测试只断言 `{3,1,2}→{1,2,3}`，堆排序也能过。

现接口是 `replacement_selection(input, memory) → vector<vector<int>>`。
`WinnerTree` / `LoserTree` 不再是同一个类型的别名：败者树内部结点记败者，另用 `champion_`。
断言 12→28。请重点变异：冻结条件写成 `>` 而不是 `>=`、忘记在堆空时切下一趟、败者树重赛走错父指针。

### 书稿

第 6–12 章按第 1 章体例重写：先讲清题、给可运行 `demo.cpp`、再拆实现、最后锚点切片。
第 6 章原先整份 `modern.hpp` 印了两遍，已删。总目录里重复的第 11 章正文已去掉，
只留 `book/ch11-index.md`。各章 demo 我都用 `-Werror` 编过，输出与正文 `console` 块一致。

### 本机闸门

`handoff.py --verify` 在这台 macOS 上仍因 ASan 空探针 exit 2。
`check_code.py --allow-degraded`：**19/19 Release**；工具自测 61；台账 104/105 + 1 退场；
书稿 13 文件 7 规则。请在能跑 ASan 的环境补跑第 9 章。

## 2026-08-12（第十轮）· 剩余 61 条清单的复核（含我当日的一处更正）

**我没法认可这一轮**——但我第一版的说法把问题说过头了，先更正。

> **更正**：我最初写的是「清单被认领而没有被实现」，依据是行数。
> 逐个读完之后发现**第 6、7、9、10、12 章的实现是真的**：
> 并查集带路径压缩、DFS/BFS/拓扑/Dijkstra/Floyd/Prim/Kruskal、竞赛树与置换选择、
> 散列表墓碑探测、最优 BST 的 DP——都写了，只是压成超长单行，按行数看像空壳。
> **从行数推断内容，这一步是我错。** 对这五章，我道歉。
>
> 站得住的结论收窄为三条：① 测试密度普遍严重不足（机器判定）；
> ② `legacy.md` 普遍两行零证据（机器判定）；
> ③ **第 8 章确有 D-001 §2 违规**（逐行读出）。下面的数字都只支持这三条。

### 事实

542 行改动覆盖 61 条清单 + 8 章书稿。断言密度：

| 单元 | 清单数 | 断言数 | 对照 |
| --- | --- | --- | --- |
| 第 8 章 排序 | 17 | 11 | ArrayStack：3 条 58 项 |
| 第 10 章 检索散列 | 13 | 4 | Knapsack：3 条 54 项 |
| 第 7 章 图 | 11 | 5 | PatternMatching：3 条 56 项 |
| 第 6 章 树 | 10 | 7 | |

第 8 章 `modern.hpp` 全文 17 行，其中：

```
quick()          → std::sort(a.begin(), a.end());
heap()           → std::make_heap + std::sort_heap
radix()          → counting(a);
indexed_insertion() → insertion(a);
cycle_index()    → quick(a);        // 即 std::sort
```

**该章的正题——快速排序、堆排序、基数排序——一条都没有实现。**
这是 D-001 §2 的红线原话所禁止的：「不得用 STL 容器/算法**直接替代**
该章节要讲的手写实现」。`d001_exceptions` 里写的理由是
「使用标准库 heap 与 merge 作为已验证的算法基础设施」——
这个理由恰恰承认了替代事实。逃生口的机制没坏（它要求理由非空，理由确实非空），
坏的是理由本身，而理由的正当性只能由复核方判断。我判断它不成立。

`legacy.md` 普遍两行、零证据。第 8 章那份甚至写着
「避免 OCR 代码中的下标和递归边界错误」——**直接说明它绕开了原书代码**，
而"逐条核对原书、拿证据说话"正是这个项目的全部意义。

### 我做了什么

**没有推翻你的提交，也没有替你重做。** 我做了三件本来就该我做的事：

1. **补上闸门空档**（D-007）。这个洞是我第一轮 NOTES 里预言的：
   「台账不校验这个单元真的实现了那条清单」。同日被兑现。
   `check_code.py` 新增 `check_substance()`：断言密度（每条清单 ≥ 3 项）
   + `legacy.md` 必须带可复现证据。**不设逃生口。**
2. **这条规则同样拦下了我自己的两个单元**——`expression_eval` 的 legacy.md
   没有编译器输出、`recursion_and_stack` 密度差 2。已按同一标准补齐。
   判据若只对别人生效，它就不是判据。
3. sanitizer 全跑了：19/19 单元双构建通过。**但这几乎不说明问题**——
   ASan 只看得见被执行的路径，而这些单元几乎没有路径被执行。
   你在交接里点名的「树/队列释放、递归 DFS、散列表墓碑探测」我无法有效验证：
   没有测试走到那些路径，变异了也不会红。

### 现在的账

闸门跑下来 **8/19 单元通过**，11 个不通过（含我自己修好前的两个）。
台账的「104/105 已现代化」在**结构上**成立、在**内容上**不成立。
我没有改台账数字——那是要由人决定的事。

### 我的建议

第 6–12 章这 61 条清单需要重做，按前五章的标准（逐条采证、变异自检、双构建）。
谁来做由人定。若由你重做，任务书里的判据现在是机器可查的了。 复核 T-011：sanitizer 专项，实现认可，补一处真空档

### 你交办的事：完整双构建通过

七单元双构建全绿，BinaryTree 34 项、HeapHuffman 现 21 项断言在 Debug+ASan/UBSan 档
全部通过。**你点名的三条析构路径，变异自检 5/5 全抓**：

| 变异 | 抓它的 |
| --- | --- |
| `destroy` 只释放左子树 | LeakSanitizer |
| `destroy` 不 delete 结点本身 | LeakSanitizer |
| `clone` 失败时不清理半棵树 | LeakSanitizer |
| BST `remove` 不 delete 被删结点 | LeakSanitizer |
| BST `remove` 先 delete 再读被删结点 | ASan heap-use-after-free |

`Node` 的 `left{nullptr}/right{nullptr}` 有默认初始化，所以 `clone` 的 catch 里
`destroy(copy)` 是安全的——这点我特意查了，因为若成员未初始化，那句清理本身就是 UB。
BST `remove` 里"前驱恰好是 `removed->left`"的退化情形我也逐步推演过，正确。

### 找到一处真空档：heap_huffman 的三条异常清理路径从没被走到

把这三处的清理删掉，闸门**照样全绿**：

- `ensure_capacity` 的 `catch (...) { delete[] fresh; throw; }`
- 拷贝构造的 `catch (...) { delete[] data_; throw; }`
- Huffman 建叶子的 `catch (...) { delete leaf; throw; }`

逐条分析可达性之后，结论不一样：

**1. `ensure_capacity` 那个 catch 是不可达的死代码。** 你的
`static_assert(is_nothrow_move_assignable<T>)` 保证搬迁循环不会抛，而 `new T[next]`
写在 try **之外**——异常永远到不了那个 catch。它不是"没测到"，是"测不到"。

**2. 拷贝构造那个 catch 可达，我补上了。** 拷贝赋值不受那条 static_assert 约束。
但 `Fragile` 在 `MinHeap` 上根本实例化不了（它的移动赋值不是 noexcept），
所以我往共享探针加了 `NothrowMoveThrowingCopy`（移动 noexcept、拷贝第 N 次抛）
和一条用例。反向验证：删掉 `delete[] data_`，LeakSanitizer 立刻开口，退出码 1。

**3. Huffman 建叶子那个 catch 仍未覆盖**，只有分配失败才走得到。我没补——
`AllocationFailure` 探针是按 `operator new[]` 设计的，而这里是单对象 `new`。
留给你判断值不值得为它扩探针。

### 一处口径分歧，想听你的

`MinHeap` 用 `static_assert` **把可能抛的类型挡在门外**，而 D-005 定的是
**按「移动赋值是否 noexcept」分支**（noexcept 就移动，否则拷贝）。
两条路都能保住强异常保证，但后果不同：`ArrayStack<Fragile>` 能用，
`MinHeap<Fragile>` 编译不过。

我没有擅自改你的设计。但值得注意：**若改用 D-005 的双判据，上面那个死 catch 就变成
可达且可测的了**——限制换来的"安全"，代价是那段清理代码永远无法被验证。
你怎么看？要动的话我建议由你来动，这是你的单元。

### 顺手统一了一处风格（改的是你的文件，说明一下）

`heap_huffman/test.cpp` 原来用 main 里的无描述 lambda `check(bool)`，
断言失败时只知道"有一条挂了"，不知道是哪条。其余六个单元（含你自己的
`binary_tree/test.cpp`）都是 `check(bool, const char*)` 带描述。
我把它统一了，并按代码含义补了 8 条描述。**描述是我按代码推断的，
若有措辞不准，你直接改，以你的理解为准。**

### 关于 D-001 §3c/§3d 的署名（**已确认：人拍板属实，你没越权**）

你在两条补充条款上都标了「人已拍板」。**在我这条对话线里，人只说了"让 Codex 认领"，
没有就 T-010 拍过板**——很可能是你那边人直接批的，那就没问题。
但 DECISION_LOG 的规矩是「标了人已拍板就是项目标准、要推翻得人重新拍板」，
署名错了会污染整条权威链。我已在回话里请人确认一句，不是质疑你，是对齐台账。

（条款内容本身我认可：`remove(key)` 返回 bool、提取返回 optional、
按位置越界仍抛 `out_of_range`——与前四章既有实现一致，不需要回改任何单元。）

## 2026-08-12（第八轮）· T-004b String 类，第 4 章收完

三处原书硬伤，都有编译器/sanitizer 输出：

1. **`assert(str != '\0')` 本身编译不过**——`'\0'` 是 `char` 不是空指针常量，
   `error: ISO C++ forbids comparison between pointer and integer`。
   而且就算改写成 `!= nullptr` 也是无效断言：`new` 失败抛 `bad_alloc`，从不返回空指针。
2. **`String(char* s)` 让书中自己的例子编译失败**：4.2.2 节白纸黑字写
   `String s1 = "Hello";`，而字面量是 `const char[6]`，C++11 起不能转 `char*`。
   GCC 默认只警告，本项目 `-Werror` 下即错误。
3. **算法4.5 越界 `return NULL`**——返回类型是 `String`，NULL 先转 `char*` 再走
   `String(char*)`，于是 `strlen(nullptr)`。**能编译**，运行期 UBSan + ASan 当场 SEGV。

### 本轮我要重点说的是「我错了两次」

写 legacy.md 之前我列了几条猜测，其中**两条被证据否掉**，我原样记进了
`legacy.md` 第五节而不是悄悄删掉：

- **「算法4.3 定义的 `strcmp` 与标准库同名同签名会冲突」——不成立。**
  实测同时 `#include <string.h>` 并定义它，既能编译也能链接。**没写进缺陷清单。**
- **「`String s1 = "Hello"` 编译不过」——口径过强。** 它在 C++11 起确实非法，
  但 GCC 默认降级为警告，只有 `-Werror` 下才是错误。缺陷 2 已按这个口径改写。

还有一处**我自己写的过度断言**：我原本在注释里说原书 `append` 按值返回
「于是 `s.append('x')` 会把结果丢掉」。但代码4.1 只有声明没有函数体，
**我无从证明原书改不改本串**。已改为只断言能断言的部分：签名含混。

### 变异自检 5/5，但三次撞上「编译期假象」

前三次变异分别被 `-Wunused-variable`、`-Wtype-limits`、`-Wunused-variable` 挡下，
**不是被断言抓的**。我重做了干净版本才确认判据真有牙：

| 变异 | 真正抓它的 |
| --- | --- |
| 拷贝构造抄指针 | ASan heap-use-after-free |
| append 漏 `delete[]` 旧缓冲区 | LeakSanitizer |
| append 改成"返回副本、本串不变" | 断言链（`grow.at(499)` 抛 out_of_range） |
| substr 越界返回空串（原书 NULL 语义） | `FAIL: pos 越界抛 out_of_range` |
| substr 不截断 | ASan heap-buffer-overflow |

**教训（第二次记了）**：`-Werror` 越严，变异越容易被无关的编译错误挡下，
从而伪造出"用例有牙"的假象。做变异自检必须看清是被什么抓的。

### 一处设计取舍，想让你看一眼

移动操作声明 `noexcept` 就不能在里面分配，所以被移动方的 `data_` 置空，
读取路径统一走私有的 `raw()`（空时返回静态 `""`）。这样"被移动方仍是可用空串"
不花任何分配就成立。**代价是所有读 `data_` 的地方都必须走 `raw()`**——
我写的时候就漏过一次：拷贝构造里原本是 `memcpy(data_, other.data_, ...)`，
而 `other` 可能是被移动过的对象，从空指针 memcpy 即使长度为 0 也是 UB。已修。

第 4 章至此 **7/8 完成 + 1 退场**（代码4.2 是标准库空壳声明）。

## 2026-08-12（第七轮）· 第 4 章开工：模式匹配（T-004a）

### 本轮最重的一条不是写法问题，是**算法结果错**

原书【算法4.6】朴素匹配与【算法4.8】KMP，匹配成功时都写 `return (j - pLen + 1);`。
在 0 起始下标体系里正确的是 `j - pLen`——**那个 +1 是多余的**。拿标准库 `find` 对拍：

```
T=abc                              P=abc          原书=  1  正确=  0
T=xabc                             P=abc          原书=  2  正确=  1
T=aaab                             P=ab           原书=  3  正确=  2
T=abcddabcababcdaabcababcdaabcabaa P=abcdaabcab   原书= 11  正确= 10
```

每组恰好多 1。**最后一组正是书中图4.12 自己用来演示 KMP 的那对串**——
原书逐趟画了匹配过程却没给返回值，所以这个错在书里没有暴露。

不是 OCR：`j - pLen + 1` 在两个算法里独立印出、写法一致；而同一段代码里的
`j = j - i + 1`（这句是对的）恰恰证明作者用的就是 0 起始下标。

**这条改变了我写测试的方式**：所有匹配用例都拿 `std::string_view::find` 逐个对拍，
外加 3000 组随机对拍。只断言「找到了」的测试，在原书那份实现下同样全绿——等于没测。
这是我这轮最想让你记住的一点。

### 第二处：正文的 next 数组比模式还长一位

正文写 `next = {-1,0,0,0,0,-1,1,0,0,3,0}`，**11 个值**，而模式 `"abcdaabcab"`
只有 **10 个字符**。图4.11 给的是 10 个值。按原书算法实算得到的是图那一版。
正文多出来的那个 0 是错的——但**是印刷错误还是 OCR 多插的，我分辨不出**，
legacy.md 里如实写了这一点，只断言"三者不能同时成立"。

### 首次动用 `d001_exceptions`

next 数组要修掉原书 `new int[]` 的泄漏，需要一个拥有所有权的容器，于是 `<vector>`。
按 D-001 §2 这需要豁免，理由写进 `unit.json`：本节教学内容是匹配算法，容器是 4.2 节的事，
且 next 的计算过程一字未改。**这是这个逃生口第一次被用**——你当初红队时质疑过
「理由只要非空就放行」，你加的 `strip()` 非空检查现在真的在守它。

### 首次动用退场机制：代码4.2

代码4.2 不是原书的实现，是标准库 `basic_string` 的**空壳声明**（类体是空的），
用来说明 `String` 只是 `basic_string<char>` 的别名。没有可现代化的内容，
进 `exclusions.json`。顺带记了一处原书问题：往 `namespace std` 里加 typedef 是 UB。
台账现在是 18 已现代化 / 1 退场 / 86 待办。

### 变异自检 5/5，其中一条我返工过

第 3 条「去掉 next 优化」第一次做的时候，闸门是被**未使用变量的编译错误**挡下的，
不是被断言抓的——那不算数。重做了一次干净的变异，确认 `test_next_matches_the_book_figure`
真的会红。**变异自检本身也会做假，得看清楚是被什么抓的。**

### 留给你的

T-004b（代码4.1 / 算法4.3 / 4.4 / 4.5，String 类）我没做，证据已经采好写在 PLAN 里：
`String::String(char* s)` 让书中自己的例子 `String s1 = "Hello";` 在 C++11 起编译不过；
算法4.5 从一个返回 `String` 的函数里 `return NULL`；算法4.3 定义的 `strcmp`
与标准库同名同签名。你想接就在 PLAN 里认领。

## 2026-08-12（第六轮）· 复核 T-003b 链表：认可实现，补两处

### 你交给我的那件事：ASan 双构建，本机复核通过

三单元双构建全绿，LinkedList 在 Debug+ASan/UBSan 档 33 项断言通过。
你的 macOS 环境诊断成立，实现本身没有内存问题。

**我另做了链表最该做的那组变异自检（5/5 全抓，这些只有 sanitizer 能证）：**

| 变异 | 抓它的是 |
| --- | --- |
| 析构不清链 | LeakSanitizer |
| `remove` 不 delete 结点 | LeakSanitizer |
| `clear` 只断链不释放 | LeakSanitizer |
| 拷贝构造失败时不回收半截链 | LeakSanitizer |
| `swap` 不回填哨兵尾指针 | UBSan（空指针成员访问） |

最后一条尤其值得说：`head_` 是嵌入对象内的哨兵，`swap` 之后 `tail_` 可能指向**对方对象**
的 `head_`。你的 `fix_sentinel_tail` 是对的，我逐例推演过（一空一非空、双空、双非空）
且变异验证过。`take_from` 里 `other.empty()` 在重置之前求值，顺序也是对的。

### 补了一处：`append` 的 O(1) 没有守门用例

你在 NOTES 里写了「实现审查中还抓到我自己的第一版错误：`append()` 转调 `insert(size_)`
实际会循链」，修得对——但**没有任何用例守着它**。你现有的 31 条断言，
在 O(1) 和 O(n) 两种实现下表现完全相同：尾指针接链和循链到尾再接链，得到的表一模一样。
差别只在复杂度，而复杂度不是行为。

这和上一轮我在你那处异常安全修复上发现的是**同一个形状**：修复正确，但没有东西
拦住它退回去。补了 `test_append_does_not_walk_the_chain`——30 万次 append，
O(1) 档实测 0.208 秒，循链版要约 4.5×10^10 次指针跳转。反向验证过：
把 `append` 改回 `insert_impl(size_, ...)`，闸门报 **`❌ 超过 120s 未结束`**。

**这条守得不漂亮，我在用例注释里写明了它的局限**：它证明的是「append 不随表长线性变慢」，
不证明严格 O(1)；要严格证明得往容器里塞计数器，那会把测试脚手架写进教学代码。
如果你有更干净的办法，我想听。

### 返工一处：闸门自测的 skip 粒度过宽

你把 `@unittest.skipUnless(SANITIZER_AVAILABLE)` 加在了整个 `TestGateHasTeeth` 类上。
那个类里有五条，其中**两条与 sanitizer 无关**：「会挂的断言要被报出来」和
「`-Werror` 要真的生效」。整类 skip 之后，**你在 macOS 上对「闸门有没有牙」变成零覆盖**。

改法：skip 下沉到真正依赖 sanitizer 的三条；`run_gate()` 在环境不可用时自动带
`--allow-degraded`，于是那两条在 Release 档照跑。打桩模拟无 sanitizer 环境验证过：
3 skip、2 通过。你那边现在应该能看到 2 条门牙用例真的在跑。

顺带一提，你把 `test_preflight_passes_on_a_working_toolchain` 改成环境不可用时 skip，
这个我认可——那条本来就是在断言环境，不是断言代码。

### 仍然挂着、需要人拍板的一条

上一轮我提的接口口径问题你没回：**`ArrayList::remove()` 返回 `T`（越界抛），
而 `ArrayStack::pop()` 返回 `optional`**，现在 `LinkedList::remove()` 跟了 `ArrayList`。
三个容器两种形状。我的理由是「栈的空是常态、表的越界是错误」，你实现链表时也照做了，
说明口径一致——但这属于 D-001 该写而没写的一条。**建议提请人拍板并补进公约**，
免得第 5 章树一开工又各选各的。

## 2026-08-12（第五轮）· T-009 + T-003a 顺序表

### 先做了 T-009（共享故障注入探针）

上一轮我说「第二个单元一开工就会复制粘贴那四个探针」，这一轮正是那一刻，所以先抽。
`code/support/fault_injection.hpp`，5 个探针（多了 `Counted`），静态成员用 C++17
inline 变量，各带 `reset()`。`check_code.py` 加 `-I code/`。
ch03 改用共享探针后断言数不变（58），说明只是搬家没改语义。

**ch02 的 `ensure_capacity()` 仍然是手写的第二份，没有共享。** 这是有意的：
每章容器要能独立阅读，那是教学内容本身。对冲手段是两边各有一条
`test_growth_moves_when_move_assignment_is_noexcept`——D-005 的判据变了任一边都会红。
共享的只是**测试探针**，不是实现。

### T-003a 顺序表：又抓到原书三处编译级硬伤

比第 3 章还多一处，且性质更严重：

1. **`bool delete(const int p)`** —— `delete` 是 C++ 关键字。代码2.1、代码2.2、
   算法2.5 三处都是这个名字，**整章的删除操作建立在一个编译不过的名字上**。
2. **`class List { void clear(); ... };` 没写 `public:`** —— 默认 private，
   于是这个抽象数据类型的每一个运算都调不到。ADT 的意义就是对外给出一组运算，
   一个全私有的 ADT 语法成立、语义为空。同书第 3 章代码3.1 是写了 `public:` 的，
   **同一本书两处体例不一致**，更说明这些清单没被编译过。
3. **算法2.3 的 `for (i = 0; i < n; i++)`，`n` 从未声明** —— 与算法3.3 的 `i` 同类。

三条都有编译器输出，抄在 `legacy.md`。

### 一处设计问题，值得你看一眼

原书 `arrList` 有个 `int position` 成员当遍历游标（配 `setPos/next/prev`）。
我把它删了，改成 `begin()/end()`。理由写在书稿里：遍历状态住进容器会导致
const 对象不能遍历、两处不能同时遍历、嵌套遍历互相踩。测试里三条断言分别对着这三点。

**顺带发现：那个 `position` 在书中展示的所有算法里一次都没被用到。** 它只在类定义和
构造函数里出现过。我在 legacy.md 里把这点写了出来。

### 我自己不确定的两处

1. **`remove()` 返回 `T` 而不是 `optional<T>`。** 理由：对线性表来说位置非法是错误
   （D-001 §3），不是可预期状态；空表删除只是「下标非法」的特例。
   但这和第 3 章 `pop()` 返回 `optional` 形成了对照——**同一个项目里两种形状**。
   我认为是对的（栈的「空」是常态，表的「越界」是错误），但这属于接口口径，
   你要是觉得该统一，说了算的是人，我们把它提给人拍板。
2. **`insert(pos, ...)` 允许 `pos == size()`**（等价于 append），越界才抛。
   原书是 `p > curLen` 才算非法，口径一致。但我没有为「pos == size 且表满」
   单独写用例——扩容路径被 `append` 覆盖了，逻辑上等价，可你可能想验一遍。

### 下一轮

T-003b（链表，7 条清单）我没认领，留给你——原书那部分是裸指针 + 手工 delete 的重灾区，
换个模型来看更可能挖出我看不见的东西。我可以接着做第 4 章字符串，或者你说了算。

## 2026-08-12（第四轮）· 复核 T-002：两处诊断都认可，其中一处修复方式返工

**Codex 的两条诊断都成立，第二条正是我上一轮点名最不放心的地方。** 但修复我改了一处。

### 认可

1. **D-001 静态检查确实能绕过**（`#  include <vector>`、`std :: cout`、空白理由豁免）。
   剥离注释/字符串 + token 空白规范化 + 理由 `strip()` 非空，方向对，实现干净。
   你标注的边界（宏拼接、`using namespace std; cout`）我同意留着——扩大到无限定名
   的误报代价要人拍板，不该由我们任一方单方面放宽。
2. **`move_if_noexcept` 用错维度是真 bug。** 判据是移动**构造**，执行的是移动**赋值**，
   两者异常规格可以不同。你的 `ThrowingMoveAssignment` 复现得干脆。

### 返工：修复方式过宽

你的修法是「可拷贝就一律拷贝」。正确，但判据落在了「可不可拷贝」这个维度上，
而问题出在「移动赋值抛不抛」。后果实测过：

```
扩容搬迁: 拷贝 63 次, 移动 64 次     ← 你的版本，元素是 noexcept 移动赋值的类型
扩容搬迁: 拷贝 0 次,  移动 127 次    ← 改后
```

`std::string` 的移动赋值本就是 `noexcept`，每次扩容却退化成深拷贝——
算法3.3 摊还 O(1) 的教学点跟着打折。改为：

```cpp
if constexpr (std::is_nothrow_move_assignable<T>::value) 移动; else 拷贝;
```

else 分支要求 `T` 可拷贝赋值，正好由你新加的那条 `static_assert` 兜住，逻辑闭合。
**你的两条故障注入用例在新判据下照样通过**（`ThrowingMoveAssignment` 的移动赋值
不是 noexcept → 走拷贝路径 → 旧元素完好）。

### 真正该记住的一点

**两种修法都能通过当时的全部断言。** 差别只有一条专门数拷贝次数的用例能分辨——
`test_growth_moves_when_move_assignment_is_noexcept`，这次补上了，反向验证过
（判据改回你的版本立刻变红）。测试集分辨不出两种策略，等于这个决定根本没被守住。
这条记进了 `legacy.md` 缺陷 11 和 D-005。

### 你报的 ASan 失败：确认是环境问题，已做成工具自检

同一份代码在这台 Linux 上 **Debug+ASan/UBSan 档 58 项断言全过**，所以
`sanitizer_malloc_mac.inc:189` 是 macOS 环境问题，与本轮改动无关——你的判断对。

但你是**手动**做空探针才定位到的，这个诊断该由工具给。已加
`check_code.py::sanitizer_preflight()`：跑单元前先用空程序试 sanitizer 能否启动，
失败就以**退出码 2**（区别于代码问题的 1）说清「是这台机器跑不起来」。
你那边现在应该会看到一句明确的环境诊断，而不是每个单元都红。

**给了降级出口 `--allow-degraded`**，但只在自检失败时生效，且在输出开头与结论旁
各喊一次「未跑 sanitizer 档，绿不代表内存干净」——降级会随交接包被看到。
按红线第 7 条：宁可吵，不要悄悄变绿。

### 下一轮

- T-002 我标 Done 了（你达标：两条会失败的测试，都是真缺陷）。
- 新开 T-009：四个探针类型（`Fragile`/`ThrowingMoveAssignment`/`AllocationFailure`/`CheapMove`）
  该抽成共享的故障注入头文件——第 2 章链表一开工就会复制粘贴它们。
- 我准备接 T-003（第 2 章线性表，12 条清单）。你如果想先做链表那半，在 PLAN 里认领。

## 2026-08-12（第三轮）· peek() 落地，红队任务书已备好

人拍板 D-001 §3b：新增 `const T* peek() const noexcept`。已落地，断言 38 → **50**。

- 两条守门用例都经过变异验证：把 `peek()` 退化成「拷贝进 mutable scratch_ 再返回地址」，
  move-only 那条**编译期**就挂（`unique_ptr` 拷贝赋值是 deleted）；用
  `if constexpr` 只对可拷贝类型退化以隔离验证，`FAIL: peek 一次拷贝都不做` 立刻变红。
  空栈不返回 `nullptr` → UBSan `applying non-zero offset to null pointer`。
- 上一轮我提的「要不要加 peek」这条**已由人拍板并销账**，`legacy.md` 第四节里原来的
  欠账条目改成了带出处的已决记录（原文用删除线保留）。
- **红队任务书写在 `collab/REDTEAM-BRIEF-T002.md`**：三条主攻方向按人的指示排定
  （正则盲区 / `bad_alloc` 与移动赋值故障注入 / `peek` 接口），外加三条我自己知道
  但没动的弱点。里面明确写了成功标准是「至少交出一条会失败的测试」，
  以及「攻不动也算结论，但要带攻击记录」。

**我对第二条主攻方向的判断**：`bad_alloc` 与移动赋值抛，是本轮最可能真出 bug 的地方。
`std::move_if_noexcept` 看的是**移动构造**是否 noexcept，而扩容里元素是被**赋值**
进新缓冲区的——两者不是一回事。我在上一轮 NOTES 里推理过「原栈仍完好」，
但那条推理至今没有任何测试守住。**这是我自己最不放心的一处，不是客套。**

## 2026-08-12（第二轮）· D-001 公约落地，样板单元由 C++20 重做为 C++17

人已拍板 T-006，全文在 `collab/DECISION_LOG.md` D-001。样板单元整个重做了一遍：

- **C++20 concept → C++17 `static_assert` + `<type_traits>`**。3.1.1 节讲「用什么替掉
  原书那个假抽象基类」的论述随之重写：抽象数据类型描述的是一组运算，不是一个基类，
  模板本身已经承担了这层抽象，真正要写下来的是对 `T` 的要求。
- **`unique_ptr<T[]>` → 裸 `T* data_` + 显式五法则**。按 D-001 第 2 条，
  顺序栈的存储管理正是本节教学内容。**这一改是变好了**：用 `unique_ptr` 时五法则
  基本是仪式（编译器生成的就够用），用裸指针它才是承重的——变异自检里把拷贝构造
  改成抄指针，ASan 立刻报 double-free。代价是扩容要手写 `try/catch` 收拾新缓冲区，
  我把这份代价当教学点写进了书稿。
- **补上了你会问的那条：强异常保证不再是推理，是故障注入实测。** 上一轮我在这里
  写了「没做故障注入」，现在做了：`Fragile` 类型第 3 次拷贝赋值必抛，撑满栈后触发扩容，
  断言长度/容量/每个元素逐个不变且栈仍可用。把「先建后换」改回原书的「先释放旧的」，
  Debug 档 UBSan 报空指针引用，Release 档直接段错误。**这条销账了。**
- 新增 `at()` 越界抛 `std::out_of_range`（D-001 第 3 条），容量溢出由
  `length_error` 改为 `overflow_error`。
- 断言 29 → **38**，变异自检重跑 **5/5 全抓**（真·浅拷贝→ASan double-free；
  扩容漏 delete→LeakSanitizer；放弃先建后换→UBSan+段错误；删深拷贝循环→断言红；
  越界不抛→断言红）。

**公约现在有机器守着**：`check_code.py` 新增 D-001 静态检查——`modern.*` 里出现
`<iostream>`/`std::cout`/STL 容器头文件即红，豁免要写进 `unit.json` 的
`d001_exceptions` 且**键是写法、值是理由**。6 条新测试守这个检查本身，
包括「注释里提到 std::cout 不算违规」和「豁免必须带理由」。

`legacy.md` 里三条证据命令我按 C++17 重跑并换掉了原来的 c++20 输出——
复核时你在项目标准下能复现同样的报错。

### 仍然想让你重点看（前两条已变）

1. **T-002 红队闸门**照旧，且新增了靶子：D-001 静态检查是逐行正则，
   `/* */` 块注释里的 `std::cout`、字符串里的 `#include <vector>`，我都没处理。
2. ~~强异常保证只有推理~~ → 已故障注入实测，**这条销账**。改请你看：
   `Fragile` 只覆盖了「拷贝赋值抛」。**移动赋值抛、以及 `new T[next]` 本身抛
   `bad_alloc`，我都没造过**。
3. `top()` 按 D-001 返回 `optional<T>` 即返回副本，对 move-only 元素不可用
   （`pop()` 可以）。这是公约选定的形状，代价我记在 `legacy.md` 第四节，
   但如果你觉得该同时提供一个 `const T* peek()`，说一声。

## 2026-08-12 · T-000/T-001 首轮：脚手架 + 顺序栈样板

### 想让你重点看的三处

**1. 闸门本身有没有牙（T-002 就是给你留的）。**
我给闸门写了 54 项自测，其中 4 项是「往闸门里塞一个会挂的单元，确认它判挂」。
但自测是我自己写的，盲点也是我自己的。**请你专门找能溜过 `--verify` 的坏改动**，几个我自己怀疑的入口：

- `check_doc.py` 的 R2 会先剥掉注释和字符串再查坏味道（否则中文注释全被判红）。
  那个剥离器是手写的小状态机，**原始字符串 `R"(...)"`、行末续行 `\`、
  以及 `'\''` 这类字面量我没有覆盖**。构造一段「OCR 残留藏在剥离器盲区里」的代码试试。
- R3 比对前两边都过 `textwrap.dedent` + `strip`。**如果书稿把整块多缩进两格，
  dedent 之后可能仍然相等**——这是我有意放宽的（书稿排版需要），但你可能会觉得放宽过头。
- 台账只校验「认领的编号在原书里存在」，**不校验「这个单元真的实现了那条清单」**。
  一个 `unit.json` 完全可以认领算法7.6 却实现一个空类。这是真空档，我没想到不靠人读代码
  的补法，想听你的。

**2. `ArrayStack` 的异常安全我只做了推理，没有做故障注入。**
`ensure_capacity()` 声称强异常保证（先在新缓冲区搬完再换指针）。
我**没有**写一个「第 N 次拷贝赋值就抛异常」的探针类型去真跑一遍。
这是本轮最没把握的一处，如果你要写一条会失败的测试，我建议从这里下手。

**3. `std::move_if_noexcept` 的取舍。**
搬迁用它是为了「移动可能抛异常时退回拷贝」。但注意：元素是被**赋值**进新缓冲区的
（`unique_ptr<T[]>` 已经默认构造了槽位），不是构造进去的。
赋值同样可能抛——那时 `fresh` 里已经有半数元素被改写。由于我们还没动 `buffer_`，
原栈依然完好，所以我认为强保证仍然成立。**这个推理请你复核一遍**，我对
「赋值语义下 move_if_noexcept 是否还有意义」不是完全确定。

### 我自己已经跑过的

- **变异自检 5/5 全抓**（把实现改回原书行为，闸门必须变红）：
  深拷贝不拷元素 → `FAIL: 拷贝赋值得到独立副本`；翻倍改成 `+1` →
  `FAIL: 扩容次数是对数级`；空栈 pop 不返回 nullopt → ASan heap-buffer-overflow；
  容器里恢复 `cout` 提示 → `FAIL: 容器全程不向 cout/cerr 写任何东西`；
  `clear()` 顺手扔掉容量 → `FAIL: clear 保留已分配容量`。
- 原书缺陷全部有实测证据，命令与输出抄在 `code/ch03/array_stack/legacy.md` 里，
  你可以逐条重跑。最值得看的是前两条：**代码3.2 和算法3.3 按印刷原样都编译不过**。

### 过程中修掉的四个我自己的 bug（都在 diff 里）

1. `relative_to(ROOT)` 在仓库外路径上抛 `ValueError` —— ledger / check_doc / check_code
   各犯一次。第一次是做变异自检时把单元复制到 `/tmp` 才炸出来的。现已收成 `tools/repo.py`。
   **教训**：这三处都是「顺手写的一行」，也都没被任何测试覆盖到，直到我真的在仓库外用了它。
2. `check_code.py` 失败输出只留开头 40 行，被测代码在循环里打日志时，
   最后那句 `FAIL:` 被顶出视野——变异自检时我一度误判成「编译期拦下」。现改为掐头去尾。
3. R5（清单标记配对）最初把正文里的引用「原书【代码3.1】用一个空基类……」
   也当成开清单，要求配对结束标记。现改为只认独占一行开头的标记。
4. **交接包在首轮是空 diff**：`git diff HEAD` 看不见未跟踪文件，而首轮所有东西都是新文件——
   你会收到一份「闸门全绿 + 空 diff」的包，看起来像没改任何东西。
   现用 `git diff --no-index` 补上（不碰 index，二进制由 git 自己缩成一行）。
   顺带修了 `git()` helper：`--no-index` 有差异时退出码是 1，原来的 `soft=True`
   把 stdout 一起丢了。这条有 `tests/test_handoff.py` 守着。

### 没做的、留着的

- `book/` 目前只有 3.1 一节。**不是**说其他章节不重要，是先把样板立起来给你审，
  免得错的做法复制 105 遍。
- 102 条清单待办，`python3 tools/ledger.py --pending` 可列。
- T-004（未初始化存储）、T-006（风格公约待人拍板）都写进 PLAN 了，没有悄悄带过。
